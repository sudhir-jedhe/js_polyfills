*** copy 04-openai-spec-generation.md ***

Use @specs/04-openai-spec-generation.md as the source of truth.

Manual setup already completed:

- OpenAI API key is present in `server/.env`.
- OpenAI model value is present in `server/.env`.

Implement only Section 04.

Do not add specs routes, database write logic, frontend UI, workers, queues, dashboard APIs, notification APIs, or test-only endpoints.

Update @context/tracker.md when done.
s
