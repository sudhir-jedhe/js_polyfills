***  03a-google-oauth-start.md ***

Use @specs/03a-google-oauth-start.md as the source of truth.

Manual setup already completed:

- Google Cloud OAuth setup is done.
- Required Google OAuth values are present in `server/.env`.

Implement only Section 03A.

Do not add OAuth callback, JWT cookies, /auth/me, logout, frontend auth, OpenAI, specs APIs, dashboard APIs, notification APIs, or test-only endpoints.

Update @context/tracker.md when done.
