*** copy 03b-oauth-callback-jwt-session.md ***

Use @specs/03b-oauth-callback-jwt-session.md as the source of truth.

Manual setup already completed:

- Google OAuth values are present in `server/.env`.
- Section 03A OAuth start route is complete.

Implement only Section 03B.

Do not add frontend auth UI, OpenAI, specs APIs, dashboard APIs, notification APIs, or test-only endpoints.

Update @context/tracker.md when done.
