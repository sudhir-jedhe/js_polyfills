*** copy 05-spec-create-async-generation.md ***

Use @specs/05-spec-create-async-generation.md as the source of truth.

Implement only Section 05.

Do not add GET /specs, GET /specs/:id, dashboard stats APIs, notification list/read APIs, frontend UI, workers, queues, or test-only endpoints.

Update @context/tracker.md when done.
