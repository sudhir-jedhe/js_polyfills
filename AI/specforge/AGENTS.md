*** copy AGENTS.md ***

# SpecForge Agent Guide

Use the project rules in `.cursor/rules/`.

For implementation work, follow the active section spec in `specs/` as the source of truth.

Keep each change scoped to the active section. Do not build future sections early.

After completing a section, update `context/tracker.md` with the completed section, next section, important decisions, and known issues.
