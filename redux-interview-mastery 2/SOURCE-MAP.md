# Source Map — js_polyfills/Redux → this repo

18 of your standalone notes were physically copied in (`from-your-notes/` under each matching topic).

## Mapped — worth checking by hand

| New topic | Check in `js_polyfills/Redux/` |
|---|---|
| `05-redux-toolkit` | `Redux ToolKit Toodo/`, `school management system redux ToolKit/` — real projects you already built, great to compare against `05-redux-toolkit/projects/rtk-counter-cart/` |
| `06-async-redux-thunks` | `Redux Crud Thunk/`, `Redux Crud Saga/` — direct side-by-side of both approaches you've already implemented |
| `02-actions-reducers` | `Redux Todo/`, `Redux/` (generic dedicated folder) |
| *(reference PDFs/images, not auto-categorized)* | 3 numbered PDFs + 1 image at the folder root — worth a manual skim |

## What wasn't touched

Nothing outside `js_polyfills/Redux` was touched.
