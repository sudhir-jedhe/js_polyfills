***  1.md ***

Here is a self-contained, fully functional React CRUD application using functional components and the `useState` hook. It allows you to **Create**, **Read**, **Update**, and **Delete** simple task/item records.

### Complete React CRUD Component

```jsx
import React, { useState } from 'react';

export default function SimpleCrudApp() {
  // State for items array (Read)
  const [items, setItems] = useState([
    { id: 1, title: 'Learn React Hooks', category: 'Work' },
    { id: 2, title: 'Buy groceries', category: 'Personal' },
  ]);

  // Form input state (Create / Update)
  const [formData, setFormData] = useState({ title: '', category: 'Work' });

  // State to track if we are editing an item
  const [editingId, setEditingId] = useState(null);

  // Handle Input Changes
  const handleInputChange = (e) => {
    const { name, value } = e.target;
    setFormData((prev) => ({ ...prev, [name]: value }));
  };

  // CREATE or UPDATE Item
  const handleSubmit = (e) => {
    e.preventDefault();
    if (!formData.title.trim()) return;

    if (editingId !== null) {
      // UPDATE logic
      setItems(
        items.map((item) =>
          item.id === editingId ? { ...item, ...formData } : item
        )
      );
      setEditingId(null);
    } else {
      // CREATE logic
      const newItem = {
        id: Date.now(),
        ...formData,
      };
      setItems([...items, newItem]);
    }

    // Reset Form
    setFormData({ title: '', category: 'Work' });
  };

  // Populate Form for UPDATE
  const handleEdit = (item) => {
    setEditingId(item.id);
    setFormData({ title: item.title, category: item.category });
  };

  // DELETE Item
  const handleDelete = (id) => {
    setItems(items.filter((item) => item.id !== id));
    if (editingId === id) {
      setEditingId(null);
      setFormData({ title: '', category: 'Work' });
    }
  };

  // Cancel Editing Mode
  const handleCancel = () => {
    setEditingId(null);
    setFormData({ title: '', category: 'Work' });
  };

  return (
    <div style={styles.container}>
      <h2>React CRUD Application</h2>

      {/* CREATE / UPDATE FORM */}
      <form onSubmit={handleSubmit} style={styles.form}>
        <h3>{editingId ? 'Edit Item' : 'Add New Item'}</h3>
        <div style={styles.inputGroup}>
          <input
            type="text"
            name="title"
            placeholder="Item title..."
            value={formData.title}
            onChange={handleInputChange}
            style={styles.input}
            required
          />
          <select
            name="category"
            value={formData.category}
            onChange={handleInputChange}
            style={styles.select}
          >
            <option value="Work">Work</option>
            <option value="Personal">Personal</option>
            <option value="Shopping">Shopping</option>
          </select>
          <button type="submit" style={styles.btnSubmit}>
            {editingId ? 'Update' : 'Add'}
          </button>
          {editingId && (
            <button type="button" onClick={handleCancel} style={styles.btnCancel}>
              Cancel
            </button>
          )}
        </div>
      </form>

      {/* READ / DISPLAY LIST */}
      <div style={styles.list}>
        <h3>Items List ({items.length})</h3>
        {items.length === 0 ? (
          <p>No items available. Add one above!</p>
        ) : (
          <ul style={{ listStyle: 'none', padding: 0 }}>
            {items.map((item) => (
              <li key={item.id} style={styles.listItem}>
                <div>
                  <strong>{item.title}</strong>{' '}
                  <span style={styles.badge}>{item.category}</span>
                </div>
                <div>
                  <button
                    onClick={() => handleEdit(item)}
                    style={styles.btnEdit}
                  >
                    Edit
                  </button>
                  <button
                    onClick={() => handleDelete(item.id)}
                    style={styles.btnDelete}
                  >
                    Delete
                  </button>
                </div>
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  );
}

// Basic inline styling
const styles = {
  container: { maxWidth: '500px', margin: '20px auto', fontFamily: 'sans-serif' },
  form: { padding: '15px', border: '1px solid #ccc', borderRadius: '8px', marginBottom: '20px' },
  inputGroup: { display: 'flex', gap: '8px', marginTop: '10px' },
  input: { flex: 2, padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  select: { flex: 1, padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  btnSubmit: { padding: '8px 16px', background: '#007bff', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' },
  btnCancel: { padding: '8px 12px', background: '#6c757d', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' },
  list: { padding: '15px', border: '1px solid #ccc', borderRadius: '8px' },
  listItem: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '10px 0', borderBottom: '1px solid #eee' },
  badge: { fontSize: '0.8em', background: '#e0e0e0', padding: '2px 6px', borderRadius: '4px', marginLeft: '8px' },
  btnEdit: { marginRight: '6px', background: '#ffc107', border: 'none', padding: '4px 8px', borderRadius: '4px', cursor: 'pointer' },
  btnDelete: { background: '#dc3545', color: '#fff', border: 'none', padding: '4px 8px', borderRadius: '4px', cursor: 'pointer' },
};

```

### Key Operations Breakdown

1. **Create (`handleSubmit`)**: Appends a new item object with a unique timestamp-based ID to the `items` array.
2. **Read (`map`)**: Renders the current `items` array using standard JSX mapping.
3. **Update (`handleEdit` & `handleSubmit`)**: Populates `formData` with the selected item's values and sets `editingId`. Upon submission, updates the matching item in state.
4. **Delete (`handleDelete`)**: Filters out the target item by `id` from the array.

Connecting a React CRUD application to an external REST API replaces local state modification with asynchronous API calls using `fetch` or `axios`.

Here is a complete, refactored implementation that connects the CRUD operations to a real fake backend API ([JSONPlaceholder](https://www.google.com/url?sa=E&source=gmail&q=https://jsonplaceholder.typicode.com/todos)).

---

### Implementation Options

You can use standard JavaScript `fetch` or install **Axios** (`npm install axios`). Below is the complete component structured with clean, modern `async/await` handling, loading indicators, and error handling.

```jsx
import React, { useState, useEffect } from 'react';
import axios from 'axios';

// Demo REST API endpoint
const API_URL = 'https://jsonplaceholder.typicode.com/todos';

export default function ApiCrudApp() {
  const [items, setItems] = useState([]);
  const [formData, setFormData] = useState({ title: '' });
  const [editingId, setEditingId] = useState(null);
  
  // UI States for Async Operations
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  // ---------------------------------------------------------
  // 1. READ: GET Request on Component Mount
  // ---------------------------------------------------------
  useEffect(() => {
    fetchItems();
  }, []);

  const fetchItems = async () => {
    setLoading(true);
    setError(null);
    try {
      // --- Option A: Axios ---
      const response = await axios.get(`${API_URL}?_limit=5`);
      setItems(response.data);

      /* --- Option B: Fetch API equivalent ---
      const res = await fetch(`${API_URL}?_limit=5`);
      if (!res.ok) throw new Error('Failed to fetch items');
      const data = await res.json();
      setItems(data);
      */
    } catch (err) {
      setError(err.message || 'Something went wrong while fetching');
    } finally {
      setLoading(false);
    }
  };

  // Handle Input Changes
  const handleInputChange = (e) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

  // ---------------------------------------------------------
  // 2. CREATE (POST) & UPDATE (PUT/PATCH)
  // ---------------------------------------------------------
  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!formData.title.trim()) return;

    setLoading(true);
    setError(null);

    try {
      if (editingId !== null) {
        // --- UPDATE (PUT Request) ---
        // Option A: Axios
        const response = await axios.put(`${API_URL}/${editingId}`, {
          title: formData.title,
          completed: false,
        });

        /* Option B: Fetch API
        const res = await fetch(`${API_URL}/${editingId}`, {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ title: formData.title, completed: false }),
        });
        const updatedItem = await res.json();
        */

        setItems(items.map(item => (item.id === editingId ? response.data : item)));
        setEditingId(null);
      } else {
        // --- CREATE (POST Request) ---
        // Option A: Axios
        const response = await axios.post(API_URL, {
          title: formData.title,
          completed: false,
        });

        /* Option B: Fetch API
        const res = await fetch(API_URL, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ title: formData.title, completed: false }),
        });
        const newItem = await res.json();
        */

        // Add response item to UI list
        setItems([response.data, ...items]);
      }

      setFormData({ title: '' });
    } catch (err) {
      setError(err.message || 'Failed to save item');
    } finally {
      setLoading(false);
    }
  };

  // ---------------------------------------------------------
  // 3. DELETE (DELETE Request)
  // ---------------------------------------------------------
  const handleDelete = async (id) => {
    setLoading(true);
    setError(null);

    try {
      // --- Option A: Axios ---
      await axios.delete(`${API_URL}/${id}`);

      /* --- Option B: Fetch API ---
      const res = await fetch(`${API_URL}/${id}`, { method: 'DELETE' });
      if (!res.ok) throw new Error('Failed to delete item');
      */

      // Remove from UI after successful API call
      setItems(items.filter(item => item.id !== id));
      
      if (editingId === id) {
        setEditingId(null);
        setFormData({ title: '' });
      }
    } catch (err) {
      setError(err.message || 'Failed to delete item');
    } finally {
      setLoading(false);
    }
  };

  const handleEdit = (item) => {
    setEditingId(item.id);
    setFormData({ title: item.title });
  };

  const handleCancel = () => {
    setEditingId(null);
    setFormData({ title: '' });
  };

  return (
    <div style={styles.container}>
      <h2>REST API CRUD Application</h2>

      {/* Error & Loading Display */}
      {error && <div style={styles.error}>{error}</div>}
      {loading && <p style={styles.loading}>Connecting to server...</p>}

      {/* FORM */}
      <form onSubmit={handleSubmit} style={styles.form}>
        <h3>{editingId ? 'Edit Item' : 'Add New Item'}</h3>
        <div style={styles.inputGroup}>
          <input
            type="text"
            name="title"
            placeholder="Item title..."
            value={formData.title}
            onChange={handleInputChange}
            style={styles.input}
            disabled={loading}
            required
          />
          <button type="submit" style={styles.btnSubmit} disabled={loading}>
            {editingId ? 'Update' : 'Add'}
          </button>
          {editingId && (
            <button type="button" onClick={handleCancel} style={styles.btnCancel}>
              Cancel
            </button>
          )}
        </div>
      </form>

      {/* LIST */}
      <div style={styles.list}>
        <h3>Items from API ({items.length})</h3>
        <ul style={{ listStyle: 'none', padding: 0 }}>
          {items.map((item) => (
            <li key={item.id} style={styles.listItem}>
              <span>{item.title}</span>
              <div>
                <button
                  onClick={() => handleEdit(item)}
                  style={styles.btnEdit}
                  disabled={loading}
                >
                  Edit
                </button>
                <button
                  onClick={() => handleDelete(item.id)}
                  style={styles.btnDelete}
                  disabled={loading}
                >
                  Delete
                </button>
              </div>
            </li>
          ))}
        </ul>
      </div>
    </div>
  );
}

const styles = {
  container: { maxWidth: '500px', margin: '20px auto', fontFamily: 'sans-serif' },
  form: { padding: '15px', border: '1px solid #ccc', borderRadius: '8px', marginBottom: '20px' },
  inputGroup: { display: 'flex', gap: '8px', marginTop: '10px' },
  input: { flex: 1, padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  btnSubmit: { padding: '8px 16px', background: '#007bff', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' },
  btnCancel: { padding: '8px 12px', background: '#6c757d', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' },
  list: { padding: '15px', border: '1px solid #ccc', borderRadius: '8px' },
  listItem: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '10px 0', borderBottom: '1px solid #eee' },
  btnEdit: { marginRight: '6px', background: '#ffc107', border: 'none', padding: '4px 8px', borderRadius: '4px', cursor: 'pointer' },
  btnDelete: { background: '#dc3545', color: '#fff', border: 'none', padding: '4px 8px', borderRadius: '4px', cursor: 'pointer' },
  error: { padding: '10px', background: '#f8d7da', color: '#721c24', borderRadius: '4px', marginBottom: '15px' },
  loading: { color: '#007bff', fontWeight: 'bold' }
};

```

---

### Core Axios vs. Fetch Differences

| Feature                   | `Axios`                                       | Native `fetch()`                                                   |
| ------------------------- | --------------------------------------------- | ------------------------------------------------------------------ |
| **JSON Data Parsing**     | Transforms automatically (`response.data`)    | Requires manual `await res.json()` call                            |
| **HTTP Errors (4xx/5xx)** | Automatically throws error into `catch` block | Does **not** reject error status; requires checking `if (!res.ok)` |
| **Headers Serialization** | Automatically serializes objects to JSON      | Requires explicit `'Content-Type': 'application/json'` header      |
| **Installation**          | NPM dependency (`npm i axios`)                | Built into browsers out-of-the-box                                 |

How do I add search filtering and pagination to this React API integration?

Adding search filtering and pagination involves passing query parameters (such as `q`, `_page`, and `_limit`) to the API endpoint and syncing them with React state.

Here is an upgraded, ready-to-use version of the REST API CRUD component featuring **debounced search filtering**, **page controls**, and **dynamic item limit configuration**.

---

### Complete Component with Search & Pagination

```jsx
import React, { useState, useEffect, useCallback } from 'react';
import axios from 'axios';

const API_URL = 'https://jsonplaceholder.typicode.com/todos';

export default function SearchPaginatedCrudApp() {
  const [items, setItems] = useState([]);
  
  // Search & Pagination States
  const [searchQuery, setSearchQuery] = useState('');
  const [debouncedQuery, setDebouncedQuery] = useState('');
  const [currentPage, setCurrentPage] = useState(1);
  const [limit, setLimit] = useState(5);
  const [totalCount, setTotalCount] = useState(0);

  // Form & UI States
  const [formData, setFormData] = useState({ title: '' });
  const [editingId, setEditingId] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  // 1. Debounce Search Input (delays API request by 400ms while typing)
  useEffect(() => {
    const timer = setTimeout(() => {
      setDebouncedQuery(searchQuery);
      setCurrentPage(1); // Reset to page 1 whenever search query changes
    }, 400);

    return () => clearTimeout(timer);
  }, [searchQuery]);

  // 2. Fetch Items from API whenever Page, Limit, or Search query changes
  const fetchItems = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      // JSONPlaceholder query params:
      // q: search query string
      // _page: current page number
      // _limit: items per page
      const response = await axios.get(API_URL, {
        params: {
          q: debouncedQuery || undefined,
          _page: currentPage,
          _limit: limit,
        },
      });

      setItems(response.data);

      // JSONPlaceholder sends total matching records in the 'x-total-count' header
      const totalHeader = response.headers['x-total-count'];
      if (totalHeader) {
        setTotalCount(parseInt(totalHeader, 10));
      } else {
        // Fallback estimate if API doesn't provide header
        setTotalCount(200); 
      }
    } catch (err) {
      setError(err.message || 'Error loading records');
    } finally {
      setLoading(false);
    }
  }, [debouncedQuery, currentPage, limit]);

  useEffect(() => {
    fetchItems();
  }, [fetchItems]);

  // CRUD Actions
  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!formData.title.trim()) return;

    setLoading(true);
    try {
      if (editingId) {
        const res = await axios.put(`${API_URL}/${editingId}`, { title: formData.title, completed: false });
        setItems(items.map((i) => (i.id === editingId ? res.data : i)));
        setEditingId(null);
      } else {
        const res = await axios.post(API_URL, { title: formData.title, completed: false });
        setItems([res.data, ...items.slice(0, limit - 1)]);
      }
      setFormData({ title: '' });
    } catch (err) {
      setError('Operation failed');
    } finally {
      setLoading(false);
    }
  };

  const handleDelete = async (id) => {
    setLoading(true);
    try {
      await axios.delete(`${API_URL}/${id}`);
      setItems(items.filter((item) => item.id !== id));
    } catch (err) {
      setError('Failed to delete item');
    } finally {
      setLoading(false);
    }
  };

  // Calculated Pagination Info
  const totalPages = Math.ceil(totalCount / limit) || 1;

  return (
    <div style={styles.container}>
      <h2>API CRUD with Search & Pagination</h2>

      {/* --- SEARCH & FILTER CONTROLS --- */}
      <div style={styles.filterBar}>
        <input
          type="text"
          placeholder="Search items..."
          value={searchQuery}
          onChange={(e) => setSearchQuery(e.target.value)}
          style={styles.searchInput}
        />
        <select
          value={limit}
          onChange={(e) => {
            setLimit(Number(e.target.value));
            setCurrentPage(1);
          }}
          style={styles.select}
        >
          <option value={5}>5 per page</option>
          <option value={10}>10 per page</option>
          <option value={20}>20 per page</option>
        </select>
      </div>

      {/* --- CREATE / EDIT FORM --- */}
      <form onSubmit={handleSubmit} style={styles.form}>
        <div style={styles.inputGroup}>
          <input
            type="text"
            placeholder="Title..."
            value={formData.title}
            onChange={(e) => setFormData({ title: e.target.value })}
            style={styles.input}
            required
          />
          <button type="submit" style={styles.btnSubmit} disabled={loading}>
            {editingId ? 'Update' : 'Add'}
          </button>
          {editingId && (
            <button
              type="button"
              onClick={() => {
                setEditingId(null);
                setFormData({ title: '' });
              }}
              style={styles.btnCancel}
            >
              Cancel
            </button>
          )}
        </div>
      </form>

      {error && <p style={styles.error}>{error}</p>}

      {/* --- LIST ITEM DISPLAY --- */}
      <div style={styles.list}>
        {loading ? (
          <p style={styles.loading}>Loading page {currentPage}...</p>
        ) : items.length === 0 ? (
          <p>No results found for "{debouncedQuery}"</p>
        ) : (
          <ul style={{ listStyle: 'none', padding: 0 }}>
            {items.map((item) => (
              <li key={item.id} style={styles.listItem}>
                <span>{item.title}</span>
                <div>
                  <button
                    onClick={() => {
                      setEditingId(item.id);
                      setFormData({ title: item.title });
                    }}
                    style={styles.btnEdit}
                  >
                    Edit
                  </button>
                  <button onClick={() => handleDelete(item.id)} style={styles.btnDelete}>
                    Delete
                  </button>
                </div>
              </li>
            ))}
          </ul>
        )}
      </div>

      {/* --- PAGINATION FOOTER --- */}
      <div style={styles.pagination}>
        <button
          onClick={() => setCurrentPage((prev) => Math.max(prev - 1, 1))}
          disabled={currentPage === 1 || loading}
          style={styles.pageBtn}
        >
          &laquo; Prev
        </button>

        <span>
          Page <strong>{currentPage}</strong> of <strong>{totalPages}</strong>
        </span>

        <button
          onClick={() => setCurrentPage((prev) => Math.min(prev + 1, totalPages))}
          disabled={currentPage === totalPages || loading}
          style={styles.pageBtn}
        >
          Next &raquo;
        </button>
      </div>
    </div>
  );
}

const styles = {
  container: { maxWidth: '550px', margin: '20px auto', fontFamily: 'sans-serif' },
  filterBar: { display: 'flex', gap: '10px', marginBottom: '15px' },
  searchInput: { flex: 1, padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  select: { padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  form: { padding: '15px', border: '1px solid #ccc', borderRadius: '8px', marginBottom: '15px' },
  inputGroup: { display: 'flex', gap: '8px' },
  input: { flex: 1, padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  btnSubmit: { padding: '8px 16px', background: '#007bff', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' },
  btnCancel: { padding: '8px 12px', background: '#6c757d', color: '#fff', border: 'none', borderRadius: '4px', cursor: 'pointer' },
  list: { padding: '15px', border: '1px solid #ccc', borderRadius: '8px', minHeight: '200px' },
  listItem: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '10px 0', borderBottom: '1px solid #eee' },
  btnEdit: { marginRight: '6px', background: '#ffc107', border: 'none', padding: '4px 8px', borderRadius: '4px', cursor: 'pointer' },
  btnDelete: { background: '#dc3545', color: '#fff', border: 'none', padding: '4px 8px', borderRadius: '4px', cursor: 'pointer' },
  pagination: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginTop: '15px' },
  pageBtn: { padding: '6px 12px', borderRadius: '4px', border: '1px solid #ccc', cursor: 'pointer' },
  loading: { color: '#007bff', textAlign: 'center' },
  error: { color: '#dc3545', margin: '10px 0' },
};

```

---

### Key Architectural Concepts

1. **Debouncing Input (`setTimeout`)**: Prevents making an HTTP API request on every keystroke. Instead, it waits until the user pauses typing for `400ms`.
2. **Synchronized State Effects**: `useEffect` runs `fetchItems()` automatically whenever `currentPage`, `limit`, or `debouncedQuery` changes.
3. **Total Record Count**: REST APIs typically supply total record counts in response headers (e.g., `x-total-count` or `Content-Range`), enabling dynamic calculation of total pages (`Math.ceil(total / limit)`).

Syncing search and pagination state with URL search parameters (e.g., `?q=react&page=2&limit=5`) keeps your app's UI shareable, bookmarkable, and responsive to the browser's back/forward navigation.

In **React Router v6 / v7**, you manage this using the `useSearchParams` hook.

---

### Key React Router Setup (`useSearchParams`)

Instead of creating separate `useState` hooks for `searchQuery` and `currentPage`, the URL itself becomes your primary source of truth.

```jsx
import React, { useEffect, useState, useCallback } from 'react';
import { useSearchParams } from 'react-router-dom';
import axios from 'axios';

const API_URL = 'https://jsonplaceholder.typicode.com/todos';

export default function UrlSyncedCrudApp() {
  // 1. React Router Hook for URL query string management
  const [searchParams, setSearchParams] = useSearchParams();

  // 2. Read values directly from URL (with fallbacks)
  const queryParam = searchParams.get('q') || '';
  const pageParam = parseInt(searchParams.get('page') || '1', 10);
  const limitParam = parseInt(searchParams.get('limit') || '5', 10);

  // Local state for the text input (to support smooth typing & debouncing)
  const [searchInput, setSearchInput] = useState(queryParam);
  
  // Data & API state
  const [items, setItems] = useState([]);
  const [totalCount, setTotalCount] = useState(0);
  const [loading, setLoading] = useState(false);

  // Sync local input state if user navigates Back/Forward in the browser
  useEffect(() => {
    setSearchInput(queryParam);
  }, [queryParam]);

  // ---------------------------------------------------------
  // Debounce input changes -> Update URL Search Params
  // ---------------------------------------------------------
  useEffect(() => {
    const timer = setTimeout(() => {
      // Only update URL if the search text actually changed
      if (searchInput !== queryParam) {
        setSearchParams((prevParams) => {
          const newParams = new URLSearchParams(prevParams);
          if (searchInput.trim()) {
            newParams.set('q', searchInput.trim());
          } else {
            newParams.delete('q'); // Clean URL when search is empty
          }
          newParams.set('page', '1'); // Reset to page 1 on new search
          return newParams;
        });
      }
    }, 400);

    return () => clearTimeout(timer);
  }, [searchInput, queryParam, setSearchParams]);

  // ---------------------------------------------------------
  // Fetch API data driven strictly by URL query parameters
  // ---------------------------------------------------------
  const fetchItems = useCallback(async () => {
    setLoading(true);
    try {
      const response = await axios.get(API_URL, {
        params: {
          q: queryParam || undefined,
          _page: pageParam,
          _limit: limitParam,
        },
      });

      setItems(response.data);

      const totalHeader = response.headers['x-total-count'];
      setTotalCount(totalHeader ? parseInt(totalHeader, 10) : 200);
    } catch (err) {
      console.error('API Error:', err);
    } finally {
      setLoading(false);
    }
  }, [queryParam, pageParam, limitParam]);

  useEffect(() => {
    fetchItems();
  }, [fetchItems]);

  // ---------------------------------------------------------
  // URL Update Helpers for Pagination & Page Limit
  // ---------------------------------------------------------
  const handlePageChange = (newPage) => {
    setSearchParams((prevParams) => {
      const newParams = new URLSearchParams(prevParams);
      newParams.set('page', newPage.toString());
      return newParams;
    });
  };

  const handleLimitChange = (newLimit) => {
    setSearchParams((prevParams) => {
      const newParams = new URLSearchParams(prevParams);
      newParams.set('limit', newLimit.toString());
      newParams.set('page', '1'); // Reset to page 1 on limit change
      return newParams;
    });
  };

  const totalPages = Math.ceil(totalCount / limitParam) || 1;

  return (
    <div style={styles.container}>
      <h2>URL Synced CRUD App</h2>
      <p style={styles.subtext}>Try refreshing or sharing the URL after searching or changing pages!</p>

      {/* SEARCH & LIMIT CONTROLS */}
      <div style={styles.filterBar}>
        <input
          type="text"
          placeholder="Search items..."
          value={searchInput}
          onChange={(e) => setSearchInput(e.target.value)}
          style={styles.searchInput}
        />
        <select
          value={limitParam}
          onChange={(e) => handleLimitChange(e.target.value)}
          style={styles.select}
        >
          <option value={5}>5 per page</option>
          <option value={10}>10 per page</option>
          <option value={20}>20 per page</option>
        </select>
      </div>

      {/* DATA DISPLAY */}
      <div style={styles.list}>
        {loading ? (
          <p>Loading data for page {pageParam}...</p>
        ) : items.length === 0 ? (
          <p>No results found for "{queryParam}"</p>
        ) : (
          <ul style={{ listStyle: 'none', padding: 0 }}>
            {items.map((item) => (
              <li key={item.id} style={styles.listItem}>
                <span>{item.title}</span>
              </li>
            ))}
          </ul>
        )}
      </div>

      {/* PAGINATION FOOTER */}
      <div style={styles.pagination}>
        <button
          onClick={() => handlePageChange(pageParam - 1)}
          disabled={pageParam <= 1 || loading}
          style={styles.pageBtn}
        >
          &laquo; Prev
        </button>

        <span>
          Page <strong>{pageParam}</strong> of <strong>{totalPages}</strong>
        </span>

        <button
          onClick={() => handlePageChange(pageParam + 1)}
          disabled={pageParam >= totalPages || loading}
          style={styles.pageBtn}
        >
          Next &raquo;
        </button>
      </div>
    </div>
  );
}

const styles = {
  container: { maxWidth: '550px', margin: '20px auto', fontFamily: 'sans-serif' },
  subtext: { color: '#666', fontSize: '0.9em', marginTop: '-10px', marginBottom: '15px' },
  filterBar: { display: 'flex', gap: '10px', marginBottom: '15px' },
  searchInput: { flex: 1, padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  select: { padding: '8px', borderRadius: '4px', border: '1px solid #ccc' },
  list: { padding: '15px', border: '1px solid #ccc', borderRadius: '8px', minHeight: '180px' },
  listItem: { padding: '8px 0', borderBottom: '1px solid #eee' },
  pagination: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginTop: '15px' },
  pageBtn: { padding: '6px 12px', borderRadius: '4px', border: '1px solid #ccc', cursor: 'pointer' },
};

```

---

### How This Works Behind the Scenes

1. **`useSearchParams()` replacing `useState**`: Reads parameters directly from the browser window URL (`window.location.search`).
2. **State-to-URL Updates**: Modifying query params is done via `setSearchParams(newParams)`, which pushes/replaces history entries without re-triggering full page reloads.
3. **Clean URL Handling**: Using `newParams.delete('q')` removes key-value pairs when the search query is empty so the URL stays clean (i.e., `?page=1` instead of `?q=&page=1`).
4. **Browser History Native Support**: Because parameters drive `useEffect`, using the browser **Back** and **Forward** buttons triggers automated re-fetching with previous parameters out-of-the-box.
