*** copy README.md ***

# HTML Interview Mastery

Same deep structure as the companion repos: every topic is a folder of folders (`theory/`, `snippets/`,
`output-based/`, `scenarios/`, `interview-qa/`, `problems/`, `assets/`) instead of flat files.

## Topics

| # | Topic | Folder |
|---|---|---|
| 01 | Semantic HTML & Document Structure | [`01-semantic-html-document-structure`](./01-semantic-html-document-structure) |
| 02 | Forms & Validation | [`02-forms-validation`](./02-forms-validation) |
| 03 | Accessibility & ARIA | [`03-accessibility-aria`](./03-accessibility-aria) |
| 04 | Media & Embeds | [`04-media-embeds`](./04-media-embeds) |
| 05 | HTML5 APIs & Storage | [`05-html5-apis-storage`](./05-html5-apis-storage) |
| 06 | Performance & Loading Attributes | [`06-performance-loading-attributes`](./06-performance-loading-attributes) |
| 07 | Metadata, `<head>` & SEO | [`07-metadata-seo-head`](./07-metadata-seo-head) |

See [`STUDY-PLAN.md`](./STUDY-PLAN.md) for a suggested order and [`SOURCE-MAP.md`](./SOURCE-MAP.md) for where
this maps to your existing `js_polyfills/HTML` notes — including a full `accesibility/` subfolder you already
had, and DOM notes that are intentionally not duplicated here since `js-interview-mastery/17-dom-events-browser-apis`
already covers DOM APIs from the JS side.

## How to use this repo

1. **Learning a topic for the first time** — read `theory/`, then run the code in `snippets/` yourself.
2. **Weekly review** — skim `interview-qa/` across all topics.
3. **The night before an interview** — do `output-based/` and `scenarios/` only.
4. **Adding new material** — drop a new numbered file in the matching subfolder.
