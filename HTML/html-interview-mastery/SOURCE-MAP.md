***  SOURCE-MAP.md ***

# Source Map — js_polyfills/HTML → this repo

Note: device staging is still blocked this pass (`untrusted_device` — sign in again in the Claude desktop app
to fix). Nothing was physically copied into `from-your-notes/`/`assets/` this time; the mapping below is
accurate and ready to execute once you're signed back in.

## Mapped — worth pulling into `from-your-notes/`

| New topic | Source file(s) in `js_polyfills/HTML/` |
|---|---|
| `01-semantic-html-document-structure` | `What are Semantic Tags in HTML.md`, `what is Difference Between an HTML Element and an HTML Tag.md`, `What Are Attributes in HTML.md`, `what is Data Attributes.md`, `Why DocType is important.md`, `id vs class.md`, `what is difference between Inline and block level Elements.md`, `what is Difference Between div and span in HTML.md` |
| `02-forms-validation` | `accesibility/best practices for handling form errors.md`, `accesibility/AutoComplete.md`, `accesibility/tabindex and focus order in forms.md` |
| `03-accessibility-aria` | The entire `accesibility/` subfolder — `What are ARIA Attributes.md` (root), `Accessible Navigation Menu.md`, `accesskey.md`, `aria-hidden in Dynamic Content.md`, `concept.md`, `dropdown.md`, `focus Managment.md`, `role button and Button.md`, `Table Accessible.md` (⚠ 3MB — likely has large embedded images, open directly rather than auto-copy) |
| `04-media-embeds` | `difference between the picture and img.md`, `HTML Character Entities.md`, `htmlEmoji.md` |
| `05-html5-apis-storage` | `window/what is window object.md`, `window/difference/window vs document.md`, `browser object Model/BOM.md` |
| `06-performance-loading-attributes` | `what is differenc between sync and asyn script in js.md` |
| `07-metadata-seo-head` | `what use of head tag.md`, `why we use meta tag.md`, `viewport.md` |
| *(general reference)* | `htmlInterview.md`, `What ahppen when ypo type a url in browser.md`, `remove-attributes.md`, `prettyHTML.md`, `generateCSSSelector.md`, `generateTableOfContents.md`, `highlightText.md`, `elementIsVisibleInViewport.md`, `infinite_scroll_feed.md`, `digitalClock.md`, `trafic_light.md` — build-it-yourself DOM exercises, worth a skim |

## Already used elsewhere — not duplicated here

The `DOM/` and `document/` subfolders (DOM traversal, `querySelectorAll`, DOM manipulation exercises,
`interview quiz.md`) overlap heavily with `js-interview-mastery/17-dom-events-browser-apis`, which already
covers DOM APIs from the JS side. Not re-copied here to avoid duplication — cross-reference that repo instead.
Two files there (`DOM/dom.md` at 249KB and `DOM/What is the DOM.md` at 2.4MB, `document/domTravesal.md` at
2.9MB) are also too large for automatic processing and are better read directly.

## What wasn't touched

`1785046849127.jpeg` (unclassified image, root of `HTML/`) and `DOM/image.png` — worth a manual look to see
which topic they belong to.
