```
1. Arrays are the collection of values in javascript. 
2. Array is a special type of object in JavaScript
3. Arrays values are indexed from 0 and have special property length
which stores the count of elements present in array
```