The **Circuit Breaker** design pattern is an important concept for building resilient systems. It helps prevent a system from repeatedly attempting operations that are likely to fail, thus avoiding resource wastage and reducing the risk of cascading failures. A **circuit breaker** monitors calls to a service or a function, and if it fails repeatedly within a certain threshold, it "opens" the circuit and stops further calls for a defined time period. Once the time period expires, the circuit is "closed" again, and normal operations are resumed.

### Breakdown of the Circuit Breaker Pattern:
1. **Failure Count**: If the function fails a certain number of times (e.g., `Y` failures), the breaker will be triggered.
2. **Time Threshold**: If the failure count reaches the threshold, the breaker will stop subsequent calls for a certain duration (`X` time threshold).
3. **State of the Circuit**:
   - **Closed**: The function operates normally, and all requests go through.
   - **Open**: The circuit is "open," and requests are blocked to prevent further failures.
   - **Half-Open**: After the timeout period expires, the circuit enters a half-open state, where it attempts to allow a single call to check if the service has recovered.

### Circuit Breaker Implementation in JavaScript:

You can implement this pattern in JavaScript using closures and state management. Below is an example implementation of the circuit breaker with a failure count threshold and a time threshold:

```javascript
const circuitBreaker = (fn, failureCount, timeThreshold) => {
  let failures = 0; // Tracks the number of failures
  let timeSinceLastFailure = 0; // Time since the last failure
  let isClosed = true; // Circuit is initially closed (function is available)

  return function (...args) {
    // If the circuit is open (service is unavailable)
    if (!isClosed) {
      const diff = Date.now() - timeSinceLastFailure;
      
      // If the time since the last failure exceeds the threshold, close the circuit
      if (diff > timeThreshold) {
        isClosed = true; // Opened circuit should now be reset and closed
      } else {
        console.error("Service unavailable. Please try again later.");
        return; // Don't proceed with the function call
      }
    }

    // Attempt to run the function if the circuit is closed
    try {
      const result = fn(...args);
      failures = 0; // Reset failure count after successful execution
      return result;
    } catch (error) {
      // If the function fails, increment failure count and record time of failure
      failures++;
      timeSinceLastFailure = Date.now();

      if (failures >= failureCount) {
        isClosed = false; // Open the circuit if failure count threshold is reached
        console.error("Circuit breaker opened due to repeated failures.");
      }

      console.error("Error occurred. Attempt failed.");
    }
  };
};

// Test function: This function fails for the first 3 times and succeeds afterward.
const testFunction = () => {
  let count = 0;

  return function () {
    count++;
    if (count < 4) {
      throw "failed"; // Simulate failure
    } else {
      return "hello"; // Simulate success
    }
  };
};

let t = testFunction();
let c = circuitBreaker(t, 3, 200);

// Simulate service calls
c(); // "Error occurred. Attempt failed."
c(); // "Error occurred. Attempt failed."
c(); // "Error occurred. Attempt failed."

// Service is now "open" and unavailable for 200ms
c(); // "Service unavailable. Please try again later."
c(); // "Service unavailable. Please try again later."
c(); // "Service unavailable. Please try again later."

// After the time threshold (200ms), the circuit is closed, and service should be available
setTimeout(() => {
  console.log(c()); // "hello" (After 200ms the service becomes available again)
}, 300);
```

### Explanation:

1. **State Management**:
   - The variable `isClosed` keeps track of whether the circuit is open or closed.
   - The `failures` variable counts how many times the function has failed.
   - `timeSinceLastFailure` records the timestamp of the last failure.

2. **Circuit Logic**:
   - If the circuit is **closed**, the function is executed normally.
   - If the function fails, `failures` are incremented, and if it reaches the specified threshold (`failureCount`), the circuit is **opened**.
   - Once the circuit is open, all subsequent calls will be blocked until the time since the last failure exceeds `timeThreshold`. If the threshold is exceeded, the circuit becomes **closed** again, and normal operation resumes.
   
3. **Test Function**:
   - The `testFunction` simulates a service that fails for the first 3 calls and succeeds afterward. This function helps test the circuit breaker logic.

### Key Concepts:

1. **Failure Count**: Tracks how many times the function fails before the circuit opens.
2. **Time Threshold**: After a failure, the circuit remains open for a certain duration before it is allowed to be reattempted.
3. **State Management**: The circuit can be in one of three states: **Closed**, **Open**, or **Half-Open**.

### Real-World Use Cases:
- **API Rate Limiting**: Prevents hitting a failing API endpoint by stopping calls for a certain amount of time.
- **Microservices Architecture**: Prevents cascading failures in microservices by stopping calls to a service that is likely to fail.
- **Frontend Resilience**: Helps front-end applications handle scenarios where backend services might intermittently fail (e.g., server overloads, network issues).

### Conclusion:
The **Circuit Breaker** design pattern is a powerful tool for building resilient systems. It helps prevent repeated attempts to access a failing service, minimizing the impact of service outages. The above implementation demonstrates how to apply this pattern in JavaScript with proper state management and time-based thresholds.

# Circuit Breaker Design Pattern (JavaScript & React)

The **Circuit Breaker Pattern** is a resilience pattern used to prevent an application from repeatedly calling a failing service.

Think of it like an electrical circuit breaker:

```text
Service Working
       ↓
Too Many Failures
       ↓
Circuit Opens
       ↓
Stop Requests
       ↓
Wait Recovery Period
       ↓
Try Again
```

***

# Why Do We Need It?

Imagine your React application calls:

```text
User Service
Payment Service
Notification Service
Product Service
```

If one service is down:

```javascript
fetch("/api/users")
```

fails every time.

Without a Circuit Breaker:

```text
User clicks refresh
↓
API fails

User retries
↓
API fails

1000 users retry
↓
API overwhelmed
↓
Complete system failure
```

***

# Circuit Breaker States

```text
Closed
 ↓
(Open after failures)
 ↓
Open
 ↓
(Recovery timeout)
 ↓
Half Open
 ↓
Success → Closed

Failure → Open
```

***

# State 1: Closed

Normal operation.

```text
Requests allowed
```

```javascript
API Call ✅
API Call ✅
API Call ✅
```

***

# State 2: Open

Too many failures.

```text
Requests blocked
```

```javascript
Error:
Service unavailable
```

No network request is sent.

***

# State 3: Half Open

After a timeout:

```text
Allow 1 request
```

If successful:

```text
Circuit closes
```

If failure:

```text
Circuit opens again
```

***

# Simple JavaScript Implementation

```javascript
class CircuitBreaker {
  constructor(
    fn,
    failureThreshold = 3,
    timeout = 5000
  ) {
    this.fn = fn;
    this.failureThreshold =
      failureThreshold;

    this.timeout = timeout;

    this.failures = 0;

    this.state = "CLOSED";

    this.nextAttempt = 0;
  }

  async call(...args) {

    if (
      this.state === "OPEN"
    ) {

      if (
        Date.now() >
        this.nextAttempt
      ) {
        this.state =
          "HALF_OPEN";
      } else {
        throw new Error(
          "Circuit Open"
        );
      }
    }

    try {

      const result =
        await this.fn(...args);

      this.failures = 0;

      this.state =
        "CLOSED";

      return result;

    } catch (error) {

      this.failures++;

      if (
        this.failures >=
        this.failureThreshold
      ) {

        this.state = "OPEN";

        this.nextAttempt =
          Date.now() +
          this.timeout;
      }

      throw error;
    }
  }
}
```

***

# Usage

```javascript
async function fetchUsers() {
  const response =
    await fetch("/api/users");

  if (!response.ok) {
    throw new Error(
      "API Failed"
    );
  }

  return response.json();
}

const breaker =
  new CircuitBreaker(
    fetchUsers
  );

breaker.call();
```

***

# React Example

## API Hook with Circuit Breaker

```jsx
import {
  useState
} from "react";

class CircuitBreaker {
  constructor(fn) {
    this.fn = fn;
    this.failureCount = 0;
    this.state = "CLOSED";
  }

  async execute() {

    if (
      this.state === "OPEN"
    ) {
      throw new Error(
        "Service temporarily unavailable"
      );
    }

    try {
      const result =
        await this.fn();

      this.failureCount = 0;

      return result;
    } catch (error) {

      this.failureCount++;

      if (
        this.failureCount >= 3
      ) {
        this.state =
          "OPEN";
      }

      throw error;
    }
  }
}
```

***

## Custom Hook

```jsx
function useUsers() {

  const [users, setUsers] =
    useState([]);

  const [error, setError] =
    useState("");

  const api =
    new CircuitBreaker(
      async () => {

        const response =
          await fetch(
            "/api/users"
          );

        if (
          !response.ok
        ) {
          throw new Error(
            "API Error"
          );
        }

        return response.json();
      }
    );

  const loadUsers =
    async () => {

      try {

        const data =
          await api.execute();

        setUsers(data);

      } catch (error) {

        setError(
          error.message
        );
      }
    };

  return {
    users,
    error,
    loadUsers
  };
}
```

***

# Enterprise Example

Suppose your React dashboard loads:

```text
Employees
Projects
Timesheets
Reports
```

If:

```text
Reports Service
```

fails repeatedly:

```text
Circuit Opens
```

Dashboard can still load:

```text
Employees ✅
Projects ✅
Timesheets ✅
Reports ❌
```

instead of hanging completely.

***

# Circuit Breaker + Retry Pattern

Very common system design interview question.

```javascript
try {
  return await breaker.call();
}
catch(error) {
  retry();
}
```

Flow:

```text
Retry 3 times
↓
Still failing
↓
Open Circuit
```

***

# Benefits

### Prevent Cascading Failures

```text
Service A
 ↓
Service B down
 ↓
Service A protected
```

***

### Faster Response

Instead of:

```text
Wait 30 sec timeout
```

Immediately:

```text
Service unavailable
```

***

### Better User Experience

```jsx
<Card>
  Reports Currently
  Unavailable
</Card>
```

instead of:

```text
Page Frozen
```

***

### Reduces Server Load

When service is already failing:

```text
No additional requests sent
```

***

# Interview Questions

## Q1: What problem does Circuit Breaker solve?

**Answer:**

Prevents repeated requests to a failing service and avoids cascading system failures.

***

## Q2: Circuit Breaker States?

```text
Closed
Open
Half Open
```

***

## Q3: Difference Between Retry and Circuit Breaker?

### Retry

```text
Keep trying
```

### Circuit Breaker

```text
Stop trying after failures
```

***

## Q4: Where is it used?

```text
Microservices
REST APIs
GraphQL APIs
Payment Systems
Notification Services
React Dashboards
```

***

# Senior React / System Design Interview Answer

> The Circuit Breaker Pattern is a fault-tolerance mechanism that protects applications from repeatedly calling failing services. It operates through three states: Closed (normal operation), Open (requests blocked after repeated failures), and Half-Open (testing recovery with limited requests). In React applications, Circuit Breakers are often implemented around API clients or data-fetching hooks to improve resilience, reduce server load, prevent cascading failures, and provide a better user experience when backend services are unavailable.



# Circuit Breaker Hook in React (Production-Style)

A common React pattern is:

```text
API Call
   ↓
Retry 3 Times
   ↓
Still Failing?
   ↓
Open Circuit
   ↓
Block Requests
   ↓
Recovery Timeout
   ↓
Half Open
```

***

# Step 1: Circuit Breaker Class

```javascript
class CircuitBreaker {
  constructor(
    apiCall,
    failureThreshold = 3,
    resetTimeout = 5000
  ) {
    this.apiCall = apiCall;
    this.failureThreshold =
      failureThreshold;

    this.resetTimeout =
      resetTimeout;

    this.failures = 0;
    this.state = "CLOSED";
    this.nextTry = 0;
  }

  async execute(...args) {

    if (this.state === "OPEN") {

      if (Date.now() < this.nextTry) {
        throw new Error(
          "Circuit is OPEN"
        );
      }

      this.state = "HALF_OPEN";
    }

    try {

      const result =
        await this.apiCall(...args);

      this.failures = 0;
      this.state = "CLOSED";

      return result;

    } catch (error) {

      this.failures++;

      if (
        this.failures >=
        this.failureThreshold
      ) {

        this.state = "OPEN";

        this.nextTry =
          Date.now() +
          this.resetTimeout;
      }

      throw error;
    }
  }
}
```

***

# Step 2: Retry Function

Very common interview pattern.

```javascript
async function retry(
  fn,
  retries = 3
) {
  let lastError;

  for (
    let i = 0;
    i < retries;
    i++
  ) {
    try {
      return await fn();
    } catch (error) {
      lastError = error;
    }
  }

  throw lastError;
}
```

***

# Step 3: React Hook

```jsx
import {
  useState,
  useMemo
} from "react";

function useUsers() {

  const [users, setUsers] =
    useState([]);

  const [error, setError] =
    useState("");

  const breaker = useMemo(
    () =>
      new CircuitBreaker(
        async () => {

          const response =
            await fetch(
              "/api/users"
            );

          if (
            !response.ok
          ) {
            throw new Error(
              "API Failed"
            );
          }

          return response.json();
        }
      ),
    []
  );

  const loadUsers =
    async () => {

      try {

        const data =
          await retry(
            () =>
              breaker.execute(),
            3
          );

        setUsers(data);

      } catch (error) {

        setError(
          error.message
        );
      }
    };

  return {
    users,
    error,
    loadUsers
  };
}
```

***

# Step 4: Component Usage

```jsx
export default function UsersPage() {

  const {
    users,
    error,
    loadUsers
  } = useUsers();

  return (

    <div>

      <button
        onClick={loadUsers}
      >
        Load Users
      </button>

      {error && (
        <p>
          Error:
          {error}
        </p>
      )}

      {users.map(user => (
        <div
          key={user.id}
        >
          {user.name}
        </div>
      ))}

    </div>
  );
}
```

***

# Retry + Circuit Breaker Flow

```text
Request
 ↓
Fail
 ↓
Retry 1
 ↓
Retry 2
 ↓
Retry 3
 ↓
Still Fails
 ↓
Failure Count++
 ↓
Threshold Reached?
 ↓
YES
 ↓
Circuit OPEN
 ↓
Reject Future Requests
 ↓
Wait 5 Seconds
 ↓
HALF OPEN
 ↓
Try One Request
 ↓
Success?
 ↓
YES → CLOSE
NO  → OPEN
```

***

# Enterprise Example

Imagine a dashboard:

```text
Employee Service
Projects Service
Timesheets Service
Reports Service
```

Reports API fails:

```text
Retry 3 Times
↓
Fail
↓
Circuit Opens
↓
Dashboard Continues Working
```

Users still see:

```text
Employees ✅
Projects ✅
Timesheets ✅
Reports ❌
```

instead of a completely broken page.

***

# Common Interview Questions

## Q1: What Problem Does Circuit Breaker Solve?

**Answer:**

Prevents repeated calls to failing services and avoids cascading failures.

***

## Q2: Circuit Breaker States?

```text
1. CLOSED
2. OPEN
3. HALF_OPEN
```

***

## Q3: Difference Between Retry and Circuit Breaker?

### Retry

```text
Keeps trying
```

Example:

```javascript
retry(apiCall, 3);
```

### Circuit Breaker

```text
Stops trying after threshold reached
```

Example:

```javascript
breaker.execute();
```

***

## Q4: Why Use Both Together?

Retry handles:

```text
Temporary network glitches
```

Circuit Breaker handles:

```text
Service outage
Database outage
Downstream API failure
```

Most enterprise systems use both.

***

## Q5: Real React Use Cases?

* API hooks
* Payment requests
* Notification services
* Dashboard widgets
* Feature flag services
* Microservice communication

***

## Q6: What Happens in HALF\_OPEN State?

```text
Allow limited requests
```

If successful:

```text
HALF_OPEN → CLOSED
```

If failed:

```text
HALF_OPEN → OPEN
```

***

## Q7: Difference Between Circuit Breaker and Rate Limiter?

### Circuit Breaker

```text
Protects from failing services
```

### Rate Limiter

```text
Controls number of requests
```

***

## Senior React / System Design Interview Answer

> The Circuit Breaker Pattern is a resilience mechanism used to stop requests to a failing service after a configurable failure threshold. It works with three states: Closed, Open, and Half-Open. In React applications, Circuit Breakers are commonly implemented around data-fetching hooks and API clients. They are often combined with Retry and Exponential Backoff patterns to handle transient errors gracefully while preventing cascading failures and reducing load on unhealthy downstream services.
# Retry with Exponential Backoff

Instead of retrying immediately:

```text
Retry 1 → 1 second
Retry 2 → 2 seconds
Retry 3 → 4 seconds
Retry 4 → 8 seconds
```

This reduces pressure on an overloaded server.

***

## Basic Implementation

```javascript
const sleep = ms =>
  new Promise(resolve =>
    setTimeout(resolve, ms)
  );

async function retryWithBackoff(
  fn,
  retries = 5,
  baseDelay = 1000
) {
  let attempt = 0;

  while (attempt < retries) {
    try {
      return await fn();
    } catch (error) {
      attempt++;

      if (attempt === retries) {
        throw error;
      }

      const delay =
        baseDelay *
        Math.pow(2, attempt - 1);

      console.log(
        `Retry ${attempt} after ${delay}ms`
      );

      await sleep(delay);
    }
  }
}
```

***

## Usage

```javascript
async function fetchUsers() {
  const response =
    await fetch("/api/users");

  if (!response.ok) {
    throw new Error("API Error");
  }

  return response.json();
}

retryWithBackoff(
  fetchUsers,
  4
);
```

***

# Retry + Circuit Breaker

Production flow:

```text
API Call
 ↓
Fail
 ↓
Retry (1s)
 ↓
Retry (2s)
 ↓
Retry (4s)
 ↓
Fail Again
 ↓
Circuit Opens
 ↓
Block Requests
 ↓
Reset Timeout
 ↓
Half Open
```

Example:

```javascript
try {
  const data =
    await retryWithBackoff(
      () => breaker.execute(),
      3
    );
} catch (error) {
  console.error(
    "Service unavailable"
  );
}
```

***

# React Query + Circuit Breaker

React Query already supports:

* Retry
* Retry Delay
* Cache
* Background Refresh

Add Circuit Breaker around the query function.

***

## Circuit Breaker Instance

```javascript
const breaker =
  new CircuitBreaker(
    async () => {
      const response =
        await fetch("/api/users");

      if (!response.ok) {
        throw new Error(
          "API Error"
        );
      }

      return response.json();
    }
  );
```

***

## React Query Integration

```javascript
import { useQuery } from
  "@tanstack/react-query";

function useUsers() {
  return useQuery({
    queryKey: ["users"],

    queryFn: () =>
      breaker.execute(),

    retry: 3,

    retryDelay: attempt =>
      Math.min(
        1000 *
          Math.pow(
            2,
            attempt
          ),
        30000
      )
  });
}
```

***

## Usage

```jsx
function Users() {
  const {
    data,
    isLoading,
    error
  } = useUsers();

  if (isLoading)
    return <p>Loading...</p>;

  if (error)
    return <p>{error.message}</p>;

  return (
    <>
      {data.map(user => (
        <div key={user.id}>
          {user.name}
        </div>
      ))}
    </>
  );
}
```

***

# Common Circuit Breaker Pitfalls

## ❌ Opening Circuit Too Early

Bad:

```javascript
failureThreshold = 1;
```

One temporary network issue:

```text
Circuit OPEN
```

Too aggressive.

### Better

```javascript
failureThreshold = 3
```

or

```javascript
failureThreshold = 5
```

***

## ❌ No Recovery Mechanism

Bad:

```javascript
state = "OPEN";
```

but never:

```javascript
HALF_OPEN
```

Result:

```text
Service recovers
Circuit remains blocked forever
```

***

## ❌ Sharing Failure Count Across APIs

Bad:

```javascript
globalCircuitBreaker
```

for:

```text
Users API
Orders API
Reports API
```

One API failure breaks all services.

### Better

```javascript
UsersBreaker
OrdersBreaker
ReportsBreaker
```

***

## ❌ Retrying Non-Retryable Errors

Never retry:

```text
400 Bad Request
401 Unauthorized
403 Forbidden
404 Not Found
```

Retry only:

```text
429 Too Many Requests
500 Internal Server Error
502 Bad Gateway
503 Service Unavailable
504 Gateway Timeout
Network Timeout
```

***

## ❌ No Exponential Backoff

Bad:

```javascript
Retry
Retry
Retry
Retry
```

immediately.

This can create:

```text
Retry Storm
```

and overload the backend.

***

## ❌ Creating Breaker Inside Component Render

Bad:

```jsx
function Users() {
  const breaker =
    new CircuitBreaker(...);
}
```

New breaker every render.

### Better

```javascript
const breaker =
  useMemo(
    () =>
      new CircuitBreaker(...),
    []
  );
```

***

## ❌ Ignoring Half-Open State

Many implementations use only:

```text
Closed
Open
```

Missing:

```text
Half Open
```

Without Half-Open:

```text
No automatic recovery
```

***

# Circuit Breaker Interview Questions

### Basic

1. What problem does Circuit Breaker solve?
2. What are the three states?
3. Difference between Retry and Circuit Breaker?
4. Why combine Retry + Circuit Breaker?

***

### Intermediate

5. What is Half-Open state?
6. What should trigger circuit opening?
7. Which HTTP status codes should be retried?
8. Why use Exponential Backoff?

***

### Advanced

9. How would you implement Circuit Breaker in React Query?
10. How would you prevent retry storms?
11. Circuit Breaker vs Rate Limiter?
12. Circuit Breaker in Microservices?
13. How would you track breaker metrics?
14. How would you handle multiple downstream APIs?

***

# Senior React/System Design Answer

> In modern React applications, a Circuit Breaker is typically placed around API clients or React Query query functions. It is often combined with Retry and Exponential Backoff. Retries handle transient failures, while the Circuit Breaker prevents repeated requests to unhealthy services. The key states are Closed, Open, and Half-Open. Common implementation mistakes include retrying non-retryable errors, omitting exponential backoff, sharing breakers across unrelated services, and failing to support automatic recovery through the Half-Open state.
# Monitoring Circuit Breaker State in React

In real-world React applications, simply implementing a Circuit Breaker is not enough. You should also monitor:

```text
Current State
Failure Count
Successful Requests
Failed Requests
Last Failure Time
Next Retry Time
```

This helps with:

✅ Debugging  
✅ Dashboard Monitoring  
✅ Observability  
✅ Incident Investigation  
✅ SRE / DevOps Metrics

***

# Enhanced Circuit Breaker

```javascript
class CircuitBreaker {
  constructor(
    apiCall,
    threshold = 3,
    timeout = 10000
  ) {
    this.apiCall = apiCall;

    this.threshold = threshold;
    this.timeout = timeout;

    this.failures = 0;

    this.successCount = 0;
    this.failureCount = 0;

    this.state = "CLOSED";

    this.nextRetry = null;

    this.listeners = [];
  }

  notify() {
    this.listeners.forEach(
      listener =>
        listener(
          this.getStatus()
        )
    );
  }

  subscribe(listener) {
    this.listeners.push(listener);

    return () => {
      this.listeners =
        this.listeners.filter(
          l => l !== listener
        );
    };
  }

  getStatus() {
    return {
      state: this.state,
      failures: this.failures,
      successCount:
        this.successCount,
      failureCount:
        this.failureCount,
      nextRetry:
        this.nextRetry
    };
  }

  async execute() {

    if (
      this.state === "OPEN"
    ) {

      if (
        Date.now() <
        this.nextRetry
      ) {
        throw new Error(
          "Circuit Open"
        );
      }

      this.state =
        "HALF_OPEN";

      this.notify();
    }

    try {

      const result =
        await this.apiCall();

      this.successCount++;

      this.failures = 0;

      this.state =
        "CLOSED";

      this.notify();

      return result;

    } catch (error) {

      this.failures++;

      this.failureCount++;

      if (
        this.failures >=
        this.threshold
      ) {

        this.state =
          "OPEN";

        this.nextRetry =
          Date.now() +
          this.timeout;
      }

      this.notify();

      throw error;
    }
  }
}
```

***

# React Hook for Monitoring

```jsx
import {
  useEffect,
  useState
} from "react";

function useCircuitMonitor(
  breaker
) {
  const [status, setStatus] =
    useState(
      breaker.getStatus()
    );

  useEffect(() => {

    const unsubscribe =
      breaker.subscribe(
        setStatus
      );

    return unsubscribe;

  }, [breaker]);

  return status;
}
```

***

# Monitoring Component

```jsx
function CircuitStatus({
  breaker
}) {

  const status =
    useCircuitMonitor(
      breaker
    );

  return (
    <div>
      <h3>
        Circuit State:
        {" "}
        {status.state}
      </h3>

      <p>
        Success:
        {" "}
        {status.successCount}
      </p>

      <p>
        Failures:
        {" "}
        {status.failureCount}
      </p>

      <p>
        Consecutive Failures:
        {" "}
        {status.failures}
      </p>
    </div>
  );
}
```

***

# Visual State Indicator

A common enterprise dashboard pattern:

```jsx
function StatusBadge({
  state
}) {

  const colors = {
    CLOSED: "green",
    OPEN: "red",
    HALF_OPEN: "orange"
  };

  return (
    <span
      style={{
        background:
          colors[state],
        color: "white",
        padding: "4px 8px"
      }}
    >
      {state}
    </span>
  );
}
```

***

# DevTools Logging

```javascript
breaker.subscribe(
  status => {

    console.log(
      "[Circuit]",
      status.state
    );

    console.log(
      "Failures:",
      status.failures
    );

  }
);
```

Sample output:

```text
[Circuit] CLOSED

[Circuit] OPEN

[Circuit] HALF_OPEN
```

***

# React Query Integration

Monitor breaker state alongside React Query.

```jsx
function useUsers() {

  const status =
    useCircuitMonitor(
      breaker
    );

  const query =
    useQuery({
      queryKey: ["users"],
      queryFn: () =>
        breaker.execute()
    });

  return {
    ...query,
    circuitState:
      status.state
  };
}
```

***

# Display Circuit State in UI

```jsx
function UsersPage() {

  const {
    data,
    circuitState
  } = useUsers();

  return (
    <>
      <StatusBadge
        state={
          circuitState
        }
      />

      {circuitState ===
        "OPEN" && (
        <div>
          Service
          temporarily
          unavailable
        </div>
      )}
    </>
  );
}
```

***

# Production Metrics to Monitor

In enterprise systems, monitor:

```text
Circuit State
Failures/minute
Success Rate
Retry Count
Open Events
Half-Open Events
Recovery Time
Average Response Time
```

Example:

```javascript
{
  state: "OPEN",
  failures: 12,
  retries: 36,
  lastFailure:
    "2026-07-03T12:15:00Z"
}
```

***

# Common Monitoring Pitfalls

### ❌ No Visibility Into State

```javascript
breaker.execute();
```

No way to know if circuit is:

```text
OPEN
HALF_OPEN
CLOSED
```

***

### ❌ No Metrics

Unable to answer:

```text
How many failures today?

How often was circuit open?
```

***

### ❌ Recreating Breaker

```jsx
new CircuitBreaker(...)
```

inside component render.

Metrics reset every render.

Use:

```jsx
useMemo(
  () =>
    new CircuitBreaker(...),
  []
);
```

***

# Senior React Interview Answer

> A Circuit Breaker should expose its internal state and metrics so the UI and monitoring systems can observe its behaviour. In React, a common approach is to extend the breaker with a subscription mechanism and build a custom hook such as `useCircuitMonitor()` that tracks state changes. The UI can then display `CLOSED`, `OPEN`, and `HALF_OPEN` states, while telemetry systems collect metrics such as failure rates, retry counts, open events, and recovery times. This makes the Circuit Breaker observable, debuggable, and production-ready.
