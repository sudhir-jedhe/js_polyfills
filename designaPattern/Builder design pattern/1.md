The **Builder Design Pattern** is a creational design pattern that allows for the step-by-step construction of a complex object. It is especially useful when you want to create an object with many optional parts, and when you want to ensure that these parts are set in a specific order.

In JavaScript, you can implement the Builder pattern by creating a "builder" class (or just an object with methods) that allows you to chain method calls to progressively set different properties of the object. Here's an example of how you can implement this pattern for a `Payment` object, using method chaining to add values and configure the object step-by-step.

### Builder Design Pattern Example in JavaScript:

#### 1. **Using a Class to Implement Builder Pattern:**

```javascript
class Payment {
  constructor(currency = '₹', amount = 0) {
    this.currency = currency;
    this.amount = amount;
  }

  // Method to add amount to the current amount
  addAmount(value) {
    this.amount += value;
    return this;  // Returning the current instance for method chaining
  }

  // Method to change the currency
  addCurrency(currency) {
    this.currency = currency;
    return this;  // Returning the current instance for method chaining
  }

  // Method to make the payment and log the final result
  pay() {
    console.log(`${this.currency} ${this.amount}`);
  }
}

// Example usage:
const p1 = new Payment();
p1.addAmount(100).addAmount(200).addAmount(200).pay();  // "₹ 500"

const p2 = new Payment();
p2.addAmount(200).addCurrency('$').pay();  // "$ 200"
```

#### **Explanation of the Code:**

1. **Constructor (`constructor`)**:
   - The `Payment` class has a constructor that allows setting `currency` (default: `'₹'`) and `amount` (default: `0`).
   
2. **Chaining Methods (`addAmount` and `addCurrency`)**:
   - The `addAmount` method increments the `amount` by the specified `value` and returns the `this` object to allow method chaining.
   - The `addCurrency` method allows changing the `currency` (e.g., changing from '₹' to '$') and also returns `this` for chaining.
   
3. **Finalizing the Payment (`pay`)**:
   - The `pay` method outputs the final amount and currency to the console.

### 2. **Using an Object Literal to Implement Builder Pattern:**

In JavaScript, you can also use an object literal to implement the builder pattern. The pattern works the same way but does not use a class constructor. This is often used when you want to create a more lightweight or flexible object.

```javascript
const payment = {
  currency: '₹',
  amount: 0,

  addAmount: function (value) {
    this.amount += value;
    return this;
  },

  addCurrency: function (currency) {
    this.currency = currency;
    return this;
  },

  pay: function () {
    console.log(`${this.currency} ${this.amount}`);
  },
};

// Example usage:
payment.addAmount(100).addAmount(200).addAmount(200).pay();  // "₹ 500"
payment.addAmount(200).addCurrency('$').pay();  // "$ 200"
```

#### **Explanation:**

1. **Object Literal**:
   - Instead of using a class, we have an object literal (`payment`) with properties `currency` and `amount`.
   
2. **Method Chaining**:
   - Methods `addAmount` and `addCurrency` are designed to allow chaining by returning `this`, which is the object itself.

3. **Final Action (`pay`)**:
   - The `pay` method simply logs the `currency` and `amount` when called.

### 3. **Advantages of Using the Builder Design Pattern:**

- **Flexible Object Creation**: 
  You can create the object step-by-step, modifying individual parts of it without having to pass all parameters at once to the constructor.
  
- **Cleaner Code**: 
  Instead of having constructors with many parameters, the builder pattern allows for method chaining, which leads to more readable code.
  
- **Customization**: 
  You can create objects with only the properties you need, avoiding the need for many different constructors or complex object creation logic.

### 4. **Real-World Use Case Example:**

Imagine you are building an online payment system that needs to handle various payment methods. Some payments might include tips, discounts, or different currencies. Using the Builder Design Pattern, you can build flexible payment objects with optional properties like `tip`, `discount`, `currency`, and `amount` step by step.

For example:

```javascript
class Payment {
  constructor(currency = '₹', amount = 0) {
    this.currency = currency;
    this.amount = amount;
    this.tip = 0;
    this.discount = 0;
  }

  addAmount(value) {
    this.amount += value;
    return this;
  }

  addCurrency(currency) {
    this.currency = currency;
    return this;
  }

  addTip(value) {
    this.tip += value;
    return this;
  }

  addDiscount(value) {
    this.discount += value;
    return this;
  }

  pay() {
    const finalAmount = this.amount + this.tip - this.discount;
    console.log(`${this.currency} ${finalAmount}`);
  }
}

// Usage example:
const p = new Payment();
p.addAmount(100).addAmount(200).addCurrency('$').addTip(10).addDiscount(20).pay();  // "$ 290"
```

### 5. **Conclusion:**

The Builder Design Pattern helps in creating complex objects step-by-step, allowing you to handle default, optional, or complex parameters with ease. Whether using a class or an object literal, JavaScript allows us to implement this pattern cleanly, supporting method chaining and making code more flexible, readable, and maintainable.


# Builder Design Pattern in JavaScript & React

The **Builder Pattern** is a Creational Design Pattern used to construct complex objects step-by-step.

Instead of:

```javascript
const employee = {
  id: 1,
  name: "Sudhir",
  role: "Project Lead",
  department: "Frontend",
  salary: 100000,
  location: "Pune"
};
```

Use a builder:

```javascript
const employee = new EmployeeBuilder()
  .setId(1)
  .setName("Sudhir")
  .setRole("Project Lead")
  .setDepartment("Frontend")
  .build();
```

***

# Problem Without Builder

Imagine an API request object.

```javascript
const user = {
  name: "Sudhir",
  email: "sudhir@gmail.com",
  age: 35,
  role: "Admin",
  department: "Frontend",
  manager: "Apoorva"
};
```

As fields grow:

```text
10 fields
20 fields
30 fields
```

Object creation becomes messy.

***

# Basic Builder Pattern

## Step 1: Object

```javascript
class Employee {
  constructor(data) {
    Object.assign(this, data);
  }
}
```

***

## Step 2: Builder

```javascript
class EmployeeBuilder {
  constructor() {
    this.employee = {};
  }

  setName(name) {
    this.employee.name = name;
    return this;
  }

  setRole(role) {
    this.employee.role = role;
    return this;
  }

  setSalary(salary) {
    this.employee.salary = salary;
    return this;
  }

  build() {
    return new Employee(
      this.employee
    );
  }
}
```

***

## Usage

```javascript
const employee =
  new EmployeeBuilder()
    .setName("Sudhir")
    .setRole("Project Lead")
    .setSalary(100000)
    .build();

console.log(employee);
```

Output:

```javascript
{
  name: "Sudhir",
  role: "Project Lead",
  salary: 100000
}
```

***

# Fluent Interface

Builder often uses fluent chaining.

```javascript
builder
  .setName("Sudhir")
  .setAge(35)
  .setRole("Lead");
```

Each method returns:

```javascript
return this;
```

***

# Advanced Builder Example

## API Request Builder

```javascript
class RequestBuilder {
  constructor() {
    this.request = {
      headers: {}
    };
  }

  setMethod(method) {
    this.request.method =
      method;
    return this;
  }

  setURL(url) {
    this.request.url =
      url;
    return this;
  }

  setHeader(
    key,
    value
  ) {
    this.request.headers[
      key
    ] = value;
    return this;
  }

  build() {
    return this.request;
  }
}
```

Usage:

```javascript
const request =
  new RequestBuilder()
    .setMethod("GET")
    .setURL("/users")
    .setHeader(
      "Authorization",
      "Bearer Token"
    )
    .build();
```

***

# Builder Pattern in React

Very common in:

✅ Form Configurations

✅ API Configurations

✅ Dynamic Component Generation

✅ Design Systems

✅ Query Builders

***

# React Form Builder

## Builder

```javascript
class FormBuilder {
  constructor() {
    this.fields = [];
  }

  addTextField(
    label,
    name
  ) {
    this.fields.push({
      type: "text",
      label,
      name
    });

    return this;
  }

  addEmailField(
    label,
    name
  ) {
    this.fields.push({
      type: "email",
      label,
      name
    });

    return this;
  }

  build() {
    return this.fields;
  }
}
```

***

## Usage

```javascript
const formConfig =
  new FormBuilder()
    .addTextField(
      "Name",
      "name"
    )
    .addEmailField(
      "Email",
      "email"
    )
    .build();
```

Output:

```javascript
[
  {
    type: "text",
    label: "Name"
  },
  {
    type: "email",
    label: "Email"
  }
]
```

***

## React Component

```jsx
function DynamicForm({
  fields
}) {
  return (
    <form>
      {fields.map(field => (
        <div
          key={field.name}
        >
          <label>
            {field.label}
          </label>

          <input
            type={field.type}
          />
        </div>
      ))}
    </form>
  );
}
```

Usage:

```jsx
<DynamicForm
  fields={formConfig}
/>
```

***

# React Query Builder Pattern

Frequently seen in enterprise apps.

```javascript
class QueryBuilder {
  constructor() {
    this.query = {};
  }

  page(page) {
    this.query.page = page;
    return this;
  }

  pageSize(size) {
    this.query.pageSize =
      size;

    return this;
  }

  search(text) {
    this.query.search =
      text;

    return this;
  }

  build() {
    return this.query;
  }
}
```

Usage:

```javascript
const filters =
  new QueryBuilder()
    .page(1)
    .pageSize(20)
    .search("React")
    .build();
```

Output:

```javascript
{
  page: 1,
  pageSize: 20,
  search: "React"
}
```

***

# Modern JavaScript Functional Builder

Instead of classes:

```javascript
const createUser =
  () => {
    const user = {};

    return {
      name(value) {
        user.name = value;
        return this;
      },

      role(value) {
        user.role = value;
        return this;
      },

      build() {
        return user;
      }
    };
  };

const user =
  createUser()
    .name("Sudhir")
    .role("Lead")
    .build();
```

***

# React Hook Builder Example

```jsx
function useUserBuilder() {
  const buildUser = ({
    name,
    role,
    age
  }) => ({
    name,
    role,
    age,
    createdAt:
      new Date()
  });

  return {
    buildUser
  };
}
```

Usage:

```jsx
const { buildUser } =
  useUserBuilder();

const user =
  buildUser({
    name: "Sudhir",
    role: "Lead",
    age: 35
  });
```

***

# Interview Comparison

| Pattern          | Purpose                            |
| ---------------- | ---------------------------------- |
| Factory          | Create object directly             |
| Builder          | Create complex object step-by-step |
| Singleton        | Single instance                    |
| Prototype        | Clone object                       |
| Abstract Factory | Create family of objects           |

***

# Real React Use Cases

### Form Builder

```javascript
FormBuilder
```

### Query Builder

```javascript
QueryBuilder
```

### Table Configuration

```javascript
ColumnBuilder
```

### UI Builder

```javascript
CardBuilder
```

### API Request Builder

```javascript
RequestBuilder
```

***

# Senior React Interview Answer

> The Builder Pattern is a creational design pattern used to construct complex objects step-by-step through a fluent API. Each builder method returns the builder itself, enabling method chaining. In React applications, the Builder Pattern is commonly used for form builders, query builders, API request configurations, table column definitions, and dynamic UI generation because it improves readability, maintainability, and scalability when dealing with objects that contain many optional properties.


# Builder Pattern in React UI Components

In large React applications (like dashboards, admin portals, design systems), Builder Pattern is extremely useful for creating:

* Dynamic Forms
* Data Tables
* Cards
* Dashboard Widgets
* API Queries
* Page Layouts

***

# Example 1: Card Builder

## Without Builder

```jsx
const card = {
  title: "React Dashboard",
  subtitle: "Frontend Metrics",
  showFooter: true,
  footerText: "Updated Today",
  variant: "primary"
};
```

As properties grow, object creation becomes messy.

***

## Builder Implementation

```javascript
class CardBuilder {
  constructor() {
    this.card = {};
  }

  setTitle(title) {
    this.card.title = title;
    return this;
  }

  setSubtitle(subtitle) {
    this.card.subtitle = subtitle;
    return this;
  }

  setVariant(variant) {
    this.card.variant = variant;
    return this;
  }

  setFooter(text) {
    this.card.showFooter = true;
    this.card.footerText = text;
    return this;
  }

  build() {
    return this.card;
  }
}
```

***

## Build Card Config

```javascript
const cardConfig =
  new CardBuilder()
    .setTitle("React Dashboard")
    .setSubtitle("Frontend Metrics")
    .setVariant("primary")
    .setFooter("Updated Today")
    .build();
```

Result:

```javascript
{
  title:"React Dashboard",
  subtitle:"Frontend Metrics",
  variant:"primary",
  showFooter:true,
  footerText:"Updated Today"
}
```

***

## React Component

```jsx
function Card({
  title,
  subtitle,
  variant,
  footerText
}) {
  return (
    <div className={variant}>
      <h2>{title}</h2>

      <p>{subtitle}</p>

      {footerText && (
        <footer>
          {footerText}
        </footer>
      )}
    </div>
  );
}
```

Usage:

```jsx
<Card {...cardConfig} />
```

***

# Example 2: Dynamic Form Builder

Very common in enterprise projects.

## Builder

```javascript
class FormBuilder {
  constructor() {
    this.fields = [];
  }

  addTextField(
    name,
    label
  ) {
    this.fields.push({
      type: "text",
      name,
      label
    });

    return this;
  }

  addEmailField(
    name,
    label
  ) {
    this.fields.push({
      type: "email",
      name,
      label
    });

    return this;
  }

  addSelectField(
    name,
    label,
    options
  ) {
    this.fields.push({
      type: "select",
      name,
      label,
      options
    });

    return this;
  }

  build() {
    return this.fields;
  }
}
```

***

## Usage

```javascript
const employeeForm =
  new FormBuilder()
    .addTextField(
      "name",
      "Employee Name"
    )
    .addEmailField(
      "email",
      "Email"
    )
    .addSelectField(
      "role",
      "Role",
      [
        "Admin",
        "User"
      ]
    )
    .build();
```

***

## Dynamic React Form

```jsx
function DynamicForm({
  fields
}) {
  return (
    <form>
      {fields.map(field => (
        <div key={field.name}>
          <label>
            {field.label}
          </label>

          {field.type ===
            "text" && (
            <input
              type="text"
            />
          )}

          {field.type ===
            "email" && (
            <input
              type="email"
            />
          )}

          {field.type ===
            "select" && (
            <select>
              {field.options.map(
                option => (
                  <option
                    key={option}
                  >
                    {option}
                  </option>
                )
              )}
            </select>
          )}
        </div>
      ))}
    </form>
  );
}
```

***

# Example 3: Table Configuration Builder

Senior React interview favourite.

```javascript
class TableBuilder {
  constructor() {
    this.columns = [];
  }

  addColumn(
    key,
    title
  ) {
    this.columns.push({
      key,
      title
    });

    return this;
  }

  build() {
    return this.columns;
  }
}
```

Usage:

```javascript
const columns =
  new TableBuilder()
    .addColumn(
      "name",
      "Employee Name"
    )
    .addColumn(
      "role",
      "Role"
    )
    .addColumn(
      "salary",
      "Salary"
    )
    .build();
```

***

# Benefits in Large React Projects

## ✅ Readability

Bad

```javascript
createUser(
  "Sudhir",
  "Lead",
  "Pune",
  100000,
  true,
  ...
);
```

Builder

```javascript
new UserBuilder()
  .setName("Sudhir")
  .setRole("Lead")
  .setLocation("Pune")
  .build();
```

Much clearer.

***

## ✅ Supports Optional Fields

```javascript
new EmployeeBuilder()
  .setName("Sudhir")
  .setRole("Lead")
  .build();
```

No need to pass 20 parameters.

***

## ✅ Avoids Constructor Hell

Bad:

```javascript
new Employee(
  id,
  name,
  role,
  age,
  salary,
  city,
  manager
);
```

Hard to remember order.

***

## ✅ Better Reusability

Builder can be reused across:

```text
Forms
Tables
Cards
Filters
Queries
API Requests
```

***

## ✅ Fluent API

```javascript
builder
 .setTitle()
 .setDescription()
 .setTheme()
 .build();
```

Easy to read.

***

# Builder Pattern Interview Questions

## Q1: What problem does Builder Pattern solve?

**Answer:**

It solves construction of complex objects with many optional properties.

***

## Q2: Difference Between Factory and Builder?

### Factory

```javascript
UserFactory.create()
```

Returns complete object immediately.

***

### Builder

```javascript
new UserBuilder()
 .setName()
 .setRole()
 .build()
```

Creates object step-by-step.

***

## Q3: Why use Builder instead of constructors?

Constructor:

```javascript
new User(
  id,
  name,
  role,
  city,
  manager
);
```

Builder:

```javascript
new UserBuilder()
 .setName(name)
 .setRole(role)
 .build();
```

More readable.

***

## Q4: Where is Builder useful in React?

Typical answers:

```text
Dynamic Forms
API Query Builders
Table Configurations
Dashboard Widgets
Design Systems
Theme Configurations
```

***

## Q5: What is Fluent Interface?

```javascript
builder
 .setName()
 .setRole()
 .setAge()
```

Each method returns:

```javascript
return this;
```

allowing chaining.

***

# Senior React Interview Answer

> The Builder Pattern is a creational design pattern used when an object contains many optional or configurable properties. Instead of passing numerous constructor parameters, a builder creates the object incrementally through a fluent API. In React, Builder Pattern is commonly used for dynamic form generation, table column configuration, dashboard widgets, query builders, API request builders, and reusable design systems. It improves readability, scalability, maintainability, and reduces constructor complexity.


# Complete React Table Example with Sorting + Filtering + Builder Pattern

Let's build a more realistic enterprise-level table.

***

# Enhanced ColumnBuilder

```javascript
class ColumnBuilder {
  constructor(key) {
    this.column = {
      key,
      title: key,
      sortable: false,
      filterable: false,
      width: 150,
      align: "left"
    };
  }

  title(title) {
    this.column.title = title;
    return this;
  }

  sortable() {
    this.column.sortable = true;
    return this;
  }

  filterable() {
    this.column.filterable = true;
    return this;
  }

  customSort(sortFn) {
    this.column.sortable = true;
    this.column.sortFn = sortFn;
    return this;
  }

  customFilter(filterFn) {
    this.column.filterable = true;
    this.column.filterFn = filterFn;
    return this;
  }

  render(renderer) {
    this.column.render = renderer;
    return this;
  }

  width(width) {
    this.column.width = width;
    return this;
  }

  build() {
    return this.column;
  }
}
```

***

# Column Configuration

```javascript
const columns = [
  new ColumnBuilder("name")
    .title("Employee Name")
    .sortable()
    .filterable()
    .build(),

  new ColumnBuilder("role")
    .title("Role")
    .sortable()
    .filterable()
    .build(),

  new ColumnBuilder("salary")
    .title("Salary")
    .sortable()
    .customSort((a, b) => a - b)
    .render(value => `₹${value}`)
    .build()
];
```

***

# Sample Data

```javascript
const employees = [
  {
    id: 1,
    name: "Sudhir",
    role: "Project Lead",
    salary: 120000
  },
  {
    id: 2,
    name: "John",
    role: "Developer",
    salary: 80000
  },
  {
    id: 3,
    name: "Sam",
    role: "Architect",
    salary: 150000
  }
];
```

***

# React Table

```jsx
import React, {
  useState,
  useMemo
} from "react";

function EmployeeTable({
  columns,
  data
}) {
  const [sortKey, setSortKey] =
    useState("");

  const [direction, setDirection] =
    useState("asc");

  const [search, setSearch] =
    useState("");

  const processedData =
    useMemo(() => {
      let result = [...data];

      // Filtering
      if (search) {
        result = result.filter(row =>
          Object.values(row)
            .join(" ")
            .toLowerCase()
            .includes(
              search.toLowerCase()
            )
        );
      }

      // Sorting
      if (sortKey) {
        const column =
          columns.find(
            c => c.key === sortKey
          );

        result.sort((a, b) => {

          if (column?.sortFn) {
            return direction === "asc"
              ? column.sortFn(
                  a[sortKey],
                  b[sortKey]
                )
              : column.sortFn(
                  b[sortKey],
                  a[sortKey]
                );
          }

          return direction === "asc"
            ? String(a[sortKey])
                .localeCompare(
                  String(b[sortKey])
                )
            : String(b[sortKey])
                .localeCompare(
                  String(a[sortKey])
                );
        });
      }

      return result;
    }, [
      data,
      search,
      sortKey,
      direction,
      columns
    ]);

  const handleSort = key => {
    setDirection(prev =>
      prev === "asc"
        ? "desc"
        : "asc"
    );

    setSortKey(key);
  };

  return (
    <>
      <input
        placeholder="Search..."
        value={search}
        onChange={e =>
          setSearch(
            e.target.value
          )
        }
      />

      <table border="1">
        <thead>
          <tr>
            {columns.map(col => (
              <th
                key={col.key}
                onClick={() =>
                  col.sortable &&
                  handleSort(
                    col.key
                  )
                }
              >
                {col.title}
              </th>
            ))}
          </tr>
        </thead>

        <tbody>
          {processedData.map(
            row => (
              <tr
                key={row.id}
              >
                {columns.map(
                  col => (
                    <td
                      key={
                        col.key
                      }
                    >
                      {col.render
                        ? col.render(
                            row[
                              col.key
                            ]
                          )
                        : row[
                            col.key
                          ]}
                    </td>
                  )
                )}
              </tr>
            )
          )}
        </tbody>
      </table>
    </>
  );
}
```

***

# Extending Builder Pattern for Pagination

Pagination is usually **table-level configuration**, not column-level, but can still be built using a Builder.

***

## PaginationBuilder

```javascript
class PaginationBuilder {
  constructor() {
    this.pagination = {
      page: 1,
      pageSize: 10
    };
  }

  page(page) {
    this.pagination.page =
      page;
    return this;
  }

  pageSize(size) {
    this.pagination.pageSize =
      size;
    return this;
  }

  serverSide() {
    this.pagination.serverSide =
      true;
    return this;
  }

  build() {
    return this.pagination;
  }
}
```

***

## Usage

```javascript
const pagination =
  new PaginationBuilder()
    .page(1)
    .pageSize(20)
    .serverSide()
    .build();
```

Output:

```javascript
{
  page: 1,
  pageSize: 20,
  serverSide: true
}
```

***

# Enterprise Table Builder

A more scalable pattern:

```javascript
class TableBuilder {
  constructor() {
    this.config = {
      columns: [],
      pagination: {},
      sorting: true,
      filtering: true
    };
  }

  addColumn(column) {
    this.config.columns.push(
      column
    );
    return this;
  }

  pagination(config) {
    this.config.pagination =
      config;
    return this;
  }

  enableSorting() {
    this.config.sorting = true;
    return this;
  }

  enableFiltering() {
    this.config.filtering =
      true;
    return this;
  }

  build() {
    return this.config;
  }
}
```

Usage:

```javascript
const tableConfig =
  new TableBuilder()
    .addColumn(
      new ColumnBuilder("name")
        .title("Name")
        .sortable()
        .build()
    )
    .addColumn(
      new ColumnBuilder("salary")
        .title("Salary")
        .sortable()
        .build()
    )
    .pagination(
      new PaginationBuilder()
        .pageSize(20)
        .build()
    )
    .build();
```

***

# Why Builder Pattern Improves Maintainability

### Without Builder

```javascript
{
  key: "salary",
  title: "Salary",
  sortable: true,
  filterable: true,
  width: 200,
  align: "right",
  render: ...
}
```

With 30 columns:

```text
Huge config file
Repeated properties
Hard to maintain
```

***

### With Builder

```javascript
new ColumnBuilder("salary")
  .title("Salary")
  .sortable()
  .filterable()
  .render(v => `₹${v}`)
  .build();
```

Benefits:

✅ Fluent API

✅ Self-documenting code

✅ Shared defaults

✅ Consistent column behaviour

✅ Easy feature expansion

✅ Works excellently with TypeScript IntelliSense

✅ Reduces configuration mistakes

***

# Senior React Interview Answer

> In large React projects, Builder Pattern is commonly used for table columns, forms, query builders, dashboards, and API request configurations. It avoids massive configuration objects, provides a fluent and readable API, centralises defaults, and allows new features such as sorting, filtering, pagination, export, permissions, and visibility rules to be added without changing existing code. This significantly improves maintainability and scalability in enterprise applications.
