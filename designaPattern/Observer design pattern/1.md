### **Observer Pattern in JavaScript**

The **Observer Pattern** (also known as **Pub/Sub** or **Publisher/Subscriber** pattern) is a design pattern where an object (the **subject**) maintains a list of observers and notifies them when its state changes. This pattern helps decouple the observer and the subject, allowing independent changes and facilitating event-driven programming. JavaScript frequently uses this pattern, especially in handling DOM events and asynchronous tasks.

Let's break down the various implementations and concepts you provided, as well as how they apply to the **Observer Pattern** in JavaScript.

---

### 1. **Basic Implementation of Observer Pattern**

The `Move` class is a basic example of the Observer Pattern in JavaScript. It allows multiple observers to subscribe, unsubscribe, and be notified when an event is fired.

#### **Code Breakdown**

```javascript
const Move = function () {
  this.handlers = [];

  this.subscribe = function (fn) {
    this.handlers.push(fn);
  };

  this.unsubscribe = function (fn) {
    this.handlers = this.handlers.filter((item) => item !== fn);
  };

  this.fire = function (o, thisObj) {
    const scope = thisObj || window;
    this.handlers.forEach((item) => {
      item.call(scope, o);
    });
  };
};

// 1st observer
const moveHandler = function (item) {
  console.log("fired: " + item);
};

// 2nd observer
const moveHandler2 = function (item) {
  console.log("Moved: " + item);
};

const move = new Move();

// Subscribe 1st observer
move.subscribe(moveHandler);
move.fire('event #1');

// Unsubscribe 1st observer
move.unsubscribe(moveHandler);
move.fire('event #2');

// Subscribe 1st & 2nd observer
move.subscribe(moveHandler);
move.subscribe(moveHandler2);
move.fire('event #3');
```

#### **Output**

```javascript
"fired: event #1"
"fired: event #3"
"Moved: event #3"
```

#### **Explanation**:
- `Move` is the **subject** (or publisher) maintaining a list of observers.
- `subscribe(fn)` adds an observer function to the list.
- `unsubscribe(fn)` removes an observer function from the list.
- `fire(o)` triggers all the subscribed observers with the provided event data.

This is a basic **Pub/Sub** mechanism where the `fire` method notifies all subscribed observers when an event occurs.

---

### 2. **Intersection Observer Example**

In the second example, we use the **IntersectionObserver API** to create a custom observer that tracks elements as they enter or leave the viewport.

#### **Code Breakdown**

```javascript
class MyIntersectionObserver {
    constructor(callbackFn, options = {}) {
        this.callbackFn = callbackFn;
        this.options = options;
        this.intersectionObserver = new IntersectionObserver(this.handleIntersect.bind(this), options);
        this.observedElements = new Set();
    }

    handleIntersect(entries) {
        this.callbackFn(entries);
    }

    observe(element) {
        if (!this.observedElements.has(element)) {
            this.observedElements.add(element);
            this.intersectionObserver.observe(element);
        }
    }

    unobserve(element) {
        if (this.observedElements.has(element)) {
            this.observedElements.delete(element);
            this.intersectionObserver.unobserve(element);
        }
    }

    disconnect() {
        this.intersectionObserver.disconnect();
        this.observedElements.clear();
    }
}
```

#### **Explanation**:
- The `MyIntersectionObserver` class allows you to observe elements entering and exiting the viewport.
- **`observe(element)`** starts observing an element, and **`unobserve(element)`** stops observing it.
- **`handleIntersect(entries)`** is the callback that gets called when an intersection change occurs (e.g., when an element enters or leaves the viewport).
- This setup works similarly to the Observer Pattern: the `IntersectionObserver` is the subject, and the callback function is the observer.

---

### 3. **Creating an Observable with EventTarget and Proxy**

The third example demonstrates how we can combine the **EventTarget interface** with the **Proxy object** to implement the Observer Pattern in JavaScript.

#### **Code Breakdown**

```javascript
class Observable extends EventTarget {
    constructor() {
      super();
      return new Proxy(this, {
        set: (target, property, value) => {
          target[property] = value;
          this.dispatchEvent(new CustomEvent(property, { detail: value }));
          return true;
        },
      });
    }
}

const subject = new Observable();
subject.addEventListener('name', event => {
    console.log(`Name changed to ${event.detail}`);
});

subject.name = 'Alice'; // Name changed to Alice
```

#### **Explanation**:
- **`Observable`** extends `EventTarget`, making it capable of emitting events.
- **`Proxy`** intercepts setting a property on the object and dispatches a `CustomEvent` to notify listeners of changes.
- Whenever `subject.name` is set, an event is triggered and all listeners for that event are notified.

This is a sophisticated approach to the Observer Pattern, utilizing JavaScript's built-in event system and Proxy object to track property changes dynamically.

---

### 4. **Advanced Observable Class with Subscribers**

Another example implements a more advanced version of the Observer pattern with **Subscriber** and **Observable** classes.

#### **Code Breakdown**

```javascript
class Observable {
    constructor(setup) {
        this.setup = setup;
    }

    subscribe(subscriber) {
        const sub = new Subscriber(subscriber);
        this.setup(sub);
        return {
            unsubscribe() {
                sub.unsubscribe();
            }
        }
    }
}

class Subscriber {
    constructor(subscriber) {
        if (typeof subscriber === 'function') {
            this.subscriber = { next: subscriber };
        } else {
            this.subscriber = subscriber;
        }
        this.isUnsubscribed = false;
    }

    next(value) {
        if (this.isUnsubscribed) return;
        if (this.subscriber.next) {
            try {
                this.subscriber.next(value);
            } catch (err) {
                this.error(err);
            }
        }
    }

    error(err) {
        if (this.isUnsubscribed) return;
        if (this.subscriber.error) {
            this.subscriber.error(err);
        }
        this.unsubscribe();
    }

    complete() {
        if (this.isUnsubscribed) return;
        if (this.subscriber.complete) {
            this.subscriber.complete();
        }
        this.unsubscribe();
    }

    unsubscribe() {
        this.isUnsubscribed = true;
    }
}
```

#### **Explanation**:
- **`Observable`** takes a setup function and uses it to control how subscribers interact with it.
- **`Subscriber`** manages how values are delivered to the observer, including handling `next`, `error`, and `complete` notifications.
- **`subscribe`** allows a subscriber to register their own `next`, `error`, and `complete` methods.

This is an implementation that closely resembles the **Reactive Programming** approach, where subscribers react to state changes emitted by the observable.

---

### 5. **`interval` and `fromEvent` Observable Implementations**

You can use observables to handle periodic events like **setInterval** and DOM events with **addEventListener**.

#### **Interval Observable**

```javascript
function interval(period) {
    return new Observable((sub) => {
        let i = 0;
        setInterval(() => {
            sub.next(i++);
        }, period);
    });
}
```

- This `interval` observable emits an incrementing number every `period` milliseconds. Each subscriber receives the current value of `i` at each interval.

#### **From Event Observable**

```javascript
function fromEvent(element, eventName, capture = false) {
    return new Observable((subscriber) => {
        const handler = (e) => subscriber.next(e);
        element.addEventListener(eventName, handler, capture);
        return () => {
            element.removeEventListener(eventName, handler, capture);
        };
    });
}
```

- The `fromEvent` function creates an observable that emits events from a DOM element. Whenever the specified event (like `'click'` or `'mousemove'`) occurs, the observer receives the event object.

---

### Conclusion:

- The **Observer Pattern** is a core concept in JavaScript, enabling asynchronous and event-driven programming.
- JavaScript frameworks and libraries like **RxJS** utilize this pattern to handle streams of data (events, HTTP requests, animations, etc.).
- In simple scenarios, custom event handling with **EventTarget** and **Proxy** can implement this pattern, while more advanced patterns use **Observers**, **Subscribers**, and **Reactive Programming** concepts.

This pattern ensures that systems can react to changes in state without tightly coupling components, making your code easier to manage and extend.


# Observer Pattern in JavaScript & React

The **Observer Pattern** is a behavioural design pattern where:

```text
Subject (Publisher)
          ↓
Notify
          ↓
Observers (Subscribers)
```

When the subject changes, all subscribed observers are automatically notified.

***

# Real World Examples

```text
YouTube Channel
       ↓
Uploads Video
       ↓
Subscribers Notified
```

```text
Redux Store
       ↓
State Changes
       ↓
Components Re-render
```

```text
WebSocket
       ↓
New Message
       ↓
UI Updates
```

***

# Basic Observer Pattern

## Subject

```javascript
class Subject {
  constructor() {
    this.observers = [];
  }

  subscribe(observer) {
    this.observers.push(observer);
  }

  unsubscribe(observer) {
    this.observers =
      this.observers.filter(
        obs => obs !== observer
      );
  }

  notify(data) {
    this.observers.forEach(
      observer => observer(data)
    );
  }
}
```

***

## Usage

```javascript
const newsChannel =
  new Subject();

const subscriber1 =
  data =>
    console.log(
      "User 1:",
      data
    );

const subscriber2 =
  data =>
    console.log(
      "User 2:",
      data
    );

newsChannel.subscribe(
  subscriber1
);

newsChannel.subscribe(
  subscriber2
);

newsChannel.notify(
  "Breaking News"
);
```

Output:

```text
User 1: Breaking News
User 2: Breaking News
```

***

# React Example

## Event Bus

```javascript
// eventBus.js

class EventBus {

  constructor() {
    this.listeners = {};
  }

  subscribe(
    event,
    callback
  ) {

    if (
      !this.listeners[event]
    ) {
      this.listeners[event] = [];
    }

    this.listeners[event]
      .push(callback);

    return () => {
      this.listeners[event] =
        this.listeners[event]
          .filter(
            cb =>
              cb !== callback
          );
    };
  }

  publish(
    event,
    data
  ) {

    this.listeners[event]
      ?.forEach(
        callback =>
          callback(data)
      );
  }
}

export default new EventBus();
```

***

# Publisher Component

```jsx
import EventBus
from "./eventBus";

function UserForm() {

  const saveUser = () => {

    EventBus.publish(
      "USER_CREATED",
      {
        id: 1,
        name: "Sudhir"
      }
    );
  };

  return (
    <button
      onClick={saveUser}
    >
      Save User
    </button>
  );
}
```

***

# Subscriber Component

```jsx
import {
  useEffect,
  useState
} from "react";

import EventBus
from "./eventBus";

function UserList() {

  const [users,
         setUsers] =
    useState([]);

  useEffect(() => {

    const unsubscribe =
      EventBus.subscribe(
        "USER_CREATED",
        user => {

          setUsers(
            prev => [
              ...prev,
              user
            ]
          );
        }
      );

    return unsubscribe;

  }, []);

  return (
    <>
      {users.map(user => (
        <div key={user.id}>
          {user.name}
        </div>
      ))}
    </>
  );
}
```

***

# Observer Pattern with WebSocket

```javascript
class ChatService {

  constructor() {
    this.observers = [];
  }

  subscribe(callback) {
    this.observers.push(callback);
  }

  receiveMessage(
    message
  ) {

    this.observers.forEach(
      cb => cb(message)
    );
  }
}
```

Usage:

```javascript
chatService.subscribe(
  msg =>
    console.log(msg)
);
```

***

# How Redux Uses Observer Pattern

```text
Redux Store
     ↓
dispatch()
     ↓
notify subscribers
     ↓
React updates
```

Example:

```javascript
store.subscribe(() => {
  console.log(
    store.getState()
  );
});
```

Observer Pattern internally.

***

# Custom Hook Example

```jsx
import {
  useEffect,
  useState
} from "react";

function useObserver(
  eventName
) {

  const [data,
         setData] =
    useState(null);

  useEffect(() => {

    const unsubscribe =
      EventBus.subscribe(
        eventName,
        setData
      );

    return unsubscribe;

  }, [eventName]);

  return data;
}
```

Usage:

```jsx
const user =
  useObserver(
    "USER_CREATED"
  );
```

***

# Advantages

### Loose Coupling

Publisher doesn't know:

```text
Who subscribes
How many subscribers exist
```

***

### Scalability

```text
1 Event
100 Subscribers
```

Works easily.

***

### Reusability

```javascript
publish()
subscribe()
unsubscribe()
```

Reusable everywhere.

***

# Disadvantages

### Memory Leaks

If you forget:

```javascript
unsubscribe()
```

subscriptions remain.

***

### Debugging Complexity

Harder to trace:

```text
Who triggered the event?
```

in large systems.

***

# Interview Questions

## Q1. What is Observer Pattern?

**Answer:**

A behavioural pattern where objects subscribe to changes and are notified automatically when the subject's state changes.

***

## Q2. What are the main parts?

```text
Subject (Publisher)
Observer (Subscriber)
```

***

## Q3. Difference Between Observer and Pub/Sub?

### Observer

```text
Direct relationship
```

```javascript
subject.subscribe()
```

### Pub/Sub

```text
Uses event bus/message broker
```

Publisher and subscriber don't know each other.

***

## Q4. Real React Examples?

✅ Redux Store

✅ Zustand Store

✅ RxJS

✅ WebSockets

✅ Event Bus

✅ React Context Updates

✅ Notifications

***

## Q5. How to avoid memory leaks?

Always unsubscribe.

```javascript
useEffect(() => {

  const unsubscribe =
    subscribe();

  return unsubscribe;

}, []);
```

***

## Q6. Observer vs Event Emitter?

Very similar.

```javascript
on()
emit()
off()
```

is essentially Observer Pattern.

***

# Senior React Interview Answer

> The Observer Pattern establishes a one-to-many relationship between a subject and its observers. When the subject changes, all observers are notified automatically. In React, common examples include Redux subscriptions, Zustand stores, WebSocket listeners, notification systems, and custom event buses. The pattern promotes loose coupling and scalability but requires careful cleanup of subscriptions to prevent memory leaks.



# 1. How to Implement `unsubscribe()` in React

One of the biggest problems with the Observer Pattern in React is **memory leaks**.

If a component subscribes but never unsubscribes:

```text
Component Unmounted
        ↓
Listener Still Exists
        ↓
Memory Leak
```

***

## Event Bus

```javascript
class EventBus {
  constructor() {
    this.listeners = {};
  }

  subscribe(event, callback) {
    if (!this.listeners[event]) {
      this.listeners[event] = [];
    }

    this.listeners[event].push(callback);

    return () => {
      this.listeners[event] =
        this.listeners[event].filter(
          cb => cb !== callback
        );
    };
  }

  publish(event, data) {
    this.listeners[event]
      ?.forEach(cb => cb(data));
  }
}

export default new EventBus();
```

***

## React Component

```jsx
import { useEffect } from "react";
import EventBus from "./eventBus";

function UserList() {
  useEffect(() => {

    const unsubscribe =
      EventBus.subscribe(
        "USER_CREATED",
        data => {
          console.log(data);
        }
      );

    return () => {
      unsubscribe();
    };

  }, []);

  return <h1>Users</h1>;
}
```

### Why?

When component unmounts:

```javascript
return () => {
  unsubscribe();
}
```

runs automatically.

***

# 2. Observer Pattern with Redux

Redux internally implements the Observer Pattern.

***

## Redux Store

```javascript
import {
  createStore
} from "redux";

const reducer = (
  state = {
    count: 0
  },
  action
) => {

  switch (action.type) {

    case "INCREMENT":
      return {
        count:
          state.count + 1
      };

    default:
      return state;
  }
};

const store =
  createStore(reducer);
```

***

## Subscribe

```javascript
const unsubscribe =
  store.subscribe(() => {

    console.log(
      store.getState()
    );

  });
```

***

## Dispatch

```javascript
store.dispatch({
  type: "INCREMENT"
});
```

Output:

```javascript
{
  count: 1
}
```

Flow:

```text
Store (Subject)
      ↓
State Updated
      ↓
Subscribers Notified
      ↓
React Re-renders
```

***

## Unsubscribe

```javascript
unsubscribe();
```

No more notifications.

***

# 3. Custom Hook Using Observer Pattern

This is a common Senior React interview example.

***

## Event Bus

```javascript
class EventBus {

  constructor() {
    this.listeners = {};
  }

  subscribe(
    event,
    callback
  ) {

    if (
      !this.listeners[event]
    ) {
      this.listeners[event] = [];
    }

    this.listeners[event]
      .push(callback);

    return () => {

      this.listeners[event] =
        this.listeners[event]
          .filter(
            cb =>
              cb !== callback
          );
    };
  }

  publish(
    event,
    payload
  ) {

    this.listeners[event]
      ?.forEach(cb =>
        cb(payload)
      );
  }
}

export default new EventBus();
```

***

## Custom Hook

```jsx
import {
  useEffect,
  useState
} from "react";

import EventBus from "./eventBus";

export function useObserver(
  eventName
) {

  const [data,
         setData] =
    useState(null);

  useEffect(() => {

    const unsubscribe =
      EventBus.subscribe(
        eventName,
        setData
      );

    return unsubscribe;

  }, [eventName]);

  return data;
}
```

***

## Publisher Component

```jsx
import EventBus
from "./eventBus";

function UserForm() {

  const saveUser = () => {

    EventBus.publish(
      "USER_CREATED",
      {
        id: 1,
        name: "Sudhir"
      }
    );
  };

  return (
    <button
      onClick={saveUser}
    >
      Create User
    </button>
  );
}
```

***

## Subscriber Component

```jsx
import {
  useObserver
} from "./useObserver";

function UserNotification() {

  const user =
    useObserver(
      "USER_CREATED"
    );

  if (!user) {
    return null;
  }

  return (
    <div>
      User Created:
      {user.name}
    </div>
  );
}
```

***

# Advanced Hook (Multiple Events)

```jsx
export function useEventBus(
  eventName,
  initialValue = null
) {

  const [state, setState] =
    useState(initialValue);

  useEffect(() => {

    const unsubscribe =
      EventBus.subscribe(
        eventName,
        payload =>
          setState(payload)
      );

    return unsubscribe;

  }, [eventName]);

  return state;
}
```

Usage:

```jsx
const newUser =
  useEventBus(
    "USER_CREATED"
  );

const newProject =
  useEventBus(
    "PROJECT_CREATED"
  );
```

***

# Real-World React Use Cases

### Notifications

```text
Notification Service
     ↓
Subscribers Updated
```

***

### WebSocket Messages

```text
New Message
      ↓
Observers Notified
```

***

### Redux Store Updates

```text
dispatch()
      ↓
subscribe()
```

***

### Feature Flags

```text
Flag Changed
      ↓
Components Updated
```

***

### Global Event Bus

```text
Modal Opened
Theme Changed
Language Changed
User Logged In
```

***

# Interview Questions

### Q1. Why is `unsubscribe()` important?

**Answer:**

To prevent memory leaks and unnecessary re-renders when a component is unmounted.

***

### Q2. How does Redux use Observer Pattern?

```text
Store = Subject
Components = Observers
```

The store notifies subscribers whenever state changes.

***

### Q3. Why use a custom observer hook?

Benefits:

```text
Reusable
Encapsulated
Automatic Cleanup
Less Boilerplate
```

***

### Q4. Observer vs Pub/Sub?

**Observer**

```text
Subject knows observers.
```

**Pub/Sub**

```text
Publisher and subscribers communicate through an event bus.
```

***

# Senior React Interview Answer

> React applications frequently use the Observer Pattern through Redux subscriptions, Zustand stores, WebSocket listeners, and custom event buses. Always return an `unsubscribe()` function from subscriptions and invoke it in the `useEffect` cleanup phase to prevent memory leaks. A common enterprise implementation is a reusable `useObserver()` custom hook that encapsulates subscription management, event handling, and automatic cleanup, allowing components to react to application-wide events without tight coupling.


# Custom Hook for Multiple Event Subscriptions (Observer Pattern)

In large React applications, you may want a component to listen to **multiple events**:

```text
USER_CREATED
USER_UPDATED
USER_DELETED
PROJECT_CREATED
NOTIFICATION_RECEIVED
```

Instead of writing multiple `useEffect` subscriptions, create a reusable hook.

***

# Step 1: Event Bus

```javascript
// eventBus.js

class EventBus {
  constructor() {
    this.listeners = {};
  }

  subscribe(eventName, callback) {
    if (!this.listeners[eventName]) {
      this.listeners[eventName] = [];
    }

    this.listeners[eventName].push(callback);

    return () => {
      this.listeners[eventName] =
        this.listeners[eventName].filter(
          cb => cb !== callback
        );
    };
  }

  publish(eventName, payload) {
    this.listeners[eventName]?.forEach(
      callback => callback(payload)
    );
  }
}

export default new EventBus();
```

***

# Step 2: Custom Hook for Multiple Events

```javascript
// useMultiObserver.js

import { useEffect, useState } from "react";
import EventBus from "./eventBus";

export function useMultiObserver(events = []) {
  const [eventData, setEventData] =
    useState({});

  useEffect(() => {
    const unsubscribers = events.map(
      eventName =>
        EventBus.subscribe(
          eventName,
          payload => {
            setEventData(prev => ({
              ...prev,
              [eventName]: payload
            }));
          }
        )
    );

    return () => {
      unsubscribers.forEach(
        unsubscribe => unsubscribe()
      );
    };
  }, [events]);

  return eventData;
}
```

***

# Step 3: Publisher Component

```jsx
import EventBus from "./eventBus";

function DashboardActions() {
  const createUser = () => {
    EventBus.publish(
      "USER_CREATED",
      {
        id: 1,
        name: "Sudhir"
      }
    );
  };

  const createProject = () => {
    EventBus.publish(
      "PROJECT_CREATED",
      {
        id: 101,
        name: "React Migration"
      }
    );
  };

  return (
    <>
      <button onClick={createUser}>
        Create User
      </button>

      <button onClick={createProject}>
        Create Project
      </button>
    </>
  );
}

export default DashboardActions;
```

***

# Step 4: Subscriber Component

```jsx
import { useMultiObserver }
from "./useMultiObserver";

function DashboardNotifications() {

  const events =
    useMultiObserver([
      "USER_CREATED",
      "PROJECT_CREATED"
    ]);

  return (
    <div>

      {events.USER_CREATED && (
        <div>
          User Created:
          {" "}
          {events.USER_CREATED.name}
        </div>
      )}

      {events.PROJECT_CREATED && (
        <div>
          Project Created:
          {" "}
          {events.PROJECT_CREATED.name}
        </div>
      )}

    </div>
  );
}

export default DashboardNotifications;
```

***

# Advanced Version – Keep Event History

Instead of storing only the latest event:

```javascript
export function useMultiObserver(events = []) {

  const [history,
         setHistory] =
    useState([]);

  useEffect(() => {

    const unsubscribers =
      events.map(eventName =>
        EventBus.subscribe(
          eventName,
          payload => {

            setHistory(prev => [
              ...prev,
              {
                eventName,
                payload,
                timestamp:
                  Date.now()
              }
            ]);
          }
        )
      );

    return () => {
      unsubscribers.forEach(
        unsub => unsub()
      );
    };

  }, [events]);

  return history;
}
```

***

# Usage

```jsx
const history =
  useMultiObserver([
    "USER_CREATED",
    "USER_DELETED"
  ]);
```

Output:

```javascript
[
  {
    eventName:
      "USER_CREATED",
    payload: {
      id: 1
    }
  },
  {
    eventName:
      "USER_DELETED",
    payload: {
      id: 1
    }
  }
]
```

***

# Enterprise Use Cases

### Notification System

```text
USER_CREATED
PROJECT_UPDATED
TASK_ASSIGNED
```

***

### WebSocket Events

```text
MESSAGE_RECEIVED
USER_ONLINE
USER_OFFLINE
```

***

### Dashboard Events

```text
REPORT_GENERATED
EXPORT_COMPLETED
```

***

### Feature Flags

```text
FEATURE_ENABLED
FEATURE_DISABLED
```

***

# Interview Question

### Why return cleanup?

```javascript
return () => {
  unsubscribers.forEach(
    unsub => unsub()
  );
};
```

Answer:

```text
Prevent Memory Leaks
Remove Subscribers
Avoid Duplicate Listeners
```

***

# Senior React Interview Answer

> A reusable `useMultiObserver` hook can subscribe to multiple application events using the Observer Pattern. The hook centralises subscription management, automatically cleans up listeners on unmount, and exposes event data or event history to React components. This pattern is useful for notifications, WebSocket updates, analytics events, dashboard refreshes, and cross-component communication while keeping components loosely coupled and easy to maintain.


# Observer Pattern with Typed Event Payloads (TypeScript)

In React projects, especially large enterprise apps, it's common to define **event payload types** for better IntelliSense and type safety.

***

## Define Event Types

```typescript
// eventTypes.ts

export type AppEvents = {
  USER_CREATED: {
    id: number;
    name: string;
    email: string;
  };

  USER_DELETED: {
    id: number;
  };

  PROJECT_CREATED: {
    id: number;
    projectName: string;
  };

  NOTIFICATION_RECEIVED: {
    message: string;
    type: "success" | "error";
  };
};
```

***

## Typed EventBus

```typescript
class EventBus<T extends Record<string, any>> {
  private listeners: {
    [K in keyof T]?: Array<
      (payload: T[K]) => void
    >;
  } = {};

  subscribe<K extends keyof T>(
    event: K,
    callback: (payload: T[K]) => void
  ) {
    this.listeners[event] ??= [];

    this.listeners[event]!.push(
      callback
    );

    return () => {
      this.listeners[event] =
        this.listeners[event]?.filter(
          cb => cb !== callback
        );
    };
  }

  publish<K extends keyof T>(
    event: K,
    payload: T[K]
  ) {
    this.listeners[event]?.forEach(
      callback => callback(payload)
    );
  }
}
```

***

## Create Bus

```typescript
import { AppEvents }
from "./eventTypes";

export const eventBus =
  new EventBus<AppEvents>();
```

***

# Testing `useMultiObserver` Hook

Assume:

```typescript
const events =
  useMultiObserver([
    "USER_CREATED",
    "PROJECT_CREATED"
  ]);
```

***

## Hook

```typescript
export function useMultiObserver(
  eventNames: string[]
) {
  const [data, setData] =
    useState({});

  useEffect(() => {

    const unsubscribers =
      eventNames.map(event =>
        eventBus.subscribe(
          event,
          payload => {

            setData(prev => ({
              ...prev,
              [event]: payload
            }));

          }
        )
      );

    return () => {
      unsubscribers.forEach(
        unsubscribe =>
          unsubscribe()
      );
    };

  }, [eventNames]);

  return data;
}
```

***

## Hook Test

```typescript
import {
  renderHook,
  act
} from "@testing-library/react";

import { eventBus }
from "./eventBus";

test(
  "receives USER_CREATED event",
  () => {

    const { result } =
      renderHook(() =>
        useMultiObserver([
          "USER_CREATED"
        ])
      );

    act(() => {

      eventBus.publish(
        "USER_CREATED",
        {
          id: 1,
          name: "Sudhir",
          email:
            "sudhir@test.com"
        }
      );

    });

    expect(
      result.current
        .USER_CREATED
    ).toEqual({
      id: 1,
      name: "Sudhir",
      email:
        "sudhir@test.com"
    });

  }
);
```

***

# Test Multiple Events

```typescript
test(
  "handles multiple events",
  () => {

    const { result } =
      renderHook(() =>
        useMultiObserver([
          "USER_CREATED",
          "PROJECT_CREATED"
        ])
      );

    act(() => {

      eventBus.publish(
        "USER_CREATED",
        {
          id: 1,
          name: "Sudhir",
          email:
            "test@test.com"
        }
      );

      eventBus.publish(
        "PROJECT_CREATED",
        {
          id: 101,
          projectName:
            "React Migration"
        }
      );

    });

    expect(
      result.current
        .USER_CREATED
        .name
    ).toBe("Sudhir");

    expect(
      result.current
        .PROJECT_CREATED
        .projectName
    ).toBe(
      "React Migration"
    );

  }
);
```

***

# Unsubscribe from Specific Events

Sometimes you don't want to unsubscribe from everything.

***

## EventBus Enhancement

```typescript
subscribe(
  event,
  callback
) {

  this.listeners[event] ??= [];

  this.listeners[event].push(
    callback
  );

  return {
    unsubscribe: () => {
      this.listeners[event] =
        this.listeners[event]
          .filter(
            cb =>
              cb !== callback
          );
    }
  };
}
```

***

## Usage

```typescript
const userSubscription =
  eventBus.subscribe(
    "USER_CREATED",
    payload => {
      console.log(payload);
    }
  );

const projectSubscription =
  eventBus.subscribe(
    "PROJECT_CREATED",
    payload => {
      console.log(payload);
    }
  );
```

***

## Unsubscribe Specific Event

```typescript
userSubscription.unsubscribe();
```

Now:

```typescript
eventBus.publish(
  "USER_CREATED",
  {
    id: 1,
    name: "Sudhir",
    email:
      "test@test.com"
  }
);
```

Nothing happens.

***

## PROJECT\_CREATED Still Works

```typescript
eventBus.publish(
  "PROJECT_CREATED",
  {
    id: 101,
    projectName:
      "React Migration"
  }
);
```

Output:

```text
React Migration
```

***

# React Example of Selective Unsubscribe

```typescript
useEffect(() => {

  const userSub =
    eventBus.subscribe(
      "USER_CREATED",
      user => {
        console.log(user);
      }
    );

  return () => {
    userSub.unsubscribe();
  };

}, []);
```

***

# Senior React Interview Answer

A production-grade Observer implementation should:

```text
✅ Use typed event payloads
✅ Support multiple subscriptions
✅ Support individual unsubscribe
✅ Cleanup automatically in useEffect
✅ Be fully unit tested
✅ Handle event history if required
✅ Prevent memory leaks
```

For React applications, the best approach is to encapsulate all subscription logic inside reusable hooks (`useObserver`, `useMultiObserver`) and ensure each subscription returns an unsubscribe function that is invoked during component cleanup. This provides a scalable pattern for notifications, WebSockets, analytics events, feature flags, and cross-component communication.
