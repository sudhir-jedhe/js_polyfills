### What is Prototype in JavaScript?

In JavaScript, every object has an internal property called **`[[Prototype]]`** (commonly accessed via `__proto__`), which refers to another object that serves as the "parent" or prototype of that object. The prototype object itself can have properties and methods, which will be inherited by any object that points to that prototype. 

In other words, the prototype allows JavaScript objects to share behavior (methods, properties) without having to duplicate them for every single object instance. This leads to more memory-efficient and maintainable code.

### **Prototype Chain**

The **prototype chain** is the mechanism by which JavaScript objects can inherit properties and methods from other objects. When you try to access a property or method of an object, JavaScript will first check if that property exists directly on the object. If it doesn’t, JavaScript will look at the object's prototype and check for the property there. If the property isn't found in the prototype, it will continue looking through the prototype’s prototype, and so on, until it reaches the top of the prototype chain (usually `Object.prototype`).

### Example Breakdown

Let’s walk through the examples you provided to understand how prototypes and the prototype chain work:

---

### 1. **Basic Example with `__proto__`**

In this example, you're creating an instance of the `Robot` class, and we can see how the `__proto__` works.

```javascript
class Robot {
  constructor(name) {
    this.name = name;
  }

  fire() {
    return "phew phew";
  }
}

const robo1 = new Robot("RoboCop");

console.log(robo1); // Logs the instance of the Robot object
console.log(robo1.__proto__); // Logs the prototype of the Robot class
```

#### Explanation:
- **`robo1.__proto__`** refers to the prototype object of `Robot`.
- The prototype of `Robot` contains the methods defined in the class like `fire()`.
- You can see that `robo1.__proto__` will point to `Robot.prototype`.

---

### 2. **Adding Methods to the Prototype**

In this example, you add a method `report()` to the **prototype** of `Robot`. This means all instances of `Robot` will inherit this method.

```javascript
class Robot {
  constructor(name) {
    this.name = name;
  }

  fire() {
    return 'phew phew';
  }
}

const robo1 = new Robot('RoboCop');
const robo2 = new Robot('AutoBot');

Robot.prototype.report = function() {
  console.log(`${this.name} reporting sir!`);
};

robo1.report(); // RoboCop reporting sir!
robo2.report(); // AutoBot reporting sir!
```

#### Explanation:
- `Robot.prototype.report` is a method added to `Robot`'s prototype. Since both `robo1` and `robo2` are instances of `Robot`, they both inherit this method and can invoke it.
- This is a key feature of **prototypal inheritance** in JavaScript: methods defined on the prototype are shared across all instances of the class.

---

### 3. **Prototype Inheritance and Overriding Properties**

Now, we see an interesting case where the `name` property is added to `Robot.prototype`, and its behavior changes.

```javascript
class Robot {
  constructor(name) {
    // this.name = name;
  }

  fire() {
    return 'phew phew';
  }
}

const robo1 = new Robot('RoboCop');
const robo2 = new Robot('AutoBot');

Robot.prototype.report = function() {
  console.log(`${this.name} reporting sir!`);
};

Robot.prototype.name = "abc";

robo1.report(); // abc reporting sir!
robo2.report(); // abc reporting sir!
```

#### Explanation:
- In this case, **`Robot.prototype.name = "abc"`** is setting a property `name` directly on the prototype, rather than the individual objects.
- Since **both `robo1` and `robo2`** are pointing to the same prototype, they both get `"abc"` as the value for `name`, not the `"RoboCop"` or `"AutoBot"` values that were passed to the constructors.
- This demonstrates how properties on the prototype affect all instances that inherit from it, which is different from instance properties that are set in the constructor.

---

### 4. **Class Inheritance: `Transform` extends `Robot`**

Here, the `Transform` class inherits from `Robot` using the `extends` keyword. This is classical inheritance, where the child class (or subclass) inherits from the parent class (or superclass).

```javascript
class Robot {
  constructor(name) {
    this.name = name;
  }

  fire() {
    return 'phew phew';
  }
}

class Transform extends Robot {
  constructor(name, nature) {
    super(name);
    this.nature = nature;
  }

  getNature() {
    console.log(`I am ${this.nature}`);
  }
}

const robo1 = new Transform('RoboCop', 'good');
const robo2 = new Transform('AutoBot', 'evil');

Robot.prototype.report = function() {
  console.log(`${this.name} reporting sir!`);
};

robo1.report(); // RoboCop reporting sir!
robo2.report(); // AutoBot reporting sir!
```

#### Explanation:
- **`Transform` class** is a subclass of `Robot`. The `super(name)` call in `Transform`'s constructor ensures that the `name` property is set using the parent class's constructor.
- Both `robo1` and `robo2` are instances of `Transform`. Since `Transform` extends `Robot`, these instances inherit all methods and properties from `Robot`, including `report()`.
- Notice that the **prototype chain** is now:
  - `robo1` (or `robo2`) → `Transform.prototype` → `Robot.prototype` → `Object.prototype`.

---

### Prototype Chain in Action

The **prototype chain** allows objects to inherit properties and methods from their constructors and prototypes.

1. When `robo1.report()` is called, JavaScript first looks for the `report` method on `robo1` itself (which doesn't exist).
2. It then checks `Transform.prototype` (the prototype of `robo1`) for the `report` method, but doesn't find it.
3. Next, it looks at `Robot.prototype` and finds the `report` method there.
4. Finally, `robo1` calls the method found in `Robot.prototype`.

This is the core behavior of the **prototype chain** in JavaScript: each object looks up the chain of prototypes when accessing properties or methods that are not directly defined on the object.

---

### Key Takeaways:

1. **Prototypes** are special objects in JavaScript that allow inheritance of properties and methods.
2. Every object has a `__proto__` property (an internal reference to its prototype), which points to the object from which it inherits.
3. The **prototype chain** enables objects to inherit properties and methods from their constructor functions and parent classes.
4. You can add properties or methods directly to an object's prototype to be shared across all instances of that object.
5. JavaScript uses **prototypal inheritance**, not classical inheritance, meaning objects can directly inherit from other objects, not just classes.

Understanding the prototype chain and how inheritance works in JavaScript is crucial for effective object-oriented programming in the language.



# Prototype Design Pattern in JavaScript & React

The **Prototype Pattern** is a creational design pattern used to create new objects by **cloning existing objects (prototypes)** instead of creating them from scratch.

***

# Why Use Prototype Pattern?

Imagine creating many similar objects:

```javascript
const employee1 = {
  name: "Sudhir",
  role: "Project Lead",
  department: "Frontend"
};

const employee2 = {
  name: "John",
  role: "Project Lead",
  department: "Frontend"
};
```

Lots of duplication.

Instead, create one prototype and clone it.

***

# Basic JavaScript Prototype Example

```javascript
const employeePrototype = {
  role: "Developer",

  getRole() {
    return this.role;
  }
};

const employee1 =
  Object.create(
    employeePrototype
  );

employee1.name = "Sudhir";

console.log(
  employee1.getRole()
);
```

Output:

```javascript
Developer
```

***

# Clone Using Object.create()

```javascript
const carPrototype = {
  wheels: 4,

  start() {
    console.log(
      "Car Started"
    );
  }
};

const car1 =
  Object.create(
    carPrototype
  );

car1.brand = "BMW";

const car2 =
  Object.create(
    carPrototype
  );

car2.brand = "Audi";
```

Output:

```javascript
car1.start();
car2.start();
```

***

# Prototype Pattern with Factory

```javascript
const userPrototype = {

  greet() {
    return `Hello ${this.name}`;
  }

};

function createUser(name) {

  const user =
    Object.create(
      userPrototype
    );

  user.name = name;

  return user;
}

const user =
  createUser(
    "Sudhir"
  );

console.log(
  user.greet()
);
```

Output:

```javascript
Hello Sudhir
```

***

# Clone Existing Object

```javascript
const employee = {
  name: "Sudhir",
  role: "Lead",

  work() {
    return "Working...";
  }
};

const clonedEmployee =
  Object.create(employee);

clonedEmployee.name =
  "John";

console.log(
  clonedEmployee.work()
);
```

Output:

```javascript
Working...
```

***

# Prototype vs Deep Clone

### Prototype Clone

```javascript
const clone =
  Object.create(
    original
  );
```

Shares prototype chain.

***

### Deep Clone

```javascript
const clone =
  structuredClone(
    original
  );
```

Creates completely separate copy.

***

# React Example: Component Configuration Prototype

Imagine reusable card configurations.

```javascript
const cardPrototype = {

  variant: "primary",

  showFooter: true,

  render() {
    return `${this.title}`;
  }

};
```

***

## Clone Card Configs

```javascript
const dashboardCard =
  Object.create(
    cardPrototype
  );

dashboardCard.title =
  "Dashboard";

const usersCard =
  Object.create(
    cardPrototype
  );

usersCard.title =
  "Users";
```

Result:

```javascript
{
  title: "Dashboard",
  variant: "primary"
}
```

***

# React Example: Form Prototype

```javascript
const inputPrototype = {

  type: "text",

  required: true,

  placeholder: ""
};
```

Clone:

```javascript
const usernameField =
  Object.create(
    inputPrototype
  );

usernameField.placeholder =
  "Username";

const emailField =
  Object.create(
    inputPrototype
  );

emailField.type =
  "email";
```

Used in dynamic forms.

***

# Class-Based Prototype Example

```javascript
class Employee {

  constructor(name) {
    this.name = name;
  }

  work() {
    return `${this.name} working`;
  }
}
```

JavaScript internally stores methods on:

```javascript
Employee.prototype
```

***

Usage:

```javascript
const emp =
  new Employee(
    "Sudhir"
  );

console.log(
  emp.work()
);
```

Prototype chain:

```text
emp
 ↓
Employee.prototype
 ↓
Object.prototype
```

***

# React Use Cases

### Dynamic Form Builders

```javascript
Field Prototype
↓
Clone
↓
Username Field
Email Field
Password Field
```

***

### Dashboard Widgets

```javascript
Widget Prototype
↓
Clone
↓
Chart Widget
Table Widget
```

***

### Table Columns

```javascript
Default Column
↓
Clone
↓
Name Column
Salary Column
```

***

### Theme Configuration

```javascript
Theme Prototype
↓
Clone
↓
Dark Theme
Light Theme
```

***

# Prototype Pattern vs Factory Pattern

### Factory

```javascript
UserFactory.create()
```

Creates a new object from scratch.

***

### Prototype

```javascript
Object.create()
```

Creates by cloning an existing object.

***

# Prototype Pattern vs Builder Pattern

### Builder

```javascript
new UserBuilder()
  .setName()
  .setRole()
  .build();
```

Constructs step-by-step.

***

### Prototype

```javascript
const clone =
  Object.create(
    existingObject
  );
```

Clones an existing object.

***

# Interview Questions

### Q1. What is Prototype Pattern?

**Answer:**

A creational pattern that creates new objects by cloning existing prototype objects.

***

### Q2. Which JavaScript method is commonly used?

```javascript
Object.create()
```

***

### Q3. Difference Between Prototype Pattern and Deep Cloning?

Prototype:

```javascript
Object.create()
```

Uses prototype inheritance.

Deep Clone:

```javascript
structuredClone()
```

Copies all data independently.

***

### Q4. How Does JavaScript Use Prototypes?

Every object inherits from a prototype chain:

```text
Object
 ↓
Prototype
 ↓
Instance
```

***

### Q5. Real React Use Cases?

✅ Dynamic Forms

✅ Table Configurations

✅ Dashboard Widgets

✅ Theme Objects

✅ Configuration Templates

***

# Senior React Interview Answer

> The Prototype Pattern is a creational design pattern that creates new objects by cloning existing prototype objects rather than constructing them from scratch. JavaScript has built-in prototype-based inheritance, making the pattern natural to implement using `Object.create()`. In React applications, Prototype Pattern is useful for creating reusable configuration objects such as form fields, table column definitions, dashboard widgets, and themes. It helps reduce duplication, promotes consistency, and improves maintainability when many similar objects share common behaviour.



# Prototype Pattern in React – Complete Enterprise Examples

The **Prototype Pattern** is useful when you want to create new objects by cloning a base configuration instead of redefining everything repeatedly.

```text
Prototype Object
        ↓
Clone
        ↓
Customise Properties
        ↓
Use in UI
```

***

# 1. Dynamic Form Using Prototype Pattern

## Field Prototype

```javascript
// prototypes/fieldPrototype.js

export const fieldPrototype = {
  type: "text",
  label: "",
  placeholder: "",
  required: false,
  disabled: false
};
```

***

## Clone Form Fields

```javascript
// formConfig.js

import { fieldPrototype } from "./fieldPrototype";

export const usernameField = {
  ...fieldPrototype,
  label: "Username",
  placeholder: "Enter username",
  required: true
};

export const emailField = {
  ...fieldPrototype,
  label: "Email",
  type: "email",
  placeholder: "Enter email"
};

export const passwordField = {
  ...fieldPrototype,
  label: "Password",
  type: "password",
  placeholder: "Enter password"
};
```

***

## Dynamic Form Component

```jsx
const fields = [
  usernameField,
  emailField,
  passwordField
];

export default function DynamicForm() {
  return (
    <form>
      {fields.map(field => (
        <div key={field.label}>
          <label>{field.label}</label>

          <input
            type={field.type}
            placeholder={field.placeholder}
            required={field.required}
          />
        </div>
      ))}
    </form>
  );
}
```

***

# 2. Table Configuration Using Prototype Pattern

## Column Prototype

```javascript
export const columnPrototype = {
  sortable: true,
  width: 150,
  align: "left",
  hidden: false
};
```

***

## Clone Columns

```javascript
export const columns = [
  {
    ...columnPrototype,
    key: "name",
    title: "Employee Name"
  },

  {
    ...columnPrototype,
    key: "email",
    title: "Email"
  },

  {
    ...columnPrototype,
    key: "salary",
    title: "Salary",
    align: "right"
  }
];
```

***

## React Table

```jsx
export default function Table({
  data
}) {
  return (
    <table>
      <thead>
        <tr>
          {columns.map(col => (
            <th key={col.key}>
              {col.title}
            </th>
          ))}
        </tr>
      </thead>

      <tbody>
        {data.map(row => (
          <tr key={row.id}>
            {columns.map(col => (
              <td key={col.key}>
                {row[col.key]}
              </td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

***

# 3. Dashboard Widgets Using Prototype Pattern

## Widget Prototype

```javascript
export const widgetPrototype = {
  width: "300px",
  height: "200px",
  refreshInterval: 30000,
  showHeader: true
};
```

***

## Clone Widgets

```javascript
const salesWidget = {
  ...widgetPrototype,
  title: "Sales"
};

const revenueWidget = {
  ...widgetPrototype,
  title: "Revenue"
};

const usersWidget = {
  ...widgetPrototype,
  title: "Users"
};
```

***

## Dashboard

```jsx
const widgets = [
  salesWidget,
  revenueWidget,
  usersWidget
];

export default function Dashboard() {
  return (
    <div>
      {widgets.map(widget => (
        <div
          key={widget.title}
          style={{
            width: widget.width,
            height: widget.height
          }}
        >
          <h3>
            {widget.title}
          </h3>
        </div>
      ))}
    </div>
  );
}
```

***

# 4. Theme Objects Using Prototype Pattern

## Theme Prototype

```javascript
const themePrototype = {
  fontSize: "16px",
  borderRadius: "8px",
  spacing: "12px"
};
```

***

## Dark Theme

```javascript
export const darkTheme = {
  ...themePrototype,
  background: "#121212",
  color: "#ffffff"
};
```

***

## Light Theme

```javascript
export const lightTheme = {
  ...themePrototype,
  background: "#ffffff",
  color: "#000000"
};
```

***

## Theme Context

```jsx
import {
  createContext,
  useContext
} from "react";

const ThemeContext =
  createContext(lightTheme);

export const useTheme =
  () =>
    useContext(
      ThemeContext
    );
```

***

## Usage

```jsx
function Card() {
  const theme =
    useTheme();

  return (
    <div
      style={{
        background:
          theme.background,

        color:
          theme.color
      }}
    >
      Prototype Theme
    </div>
  );
}
```

***

# 5. Configuration Templates Using Prototype Pattern

## API Configuration Prototype

```javascript
const apiConfigPrototype = {
  timeout: 5000,
  retry: 3,
  cache: true
};
```

***

## User API

```javascript
const userApi = {
  ...apiConfigPrototype,
  endpoint: "/users"
};
```

***

## Product API

```javascript
const productApi = {
  ...apiConfigPrototype,
  endpoint: "/products"
};
```

***

## Service

```javascript
function callApi(config) {
  console.log(
    config.endpoint
  );
}

callApi(userApi);
```

***

# Advanced Prototype Factory

```javascript
function createField(
  overrides = {}
) {
  return {
    ...fieldPrototype,
    ...overrides
  };
}
```

Usage:

```javascript
const phoneField =
  createField({
    label: "Phone",
    placeholder:
      "Enter phone",
    required: true
  });
```

***

# Interview Questions

### Q1: What is Prototype Pattern?

Creates new objects by cloning an existing object rather than creating everything from scratch.

***

### Q2: What JavaScript feature supports Prototype Pattern?

```javascript
Object.create()
```

and

```javascript
prototype chain
```

***

### Q3: Difference Between Prototype and Factory?

**Factory**

```javascript
createUser()
```

Creates new object logic every time.

**Prototype**

```javascript
Object.create()
```

Clones reusable object.

***

### Q4: Real React Use Cases?

```text
✅ Dynamic Forms
✅ Table Configurations
✅ Dashboard Widgets
✅ Theme Objects
✅ Configuration Templates
✅ API Configurations
✅ Design Systems
✅ Column Definitions
```

***

# Senior React Interview Answer

> The Prototype Pattern creates objects by cloning a reusable prototype rather than instantiating them from scratch. In React, it's particularly useful for dynamic forms, table column definitions, dashboard widgets, theme configurations, and reusable templates. It promotes consistency, reduces duplication, centralises default behaviour, and makes large enterprise applications easier to maintain and extend.


# 1. Dynamic Form with Validation (Prototype Pattern)

Instead of creating fields repeatedly, create a base field prototype.

## Field Prototype

```javascript
// fieldPrototype.js

export const fieldPrototype = {
  type: "text",
  required: false,
  placeholder: "",
  validation: value => true,
  errorMessage: ""
};
```

***

## Extend Prototype

```javascript
const usernameField = {
  ...fieldPrototype,

  name: "username",

  label: "Username",

  required: true,

  validation: value =>
    value.length >= 3,

  errorMessage:
    "Minimum 3 characters required"
};

const emailField = {
  ...fieldPrototype,

  name: "email",

  type: "email",

  label: "Email",

  validation: value =>
    /\S+@\S+\.\S+/.test(value),

  errorMessage:
    "Invalid email"
};
```

***

## Dynamic Form Component

```jsx
import { useState } from "react";

const fields = [
  usernameField,
  emailField
];

export default function DynamicForm() {

  const [values, setValues] =
    useState({});

  const [errors, setErrors] =
    useState({});

  const handleChange =
    (name, value) => {

      setValues(prev => ({
        ...prev,
        [name]: value
      }));
    };

  const validate = () => {

    const newErrors = {};

    fields.forEach(field => {

      const value =
        values[field.name] || "";

      if (
        !field.validation(value)
      ) {
        newErrors[field.name] =
          field.errorMessage;
      }
    });

    setErrors(newErrors);

    return (
      Object.keys(newErrors)
        .length === 0
    );
  };

  const handleSubmit =
    e => {

      e.preventDefault();

      if (validate()) {
        console.log(values);
      }
    };

  return (
    <form
      onSubmit={
        handleSubmit
      }
    >
      {fields.map(field => (
        <div key={field.name}>
          <label>
            {field.label}
          </label>

          <input
            type={field.type}
            placeholder={
              field.placeholder
            }
            onChange={e =>
              handleChange(
                field.name,
                e.target.value
              )
            }
          />

          {errors[field.name] && (
            <p style={{
              color:"red"
            }}>
              {
                errors[field.name]
              }
            </p>
          )}
        </div>
      ))}

      <button
        type="submit"
      >
        Submit
      </button>
    </form>
  );
}
```

***

# 2. Themed Dashboard Widget Component

## Widget Prototype

```javascript
export const widgetPrototype = {
  width: "300px",
  height: "200px",
  showHeader: true,
  borderRadius: "12px"
};
```

***

## Themes

```javascript
export const lightTheme = {
  background: "#fff",
  color: "#000"
};

export const darkTheme = {
  background: "#1f1f1f",
  color: "#fff"
};
```

***

## Widget Configuration

```javascript
const salesWidget = {
  ...widgetPrototype,

  title: "Sales",

  value: "$100,000"
};

const usersWidget = {
  ...widgetPrototype,

  title: "Users",

  value: "1,250"
};
```

***

## Widget Component

```jsx
function Widget({
  config,
  theme
}) {

  return (
    <div
      style={{
        width:
          config.width,

        height:
          config.height,

        background:
          theme.background,

        color:
          theme.color,

        borderRadius:
          config.borderRadius,

        padding: "16px"
      }}
    >

      {config.showHeader && (
        <h3>
          {config.title}
        </h3>
      )}

      <h1>
        {config.value}
      </h1>

    </div>
  );
}
```

***

## Dashboard

```jsx
function Dashboard() {

  return (
    <div
      style={{
        display: "flex",
        gap: "20px"
      }}
    >
      <Widget
        config={salesWidget}
        theme={darkTheme}
      />

      <Widget
        config={usersWidget}
        theme={lightTheme}
      />
    </div>
  );
}
```

***

# 3. Extending Prototypes for New Configurations

One of the biggest advantages of Prototype Pattern is creating new configurations by extending existing ones.

***

## Base Table Column Prototype

```javascript
const columnPrototype = {
  sortable: true,
  filterable: true,
  align: "left"
};
```

***

## Extend for Employee Table

```javascript
const nameColumn = {
  ...columnPrototype,

  key: "name",

  title: "Employee Name"
};

const salaryColumn = {
  ...columnPrototype,

  key: "salary",

  title: "Salary",

  align: "right"
};
```

***

## Extend Again for Admin Table

```javascript
const adminColumn = {
  ...nameColumn,

  permission:
    "ADMIN_ONLY"
};
```

***

# Prototype Factory (Recommended)

Instead of manually spreading every time:

```javascript
function createField(
  overrides = {}
) {
  return {
    ...fieldPrototype,
    ...overrides
  };
}
```

Usage:

```javascript
const phoneField =
  createField({
    label: "Phone",
    required: true,
    validation: value =>
      value.length === 10,
    errorMessage:
      "Invalid phone number"
  });
```

***

# Real Enterprise Examples

### Dynamic Form System

```text
Field Prototype
 ↓
Username
Email
Phone
Password
```

### Design System

```text
Button Prototype
 ↓
Primary Button
Danger Button
Secondary Button
```

### Dashboard Widgets

```text
Widget Prototype
 ↓
Sales Widget
Revenue Widget
Customer Widget
```

### Table System

```text
Column Prototype
 ↓
Name Column
Salary Column
Status Column
```

### API Configurations

```text
Request Prototype
 ↓
User API
Product API
Order API
```

***

# Senior React Interview Answer

> The Prototype Pattern is highly useful in React when many UI configurations share common defaults. A prototype acts as a reusable template, while individual configurations extend it with custom behaviour. Common use cases include dynamic forms, dashboard widgets, theme configurations, design systems, and table column definitions. By centralising defaults and cloning configurations, the Prototype Pattern reduces duplication, improves consistency, and simplifies maintenance in large-scale React applications.
