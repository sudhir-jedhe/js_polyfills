### Explanation of Iterators and Generators in JavaScript

In JavaScript, **iterators** and **generators** are powerful features that allow you to work with sequences of data more flexibly, enabling you to control the flow of iteration. Let's break down their concepts, examples, and differences.

### 1. **What is an Iterator?**
An **iterator** is an object that allows you to traverse a collection, such as an array, one element at a time. The iterator implements a method called `next()`, which returns an object containing two properties:
- **value**: The current value in the iteration.
- **done**: A boolean flag indicating whether the iteration is complete.

The iterator itself must follow the **Iterator protocol**, which means it should have a `next()` method, which returns `{ value, done }`.

Here's an example of creating a basic iterator function:

```javascript
function createIterator(collection) {
  let i = 0;
  return {
    next() {
      if (i < collection.length) {
        return { value: collection[i++], done: false };
      }

      return { value: null, done: true };
    },
  };
}

const arr = [1, 2, 3];
const iterator = createIterator(arr);

console.log(iterator.next()); // {"value": 1, "done": false }
console.log(iterator.next()); // {"value": 2, "done": false }
console.log(iterator.next()); // {"value": 3, "done": false }
console.log(iterator.next()); // {"value": null, "done": true }
```

#### Key Concepts:
- The `next()` method checks if the iterator has reached the end of the collection. If not, it returns the next value.
- When the iterator has finished iterating, it returns `{ value: null, done: true }`.

---

### 2. **Generator Functions**
A **generator** is a special kind of function that can be paused and resumed, providing a convenient way to create iterators. Generator functions are defined using `function*`, and inside the function, you use the `yield` keyword to return values incrementally.

Example of a generator:

```javascript
function* Gen() {
  yield* ["a", "b", "c"];
}

const g = Gen();

console.log(g.next()); // { value: "a", done: false }
console.log(g.next()); // { value: "b", done: false }
console.log(g.next()); // { value: "c", done: false }
console.log(g.next()); // { value: undefined, done: true }
```

#### Key Concepts:
- **`yield`**: Pauses the generator function and returns a value. The state of the generator is saved, and it can be resumed when `next()` is called again.
- The **done** property will be `true` when there are no more values to yield.

---

### 3. **Custom Iterable Objects**
In JavaScript, you can also create custom iterable objects by defining the `Symbol.iterator` method. This allows you to make your objects work with `for...of` loops.

```javascript
const g = {};

g[Symbol.iterator] = function* () {
  yield 1;
  yield 2;
  yield 3;
};

console.log([...g]); // [1, 2, 3]
```

Here, the object `g` becomes iterable because we implement its `[Symbol.iterator]` method, which is a generator function.

---

### 4. **Iterator with `for...of` Loop**

You can use the `for...of` loop with any iterable object (like arrays, maps, sets, or custom iterables). This loop automatically calls the `next()` method until `done` is `true`.

```javascript
function* createIterator() {
  yield 1;
  yield 2;
}

const iterator = createIterator();

for (const item of iterator) {
  console.log(item); // 1, 2
}

// This will not run again, because the iterator is exhausted
for (const item of iterator) {
  console.log(item);
}
```

---

### 5. **Custom Iterable with Generator**
You can also define a custom iterable object using `Symbol.iterator` with a generator function:

```javascript
const customIterable = {
  *[Symbol.iterator]() {
    yield 1;
    yield 2;
    yield 3;
  },
};

for (const value of customIterable) {
  console.log(value); // 1, 2, 3
}

for (const value of customIterable) {
  console.log(value); // 1, 2, 3 (iteration can be repeated)
}
```

In this case, the custom iterable object has its own `Symbol.iterator` method, which makes it iterable, and we can use the `for...of` loop to iterate through its values.

---

### 6. **Round Robin Iterator**
A **Round Robin iterator** is an interesting type of iterator where after iterating through all the items, it starts over from the beginning. The example below demonstrates how to implement a Round Robin iterator with a reset feature.

```javascript
function* RoundRobin(collection) {
  let current = 0;
  while (true) {
    const reset = yield collection[current++ % collection.length];
    if (reset) {
      current = 0; // Reset to start from the beginning
    }
  }
}

const rr = RoundRobin([1, 2, 3, 4]);
console.log(rr.next()); // { value: 1, done: false }
console.log(rr.next()); // { value: 2, done: false }
console.log(rr.next()); // { value: 3, done: false }
console.log(rr.next(true)); // { value: 1, done: false } // Resets the counter
console.log(rr.next()); // { value: 2, done: false }
console.log(rr.next()); // { value: 3, done: false }
console.log(rr.next()); // { value: 4, done: false }
console.log(rr.next()); // { value: 1, done: false } // Starts again
```

#### Key Concepts:
- **Round Robin**: After reaching the end of the collection, the iteration starts over again.
- **Reset Feature**: The iterator can be reset manually by passing `true` to the `next()` method, which causes the index to reset to the beginning.

---

### 7. **Using Iterables in Arrays, Sets, and Maps**
Arrays, Sets, and Maps in JavaScript are **built-in iterable** objects, so you can use them directly in a `for...of` loop or with other iterable methods:

```javascript
const arr = [1, 2, 3];

for (const item of arr) {
  console.log(item); // 1, 2, 3
}

const set = new Set([1, 2, 3]);
for (const item of set) {
  console.log(item); // 1, 2, 3
}

const map = new Map([["a", 1], ["b", 2]]);
for (const [key, value] of map) {
  console.log(key, value); // a 1, b 2
}
```

These collections come with an iterator by default, which allows you to use the `for...of` loop directly.

---

### Conclusion:
- **Iterators** allow you to traverse collections and return elements sequentially.
- **Generators** provide a simpler way to create iterators with the `yield` keyword.
- **Custom Iterables** let you define your own objects that can be iterated using the `Symbol.iterator` method.
- Iteration over **arrays**, **sets**, and **maps** is built-in, but **objects** are not inherently iterable.

These concepts can be used in various scenarios such as when dealing with asynchronous operations, implementing algorithms, or working with complex data structures.


# Iterator Pattern in JavaScript & React

The **Iterator Pattern** is a behavioural design pattern that provides a way to access elements of a collection sequentially **without exposing its internal structure**.

***

# Real World Example

Imagine:

```text
Employee List
Product List
User List
Tree Structure
Folder Structure
```

Instead of:

```javascript
employees[0]
employees[1]
employees[2]
```

We use an iterator to traverse the collection.

***

# Built-in JavaScript Iterator

Arrays already implement the iterator pattern.

```javascript
const arr = [10, 20, 30];

const iterator =
  arr[Symbol.iterator]();

console.log(iterator.next());
console.log(iterator.next());
console.log(iterator.next());
console.log(iterator.next());
```

Output:

```javascript
{ value: 10, done: false }
{ value: 20, done: false }
{ value: 30, done: false }
{ value: undefined, done: true }
```

***

# What is `next()`?

Every iterator returns:

```javascript
{
  value,
  done
}
```

Example:

```javascript
{
  value: 10,
  done: false
}
```

Meaning:

```text
Current Item = 10
More Items Available
```

***

# Custom Iterator Pattern

## Employee Collection

```javascript
class Employees {
  constructor() {
    this.data = [
      "Sudhir",
      "John",
      "Sam"
    ];
  }

  [Symbol.iterator]() {

    let index = 0;

    const employees =
      this.data;

    return {
      next() {

        if (
          index <
          employees.length
        ) {
          return {
            value:
              employees[index++],
            done: false
          };
        }

        return {
          value: undefined,
          done: true
        };
      }
    };
  }
}
```

***

# Usage

```javascript
const employees =
  new Employees();

for (const employee of employees) {
  console.log(employee);
}
```

Output:

```javascript
Sudhir
John
Sam
```

***

# Generator = Iterator Shortcut

In modern JavaScript, generators make iterators easy.

## Without Generator

```javascript
next() {
 ...
}
```

Lots of code.

***

## With Generator

```javascript
function* users() {
  yield "Sudhir";
  yield "John";
  yield "Sam";
}
```

Usage:

```javascript
const iterator =
  users();

console.log(
  iterator.next()
);
```

Output:

```javascript
{
  value: "Sudhir",
  done: false
}
```

***

# React Example

## Lazy Rendering Large Data

```jsx
function* employeeGenerator(
  employees
) {
  for (const employee of employees) {
    yield employee;
  }
}
```

***

```jsx
const employees = [
  "Sudhir",
  "John",
  "Sam"
];

const iterator =
  employeeGenerator(
    employees
  );

console.log(
  iterator.next().value
);
```

Output:

```javascript
Sudhir
```

***

# Infinite Iterator

Very common interview question.

```javascript
function* counter() {

  let i = 1;

  while (true) {
    yield i++;
  }
}
```

Usage:

```javascript
const gen =
  counter();

console.log(
  gen.next().value
);

console.log(
  gen.next().value
);

console.log(
  gen.next().value
);
```

Output:

```javascript
1
2
3
```

***

# Tree Iterator Example

```javascript
const tree = {
  value: "A",
  children: [
    {
      value: "B",
      children: []
    },
    {
      value: "C",
      children: []
    }
  ]
};
```

Generator:

```javascript
function* traverse(node) {

  yield node.value;

  for (
    const child of node.children
  ) {
    yield* traverse(child);
  }
}
```

Usage:

```javascript
for (
  const value of traverse(tree)
) {
  console.log(value);
}
```

Output:

```javascript
A
B
C
```

***

# Iterator Pattern in React Tables

Suppose:

```javascript
const employees = [
  { id: 1, name: "Sudhir" },
  { id: 2, name: "John" }
];
```

Instead of:

```javascript
employees[0]
employees[1]
```

React uses iteration:

```jsx
employees.map(employee => (
  <tr key={employee.id}>
    <td>{employee.name}</td>
  </tr>
));
```

Conceptually:

```text
Collection
 ↓
Iterator
 ↓
Render Items
```

***

# Benefits of Iterator Pattern

### Encapsulation

Hide collection structure.

```javascript
iterator.next()
```

instead of:

```javascript
array[index]
```

***

### Works With Any Data Structure

```text
Array
Tree
Linked List
Graph
Queue
Stack
```

***

### Lazy Evaluation

Useful for:

```text
Large Lists
Pagination
Streaming Data
Virtual Scrolling
```

***

# Interview Questions

## Q1: What is Iterator Pattern?

**Answer:**

Provides sequential access to collection elements without exposing internal details.

***

## Q2: What method must an iterator implement?

```javascript
next()
```

***

## Q3: What does `next()` return?

```javascript
{
  value,
  done
}
```

***

## Q4: What is `Symbol.iterator`?

Makes an object iterable by:

```javascript
for...of
spread (...)
Array.from()
```

***

## Q5: Difference Between Iterator and Generator?

### Iterator

Manual:

```javascript
next()
```

implementation.

### Generator

Automatic iterator creation using:

```javascript
function* ()
```

***

## Q6: Real React Use Cases?

✅ Table rendering

✅ Infinite scrolling

✅ Lazy loading

✅ Virtualised lists

✅ Tree views

✅ Streaming APIs

***

# Senior Frontend Interview Answer

> The Iterator Pattern provides a standard way to traverse a collection without exposing how the collection is implemented. In JavaScript, iterators implement a `next()` method returning `{ value, done }`, and iterable objects expose `Symbol.iterator`. Modern JavaScript generators (`function*`) simplify iterator creation and are commonly used for lazy evaluation, pagination, infinite scrolling, tree traversal, and large dataset processing in React applications.



# React Example Using Iterator for Lazy Loading

A common use case for iterators/generators in React is **lazy loading large datasets** instead of rendering thousands of records at once.

***

## Step 1: Generator (Lazy Iterator)

```javascript
function* employeeGenerator(employees) {
  for (const employee of employees) {
    yield employee;
  }
}
```

***

## Step 2: React Component

```jsx
import React, { useRef, useState } from "react";

const allEmployees = Array.from(
  { length: 1000 },
  (_, i) => ({
    id: i + 1,
    name: `Employee ${i + 1}`
  })
);

function* employeeGenerator(data) {
  for (const item of data) {
    yield item;
  }
}

export default function EmployeeList() {
  const generatorRef = useRef(
    employeeGenerator(allEmployees)
  );

  const [employees, setEmployees] =
    useState([]);

  const loadMore = () => {
    const nextItems = [];

    for (let i = 0; i < 20; i++) {
      const result =
        generatorRef.current.next();

      if (result.done) break;

      nextItems.push(result.value);
    }

    setEmployees(prev => [
      ...prev,
      ...nextItems
    ]);
  };

  return (
    <div>
      <button onClick={loadMore}>
        Load More
      </button>

      {employees.map(emp => (
        <div key={emp.id}>
          {emp.name}
        </div>
      ))}
    </div>
  );
}
```

### Benefits

```text
✅ Doesn't render all 1000 items
✅ Lower memory usage
✅ Better performance
✅ Similar to infinite scroll
```

***

# Iterator vs Generator

## Iterator

You manually implement:

```javascript
class Counter {
  constructor() {
    this.count = 1;
  }

  next() {
    return {
      value: this.count++,
      done: false
    };
  }
}

const counter =
  new Counter();

console.log(counter.next());
```

### Output

```javascript
{ value: 1, done: false }
```

***

## Generator

JavaScript creates the iterator automatically.

```javascript
function* counter() {
  let i = 1;

  while (true) {
    yield i++;
  }
}

const gen = counter();

console.log(gen.next());
```

### Output

```javascript
{ value: 1, done: false }
```

***

# Comparison

| Feature                 | Iterator | Generator |
| ----------------------- | -------- | --------- |
| `next()` implementation | Manual   | Automatic |
| Code Size               | More     | Less      |
| Readability             | Lower    | Higher    |
| Lazy Evaluation         | ✅        | ✅         |
| React Usage             | Rare     | Common    |
| Infinite Sequences      | Harder   | Easier    |

***

# React Infinite Scroll Example

```javascript
function* pageGenerator(data) {
  const pageSize = 10;

  for (
    let i = 0;
    i < data.length;
    i += pageSize
  ) {
    yield data.slice(
      i,
      i + pageSize
    );
  }
}
```

Usage:

```javascript
const pages =
  pageGenerator(users);

const firstPage =
  pages.next().value;

const secondPage =
  pages.next().value;
```

Useful for:

```text
Pagination
Infinite Scrolling
Large Tables
Virtual Lists
```

***

# Iterator Pattern Interview Questions

## Basic

### Q1: What is the Iterator Pattern?

**Answer:**

A behavioural design pattern that provides sequential access to items in a collection without exposing its internal structure.

***

### Q2: What method must every iterator implement?

```javascript
next()
```

***

### Q3: What does `next()` return?

```javascript
{
  value,
  done
}
```

***

### Q4: What is `Symbol.iterator`?

It makes an object iterable by:

```javascript
for...of
Array.from()
spread(...)
```

***

## Intermediate

### Q5: Difference between Iterator and Iterable?

**Iterable**

```javascript
Array
String
Map
Set
```

Has:

```javascript
[Symbol.iterator]
```

**Iterator**

Has:

```javascript
next()
```

***

### Q6: Difference between `for...of` and Iterator?

```javascript
for...of
```

uses an iterator internally.

***

### Q7: What is lazy evaluation?

Values are produced only when requested.

```javascript
iterator.next()
```

***

## Advanced

### Q8: Difference between Iterator and Generator?

Iterator:

```javascript
Manual next()
```

Generator:

```javascript
function* ()
```

Automatic iterator creation.

***

### Q9: Why are generators useful in React?

```text
Infinite scrolling
Large list rendering
Pagination
Streaming data
Tree traversal
```

***

### Q10: What happens when generator reaches the end?

```javascript
{
  value: undefined,
  done: true
}
```

***

### Q11: Can generators be paused?

Yes.

```javascript
yield
```

pauses execution until:

```javascript
next()
```

is called.

***

### Q12: Real-world React scenarios?

```text
✅ Infinite Scroll
✅ Lazy Loading
✅ Paginated APIs
✅ Virtual Tables
✅ Tree Components
✅ File Explorers
✅ Streaming Data
```

***

# Senior Frontend Interview Answer

> The Iterator Pattern provides a standard way to traverse collections through a `next()` method that returns `{ value, done }`. In modern JavaScript, generators (`function*`) are preferred because they automatically create iterators and support lazy evaluation. In React applications, generators are particularly useful for infinite scrolling, paginated data loading, virtualised lists, and processing large datasets efficiently without loading everything into memory at once.
# Infinite Iterator (Generator) in React

An infinite iterator generates values forever and only produces the next value when requested.

***

## Basic Infinite Generator

```javascript
function* counterGenerator() {
  let count = 1;

  while (true) {
    yield count++;
  }
}
```

Usage:

```javascript
const counter = counterGenerator();

console.log(counter.next().value);
console.log(counter.next().value);
console.log(counter.next().value);
```

Output:

```javascript
1
2
3
```

***

# React Example: Infinite Number Loader

```jsx
import React, { useRef, useState } from "react";

function* counterGenerator() {
  let count = 1;

  while (true) {
    yield count++;
  }
}

export default function App() {
  const generatorRef =
    useRef(counterGenerator());

  const [numbers, setNumbers] =
    useState([]);

  const loadMore = () => {
    const nextBatch = [];

    for (let i = 0; i < 10; i++) {
      nextBatch.push(
        generatorRef.current.next().value
      );
    }

    setNumbers(prev => [
      ...prev,
      ...nextBatch
    ]);
  };

  return (
    <>
      <button onClick={loadMore}>
        Load More Numbers
      </button>

      {numbers.map(number => (
        <div key={number}>
          {number}
        </div>
      ))}
    </>
  );
}
```

Output after clicking twice:

```text
1 - 10
11 - 20
```

***

# Infinite Scroll Style Generator

```javascript
function* pageGenerator() {
  let page = 1;

  while (true) {
    yield {
      page,
      pageSize: 20
    };

    page++;
  }
}
```

Usage:

```javascript
const pages =
  pageGenerator();

console.log(
  pages.next().value
);
```

Output:

```javascript
{
  page: 1,
  pageSize: 20
}
```

***

# Pause and Resume Generators

One of the biggest advantages of generators is that they can pause execution.

***

## Basic Example

```javascript
function* taskGenerator() {
  console.log("Step 1");

  yield;

  console.log("Step 2");

  yield;

  console.log("Step 3");
}

const task =
  taskGenerator();
```

Nothing runs yet.

***

### Resume Execution

```javascript
task.next();
```

Output:

```text
Step 1
```

Execution pauses at:

```javascript
yield;
```

***

### Continue

```javascript
task.next();
```

Output:

```text
Step 2
```

***

### Continue Again

```javascript
task.next();
```

Output:

```text
Step 3
```

***

# React Example: Pause / Resume Workflow

```jsx
import React, { useRef } from "react";

function* processSteps() {
  console.log("Fetch Users");

  yield;

  console.log("Fetch Projects");

  yield;

  console.log("Fetch Reports");

  yield;

  console.log("Complete");
}

export default function App() {
  const processRef =
    useRef(processSteps());

  const nextStep = () => {
    processRef.current.next();
  };

  return (
    <button onClick={nextStep}>
      Run Next Step
    </button>
  );
}
```

Each click resumes the generator from exactly where it stopped.

***

# Passing Values Back into Generators

Very common senior interview question.

```javascript
function* calculator() {

  const value =
    yield "Enter Number";

  yield value * 2;
}

const gen =
  calculator();

console.log(
  gen.next()
);
```

Output:

```javascript
{
  value: "Enter Number",
  done: false
}
```

***

Resume with data:

```javascript
console.log(
  gen.next(10)
);
```

Output:

```javascript
{
  value: 20,
  done: false
}
```

***

# React Example: Wizard Form

```jsx
function* wizard() {

  yield "Personal Details";

  yield "Address Details";

  yield "Payment Details";

  yield "Confirmation";
}
```

Component:

```jsx
const wizardRef =
  useRef(wizard());

const [step, setStep] =
  useState("");

const next = () => {

  const result =
    wizardRef.current.next();

  if (!result.done) {
    setStep(result.value);
  }
};
```

Perfect for:

```text
Multi-Step Forms
Checkout Flows
Onboarding Wizards
```

***

# Iterator vs Generator in React

| Feature                          | Iterator    | Generator |
| -------------------------------- | ----------- | --------- |
| Must implement `next()` manually | ✅           | ❌         |
| Automatically creates iterator   | ❌           | ✅         |
| Can pause execution              | ❌           | ✅         |
| Can resume execution             | ❌           | ✅         |
| React infinite scroll            | ⚠️ Possible | ✅ Common  |
| Multi-step workflows             | ⚠️ Complex  | ✅ Easy    |
| Lazy loading                     | ✅           | ✅         |

***

# Common Interview Questions

### Q1. What is an infinite iterator?

```javascript
function* counter() {
  let i = 1;

  while (true) {
    yield i++;
  }
}
```

Generates values forever until the consumer stops calling `next()`.

***

### Q2. How do generators pause?

Using:

```javascript
yield;
```

Execution stops at the yield statement.

***

### Q3. How do generators resume?

Using:

```javascript
generator.next();
```

Execution continues from the previous yield point.

***

### Q4. Why use generators in React?

Useful for:

```text
✅ Infinite Scrolling
✅ Lazy Loading
✅ Pagination
✅ Virtual Lists
✅ Wizards
✅ Multi-step Workflows
✅ Streaming Data
✅ Tree Traversal
```

***

# Senior React Interview Answer

> Generators are special functions (`function*`) that implement the Iterator Pattern automatically. They support lazy evaluation by producing values only when `next()` is called. Unlike manual iterators, generators can pause execution using `yield` and resume later from the same point. In React, generators are particularly useful for infinite scrolling, lazy loading large datasets, paginated APIs, multi-step forms, and workflow engines where stateful sequential processing is required.


# Why Pause Generators in React?

The biggest advantage of generators over normal functions is:

```text
Pause → Keep State → Resume Later
```

This makes them useful for UI workflows that happen step-by-step.

***

# 1. Multi-Step Wizard Forms

Instead of:

```javascript
if (step === 1) { ... }
if (step === 2) { ... }
if (step === 3) { ... }
```

Use a generator:

```javascript
function* registrationWizard() {
  yield "Personal Information";
  yield "Address Information";
  yield "Payment Information";
  yield "Confirmation";
}
```

Each button click resumes the generator.

***

# 2. Checkout Flow

```text
Cart
 ↓
Shipping
 ↓
Payment
 ↓
Review
 ↓
Success
```

Generator remembers the current stage.

```javascript
function* checkout() {
  yield "Cart";
  yield "Shipping";
  yield "Payment";
  yield "Review";
}
```

***

# 3. Infinite Scroll

Instead of loading:

```text
10000 Records
```

Load:

```text
20 Records
 ↓
Pause
 ↓
Scroll
 ↓
Next 20
```

Generator keeps track of position.

***

# 4. API Workflow Engine

Example:

```text
Fetch User
 ↓
Fetch Projects
 ↓
Fetch Reports
 ↓
Generate Dashboard
```

```javascript
function* dashboardWorkflow() {
  yield "users";
  yield "projects";
  yield "reports";
}
```

***

# 5. Guided User Onboarding

```text
Welcome
 ↓
Profile Setup
 ↓
Preferences
 ↓
Finish
```

Generator stores progress naturally.

***

# Generator Input + Output Example

This is a favourite senior interview question.

***

## Vanilla JavaScript

```javascript
function* calculator() {

  const number =
    yield "Enter a number";

  const result =
    number * 2;

  yield result;
}

const gen =
  calculator();

console.log(gen.next());
```

Output:

```javascript
{
  value: "Enter a number",
  done: false
}
```

***

### Pass Input

```javascript
console.log(
  gen.next(10)
);
```

Output:

```javascript
{
  value: 20,
  done: false
}
```

Flow:

```text
Generator asks
     ↓
Input = 10
     ↓
Processes
     ↓
Output = 20
```

***

# React Example: Generator with Input and Output

Imagine a simple salary calculator.

***

## Generator

```javascript
function* salaryCalculator() {

  const salary =
    yield "Enter Salary";

  const bonus =
    salary * 0.10;

  yield {
    salary,
    bonus,
    total:
      salary + bonus
  };
}
```

***

## React Component

```jsx
import React, {
  useRef,
  useState
} from "react";

function* salaryCalculator() {

  const salary =
    yield "Enter Salary";

  const bonus =
    salary * 0.10;

  yield {
    salary,
    bonus,
    total:
      salary + bonus
  };
}

export default function App() {

  const generatorRef =
    useRef(
      salaryCalculator()
    );

  const [salary,
         setSalary] =
    useState("");

  const [result,
         setResult] =
    useState(null);

  const start = () => {
    generatorRef.current.next();
  };

  const calculate =
    () => {

      const output =
        generatorRef.current.next(
          Number(salary)
        );

      setResult(
        output.value
      );
    };

  return (
    <div>

      <button
        onClick={start}
      >
        Start
      </button>

      <input
        value={salary}
        onChange={e =>
          setSalary(
            e.target.value
          )
        }
      />

      <button
        onClick={calculate}
      >
        Calculate
      </button>

      {result && (
        <div>
          <p>
            Salary:
            {result.salary}
          </p>

          <p>
            Bonus:
            {result.bonus}
          </p>

          <p>
            Total:
            {result.total}
          </p>
        </div>
      )}

    </div>
  );
}
```

***

# More Advanced Example: Async Workflow

```javascript
function* employeeWorkflow() {

  const employeeId =
    yield "Enter Employee Id";

  const employee =
    yield fetchEmployee(
      employeeId
    );

  const projects =
    yield fetchProjects(
      employee.id
    );

  return {
    employee,
    projects
  };
}
```

Useful for:

```text
Workflow Engines
Saga Patterns
State Machines
Complex Forms
```

***

# Interview Questions

### Q1: Why would you pause a generator?

**Answer:**

To preserve execution state and continue later without restarting the function.

***

### Q2: How does a generator receive input?

```javascript
generator.next(value)
```

Example:

```javascript
const input =
  yield "Enter Number";
```

***

### Q3: What is returned from `next()`?

```javascript
{
  value,
  done
}
```

***

### Q4: What happens at `yield`?

```text
Execution pauses
State preserved
Control returned
```

***

### Q5: Real React Use Cases?

✅ Multi-step forms

✅ Infinite scrolling

✅ Paginated APIs

✅ Onboarding flows

✅ Workflow engines

✅ Async state machines

✅ Lazy loading large datasets

***

# Senior React Interview Answer

> Generators are useful in React when an operation needs to be executed incrementally while preserving state between steps. The `yield` keyword pauses execution and allows the component to resume later using `next()`. This makes generators ideal for multi-step wizards, onboarding flows, paginated data loaders, infinite scrolling, workflow engines, and state-machine-like behaviour. Generators can also receive inputs through `next(value)` and produce outputs through `yield`, enabling controlled two-way communication between the React component and the generator.



# Generator with Async Input in React

One powerful use case is handling a workflow where a generator pauses and waits for async API data before continuing.

***

## Generator Workflow

```javascript
function* employeeWorkflow() {
  // Step 1
  const employeeId =
    yield "Enter Employee Id";

  // Step 2
  const employee =
    yield fetchEmployee(employeeId);

  // Step 3
  const projects =
    yield fetchProjects(employee.id);

  return {
    employee,
    projects
  };
}
```

Flow:

```text
yield "Enter Employee Id"
        ↓
next(101)
        ↓
yield fetchEmployee(101)
        ↓
API Response
        ↓
next(employeeObject)
        ↓
yield fetchProjects(employee.id)
        ↓
API Response
        ↓
next(projects)
        ↓
Return Final Result
```

***

# React Example

## Fake APIs

```javascript
function fetchEmployee(id) {
  return new Promise(resolve => {
    setTimeout(() => {
      resolve({
        id,
        name: "Sudhir"
      });
    }, 1000);
  });
}

function fetchProjects(id) {
  return new Promise(resolve => {
    setTimeout(() => {
      resolve([
        "Project A",
        "Project B"
      ]);
    }, 1000);
  });
}
```

***

## Generator

```javascript
function* employeeWorkflow() {
  const employeeId =
    yield "Enter Employee ID";

  const employee =
    yield fetchEmployee(employeeId);

  const projects =
    yield fetchProjects(employee.id);

  return {
    employee,
    projects
  };
}
```

***

## React Component

```jsx
import React,
{
  useRef,
  useState
} from "react";

export default function App() {
  const workflowRef =
    useRef(employeeWorkflow());

  const [result, setResult] =
    useState(null);

  const runWorkflow =
    async () => {

      workflowRef.current.next();

      const employeePromise =
        workflowRef.current.next(101).value;

      const employee =
        await employeePromise;

      const projectPromise =
        workflowRef.current.next(employee).value;

      const projects =
        await projectPromise;

      const finalResult =
        workflowRef.current.next(projects);

      setResult(
        finalResult.value
      );
    };

  return (
    <div>

      <button
        onClick={runWorkflow}
      >
        Load Employee
      </button>

      <pre>
        {
          result &&
          JSON.stringify(
            result,
            null,
            2
          )
        }
      </pre>

    </div>
  );
}
```

Output:

```javascript
{
  employee: {
    id: 101,
    name: "Sudhir"
  },
  projects: [
    "Project A",
    "Project B"
  ]
}
```

***

# Managing Generator State with React Hooks

A common mistake is:

```jsx
const generator =
  employeeWorkflow();
```

inside a component.

Every re-render creates a new generator.

❌ State is lost.

***

## Correct Approach: useRef

```jsx
const generatorRef =
  useRef(
    employeeWorkflow()
  );
```

Benefits:

```text
Re-render Safe
Generator State Preserved
No Re-Creation
```

***

# Custom Hook Pattern

A senior-level approach.

```jsx
import { useRef } from "react";

function useGenerator(generatorFn) {
  const generatorRef =
    useRef(generatorFn());

  const next = value =>
    generatorRef.current.next(value);

  const reset = () => {
    generatorRef.current =
      generatorFn();
  };

  return {
    next,
    reset
  };
}
```

***

## Usage

```jsx
function* wizard() {
  yield "Profile";

  yield "Address";

  yield "Payment";

  return "Done";
}

function App() {

  const {
    next,
    reset
  } = useGenerator(wizard);

  const [step, setStep] =
    useState("");

  const handleNext =
    () => {
      const result =
        next();

      setStep(result.value);
    };

  return (
    <>
      <button
        onClick={
          handleNext
        }
      >
        Next
      </button>

      <button
        onClick={reset}
      >
        Reset
      </button>

      <h2>{step}</h2>
    </>
  );
}
```

***

# Real React Use Cases

## Multi-Step Form

```text
Profile
 ↓
Address
 ↓
Skills
 ↓
Review
```

Generator remembers current step.

***

## Infinite Scrolling

```text
Load 20
 ↓
Pause
 ↓
Scroll
 ↓
Load Next 20
```

***

## Workflow Engine

```text
Fetch User
 ↓
Fetch Projects
 ↓
Generate Dashboard
 ↓
Render
```

***

## Onboarding Flows

```text
Welcome
 ↓
Setup Profile
 ↓
Choose Preferences
 ↓
Done
```

***

# Interview Questions

### Q1. Why use `useRef` with generators?

**Answer:**

Because generators maintain internal execution state. `useRef` preserves the generator instance across React re-renders.

***

### Q2. What happens if you create the generator inside render?

```jsx
const gen = workflow();
```

A new generator is created on every render and execution state is lost.

***

### Q3. How do generators receive async results?

```javascript
generator.next(apiResponse);
```

The yielded expression receives the response.

***

### Q4. Why are generators useful for workflows?

Because they naturally model:

```text
Pause
Wait
Resume
Continue
```

without lots of nested callbacks or state variables.

***

# Senior React Interview Answer

> Generators are useful in React when a workflow must pause and resume while preserving execution state. By storing the generator in `useRef`, React re-renders do not recreate it. Async results can be passed back into a generator using `next(value)`, enabling complex workflows such as multi-step forms, onboarding flows, paginated loading, and API orchestration. Generators effectively act as lightweight state machines where `yield` pauses execution and `next()` resumes it with new input.
# How to Securely Handle Premium Content in React

For a Medium-like application, the **golden rule** is:

> **Never send premium content to unauthorised users.**

React is only responsible for displaying content. **Security must be enforced on the backend.**

***

# ❌ Bad Approach

Backend sends the full article:

```json
{
  "title": "Advanced React Patterns",
  "content": "Full premium article..."
}
```

Frontend hides part of it:

```jsx
<div className="blurred">
  {content}
</div>
```

Problems:

```text
User can inspect Network tab
User can view React state
User can view page source
User can remove CSS
```

The content is already exposed.

***

# ✅ Correct Architecture

```text
React App
    ↓
Authentication
    ↓
Backend Authorization Check
    ↓
Premium User?
    ↓
YES → Full Content
NO  → Preview Only
```

***

# Backend API Design

## Guest User Response

```json
{
  "id": 1,
  "title": "Advanced React Patterns",
  "preview": "Builder pattern helps create complex objects...",
  "locked": true
}
```

***

## Premium User Response

```json
{
  "id": 1,
  "title": "Advanced React Patterns",
  "content": "Full premium article...",
  "locked": false
}
```

***

# React Component

```jsx
function Article({ article }) {
  return (
    <div>
      <h1>{article.title}</h1>

      {article.locked ? (
        <>
          <p>{article.preview}</p>

          <Paywall />
        </>
      ) : (
        <p>{article.content}</p>
      )}
    </div>
  );
}
```

***

# Member-Only Overlay

```jsx
function Paywall() {
  return (
    <div className="paywall">
      <h3>
        🔒 Member-only story
      </h3>

      <button>
        Subscribe
      </button>
    </div>
  );
}
```

***

# Secure Route Protection

## Frontend

```jsx
function PremiumRoute({
  children
}) {
  const user =
    useCurrentUser();

  if (!user?.isPremium) {
    return <UpgradePage />;
  }

  return children;
}
```

Usage:

```jsx
<PremiumRoute>
  <PremiumDashboard />
</PremiumRoute>
```

***

## Backend Validation

Never trust the frontend.

```javascript
app.get(
  "/api/article/:id",
  async (req, res) => {

    const user =
      req.user;

    const article =
      await getArticle();

    if (
      article.premium &&
      !user.isPremium
    ) {
      return res.json({
        title: article.title,
        preview:
          article.preview,
        locked: true
      });
    }

    return res.json({
      title: article.title,
      content:
        article.content
    });
  }
);
```

***

# Use Signed JWT Tokens

```text
Login
↓
JWT Token
↓
Backend verifies subscription
↓
Returns content
```

Example:

```javascript
Authorization:
Bearer eyJ...
```

***

# Prevent API Abuse

## Rate Limiting

```javascript
100 Requests / Minute
```

using:

```javascript
express-rate-limit
```

or API Gateway limits.

***

## Validate User on Every Request

Never trust:

```javascript
localStorage.isPremium
```

Bad:

```javascript
if (localStorage.isPremium)
```

Users can edit it.

Always validate on server.

***

# Secure Media (Videos / PDFs)

For premium assets:

❌

```text
/public/video.mp4
```

Anyone can download it.

***

✅

```text
Protected Storage
↓
Generate signed URL
↓
Temporary access
```

Common options:

```text
AWS S3 Signed URLs
Azure SAS Tokens
CloudFront Signed URLs
```

***

# React Query Example

```jsx
const { data } =
  useQuery({
    queryKey: [
      "article",
      articleId
    ],
    queryFn: () =>
      fetchArticle(
        articleId
      )
  });
```

Backend decides:

```text
Preview
or
Full Content
```

React never decides security.

***

# Common Security Mistakes

### ❌ CSS Hiding

```css
display:none;
```

Not security.

***

### ❌ Truncation Only

```javascript
content.slice(0, 300);
```

Full content is still available in API response.

***

### ❌ Frontend Premium Checks Only

```javascript
if(user.isPremium)
```

Can be bypassed.

***

### ❌ Storing Premium Flags in LocalStorage

```javascript
localStorage.setItem(
  "premium",
  true
);
```

Users can modify it.

***

# Senior React Interview Answer

> Premium content should be protected on the server, not in React. The backend must verify authentication and subscription status before returning premium data. Unauthorised users should receive only a preview, while authorised users receive the full content. Frontend techniques such as truncation, blur overlays, and paywalls improve user experience but do not provide security. For premium documents and media, use protected storage and signed URLs rather than exposing direct file URLs.
