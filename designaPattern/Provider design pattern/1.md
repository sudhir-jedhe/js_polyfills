You're absolutely right about **props drilling** in React — it can become cumbersome, especially in deeply nested components, leading to code repetition, maintenance overhead, and potential bugs. Luckily, React offers several solutions for managing this problem, such as **Context API** and **state management libraries** like Redux. These solutions allow you to pass data without the need to manually pass props through every layer of the component tree.

Let’s break down the examples you've provided and discuss how to manage and avoid **props drilling** using **React's Context API**.

### **Props Drilling** Problem

In your initial example, the data (in this case, the `topic` object) is passed down through multiple layers of components, from `Parent` to `Child` to `Header` and `Main`:

```jsx
const Parent = () => {
  const topic = { title: "Tech", articles: [{ title: "web3", content: "" }] };
  return <Child topic={topic} />;
};

const Child = ({ topic }) => {
  return (
    <div>
      <Header title={topic.title} />
      <Main articles={topic.articles} />
    </div>
  );
};

const Header = ({ title }) => {
  return (
    <header>
      <h1>{title}</h1>
    </header>
  );
};

const Main = ({ articles }) => {
  return (
    <section className="articles">
      {articles.map((e) => (
        <article key={e.id}>
          <h1>{e.title}</h1>
          <p>{e.content}</p>
        </article>
      ))}
    </section>
  );
};
```

- **Problem**: Each component needs to explicitly pass props down to its child components.
- **Solution**: Use **React Context** to provide a global way of accessing the data, without needing to pass props manually through each component.

### **Using React Context API to Avoid Props Drilling**

React’s **Context API** allows you to share values between components without having to manually pass props through every level of the component tree.

#### **Context API Example (Feature Flags)**

Let’s look at an example using the **FeatureFlagProvider** to avoid props drilling:

```jsx
import React, { useEffect, useState } from "react";

// Create a Context to manage feature flags
export const FeatureFlag = React.createContext({});

export const FeatureFlagProvider = ({ children }) => {
  const [features, setFeatures] = useState({
    darkMode: true,
    chatEnabled: false
  });

  useEffect(() => {
    // Simulate API call to fetch feature flags
    setTimeout(() => {
      setFeatures({
        darkMode: true,
        chatEnabled: true, // Enable chat feature after fetching
      });
    }, 1000);
  }, []);

  return (
    <FeatureFlag.Provider value={{ features }}>
      {children}
    </FeatureFlag.Provider>
  );
};

const ChatWrapper = () => {
  // Consume the feature flag context
  const { features } = React.useContext(FeatureFlag);
  return features.chatEnabled ? <Chat /> : null;
};

const Chat = () => {
  return <div>Chat Component</div>;
};

const App = () => {
  return (
    <FeatureFlagProvider>
      <ChatWrapper />
    </FeatureFlagProvider>
  );
};

export default App;
```

### **Explanation of the Context API Usage**

1. **Create Context**: The `FeatureFlag` context holds the feature flags (`darkMode`, `chatEnabled`, etc.).
2. **Provider**: The `FeatureFlagProvider` component fetches or manages the state of feature flags and provides them to the rest of the component tree via `<FeatureFlag.Provider>`.
3. **Consumer (useContext)**: Any child component (like `ChatWrapper`) can access the context values using `React.useContext(FeatureFlag)`. This eliminates the need for props drilling.

Now, instead of passing `features` or `isChatEnabled` down manually through every level of components, we access them directly from the context in any component.

### **Feature Flags Class Implementation**

You’ve also provided an example of a **FeatureFlag** class to manage feature flags manually. This class encapsulates the logic for enabling, disabling, and checking feature flags:

```javascript
class FeatureFlag {
  constructor() {
    this.flags = new Map();
  }

  enableFeature(feature) {
    this.flags.set(feature, true);
  }

  disableFeature(feature) {
    this.flags.set(feature, false);
  }

  isFeatureEnabled(feature) {
    return this.flags.get(feature) || false;
  }
}

// Example usage:
const featureFlag = new FeatureFlag();

// Enable a feature
featureFlag.enableFeature('newFeature');

// Check if a feature is enabled
console.log('Is newFeature enabled?', featureFlag.isFeatureEnabled('newFeature')); // Output: true

// Disable a feature
featureFlag.disableFeature('newFeature');

// Check if a feature is enabled after disabling it
console.log('Is newFeature enabled?', featureFlag.isFeatureEnabled('newFeature')); // Output: false
```

#### **Explanation**:
- **FeatureFlag Class**: Manages feature flags as key-value pairs where the key is the feature name, and the value is a boolean indicating whether the feature is enabled or disabled.
- **Methods**:
  - `enableFeature(feature)`: Enables a feature by setting its flag to `true`.
  - `disableFeature(feature)`: Disables a feature by setting its flag to `false`.
  - `isFeatureEnabled(feature)`: Checks if a feature is enabled.

This class can be useful for managing feature flags in a central place, and it can be integrated into a **React Context** or state management solution like **Redux** to avoid props drilling.

---

### **Why Use Context API Over Prop Drilling?**

Here are a few reasons why using **React Context API** (or similar solutions) is better than props drilling:

1. **Cleaner Code**: By using context, you eliminate the need to manually pass down props through every level of components. The code becomes more readable and easier to maintain.
   
2. **Performance**: If the data is only needed by specific components, using context prevents unnecessary re-renders of intermediate components that don't need the data. It can also help to optimize performance by avoiding redundant data passing.

3. **Scalability**: In larger applications with deeply nested components, context makes it easier to share data globally. Prop drilling becomes harder to maintain as the application grows.

4. **Avoids Redundant Logic**: You don’t need to write the same logic to pass down props at each level, which reduces the potential for bugs, such as missing props or incorrect data passed down.

---

### **When to Use Context API vs. Prop Drilling**

- **Use Context API**:
  - When you need to share state/data across multiple components without explicitly passing props through each intermediate component.
  - Common scenarios include theme settings, user authentication, feature flags, and global state management.

- **Use Prop Drilling**:
  - For simple applications or components where the data only needs to be passed down one or two levels.
  - In situations where the data doesn’t need to be shared globally and is just relevant to a small part of the app.

---

### Conclusion

While **props drilling** works fine in simple cases, it can become inefficient and error-prone as the component hierarchy deepens. **React's Context API** offers a better solution for sharing global state and avoiding the need to pass props down through multiple layers of components. This not only makes the code cleaner but also improves maintainability, scalability, and performance.


# Provider Pattern in React

The **Provider Pattern** is one of the most important React patterns.

It is built on top of **React Context API** and is used to share state globally without prop drilling.

React itself contains internal concepts such as provider/context types in its implementation. [\[persistent...epoint.com\]](https://persistentsystems.sharepoint.com/sites/PersistentLearningandDevelopment/Propeller%20Program/Batch%2024/Propeller%20Group%201/codebase/corporate-learning-system-main/frontend/node_modules/react/umd/react.development.js?web=1), [\[persistent...epoint.com\]](https://persistentsystems.sharepoint.com/sites/PersistentLearningandDevelopment/Propeller%20Program/Batch%2024/Propeller%20Group%201/codebase/corporate-learning-system-main/frontend/node_modules/react/cjs/react-jsx-dev-runtime.development.js?web=1)

***

# Problem Without Provider Pattern

Imagine:

```text
App
 ↓
Dashboard
 ↓
Sidebar
 ↓
UserProfile
```

Passing user data:

```jsx
<App user={user}>
  <Dashboard user={user}>
    <Sidebar user={user}>
      <UserProfile user={user}/>
    </Sidebar>
  </Dashboard>
</App>
```

This is called:

```text
Prop Drilling
```

Not scalable.

***

# Provider Pattern Solution

```text
UserProvider
        ↓
React Context
        ↓
Any Component
```

***

# Complete Example

## 1. Create Context

```jsx
// context/UserContext.js

import {
  createContext,
  useContext,
  useState
} from "react";

const UserContext =
  createContext(null);
```

***

## 2. Create Provider

```jsx
export function UserProvider({
  children
}) {

  const [user, setUser] =
    useState({
      id: 1,
      name: "Sudhir",
      role: "Project Lead"
    });

  const updateUser =
    updatedUser => {
      setUser(updatedUser);
    };

  return (
    <UserContext.Provider
      value={{
        user,
        updateUser
      }}
    >
      {children}
    </UserContext.Provider>
  );
}
```

***

## 3. Create Custom Hook

```jsx
export function useUser() {

  const context =
    useContext(
      UserContext
    );

  if (!context) {
    throw new Error(
      "Must be used within UserProvider"
    );
  }

  return context;
}
```

***

## 4. Wrap App

```jsx
import {
  UserProvider
} from "./context/UserContext";

function App() {
  return (
    <UserProvider>
      <Dashboard />
    </UserProvider>
  );
}
```

***

## 5. Consume Context Anywhere

```jsx
import { useUser }
from "./context/UserContext";

function UserProfile() {

  const { user } =
    useUser();

  return (
    <>
      <h2>{user.name}</h2>

      <p>{user.role}</p>
    </>
  );
}
```

***

# Theme Provider Pattern

Very common in React.

## Theme Context

```jsx
const ThemeContext =
  createContext();
```

***

## Provider

```jsx
export function ThemeProvider({
  children
}) {

  const [theme, setTheme] =
    useState("light");

  const toggleTheme =
    () => {

      setTheme(prev =>
        prev === "light"
          ? "dark"
          : "light"
      );

    };

  return (
    <ThemeContext.Provider
      value={{
        theme,
        toggleTheme
      }}
    >
      {children}
    </ThemeContext.Provider>
  );
}
```

***

## Hook

```jsx
export function useTheme() {
  return useContext(
    ThemeContext
  );
}
```

***

## Consumer

```jsx
function Header() {

  const {
    theme,
    toggleTheme
  } = useTheme();

  return (
    <>
      <button
        onClick={toggleTheme}
      >
        Toggle
      </button>

      <div>
        Theme:
        {theme}
      </div>
    </>
  );
}
```

***

# Multiple Providers

Enterprise apps commonly use:

```jsx
<AppProviders>
```

pattern.

```jsx
function AppProviders({
  children
}) {

  return (

    <ThemeProvider>

      <UserProvider>

        <AuthProvider>

          <NotificationProvider>

            {children}

          </NotificationProvider>

        </AuthProvider>

      </UserProvider>

    </ThemeProvider>

  );
}
```

Usage:

```jsx
root.render(
  <AppProviders>
    <App />
  </AppProviders>
);
```

***

# Provider Pattern for API State

```jsx
const UsersContext =
  createContext();
```

***

```jsx
export function UsersProvider({
  children
}) {

  const [users, setUsers] =
    useState([]);

  useEffect(() => {

    fetchUsers()
      .then(setUsers);

  }, []);

  return (
    <UsersContext.Provider
      value={{
        users
      }}
    >
      {children}
    </UsersContext.Provider>
  );
}
```

***

# Auth Provider (Enterprise Example)

```jsx
const AuthContext =
  createContext();
```

```jsx
export function AuthProvider({
  children
}) {

  const [user, setUser] =
    useState(null);

  const login =
    async credentials => {

      const response =
        await api.login(
          credentials
        );

      setUser(
        response.user
      );
    };

  const logout = () => {
    setUser(null);
  };

  return (

    <AuthContext.Provider
      value={{
        user,
        login,
        logout
      }}
    >
      {children}
    </AuthContext.Provider>

  );
}
```

***

# Performance Optimisation

### Bad

```jsx
value={{
  user,
  updateUser
}}
```

New object every render.

***

### Better

```jsx
const value =
  useMemo(
    () => ({
      user,
      updateUser
    }),
    [user]
  );
```

```jsx
<UserContext.Provider
  value={value}
/>
```

***

# When to Use Provider Pattern

✅ Authentication

✅ Theme Management

✅ Current User

✅ Language Settings

✅ Notifications

✅ Feature Flags

✅ Global App Configuration

✅ Shopping Cart

✅ Dashboard Filters

***

# Interview Questions

### Q1: What is Provider Pattern?

A pattern that uses React Context to share state across components without prop drilling.

***

### Q2: Difference Between Context and Provider?

**Context**

```jsx
createContext()
```

Creates storage.

**Provider**

```jsx
<Context.Provider>
```

Supplies values.

***

### Q3: Why Use Custom Hooks?

```jsx
useUser()
```

Benefits:

* Cleaner code
* Reusable
* Type-safe (TypeScript)
* Centralised logic

***

### Q4: Provider Pattern vs Redux

| Provider            | Redux            |
| ------------------- | ---------------- |
| Small/Medium Apps   | Large Apps       |
| Built into React    | External Library |
| Simple State        | Complex State    |
| Minimal Boilerplate | More Features    |

***

### Q5: Common Providers in Enterprise Apps?

```text
AuthProvider
ThemeProvider
UserProvider
NotificationProvider
SettingsProvider
FeatureFlagProvider
```

***

# Senior React Interview Answer

> The Provider Pattern is a React state-sharing pattern built on Context API. It eliminates prop drilling by placing state in a Provider and allowing any descendant component to consume it through `useContext`. Common implementations include authentication, themes, settings, notifications, and global application state. In enterprise React applications, providers are typically combined into an `AppProviders` component and exposed through custom hooks such as `useAuth()` and `useTheme()` to improve maintainability, reusability, and developer experience.


Let's combine the **Provider Pattern + Dynamic Form + Themed Dashboard Widgets** into a realistic React application.

***

# 1. Form Submission Handling Using Provider Pattern

Instead of managing form state locally, store it globally in a Provider.

## UserProvider

```jsx
// context/UserContext.jsx

import {
  createContext,
  useContext,
  useState,
  useMemo
} from "react";

const UserContext =
  createContext(null);

export function UserProvider({
  children
}) {

  const [users, setUsers] =
    useState([]);

  const addUser = user => {

    setUsers(prev => [
      ...prev,
      {
        id: Date.now(),
        ...user
      }
    ]);
  };

  const value = useMemo(
    () => ({
      users,
      addUser
    }),
    [users]
  );

  return (
    <UserContext.Provider
      value={value}
    >
      {children}
    </UserContext.Provider>
  );
}

export function useUsers() {
  return useContext(
    UserContext
  );
}
```

***

## Dynamic Form with Validation

```jsx
import React,
{
  useState
} from "react";

import {
  useUsers
} from "./context/UserContext";

export default function UserForm() {

  const { addUser } =
    useUsers();

  const [form,
         setForm] =
    useState({
      name: "",
      email: ""
    });

  const [errors,
         setErrors] =
    useState({});

  const validate = () => {

    const validationErrors =
      {};

    if (
      !form.name.trim()
    ) {
      validationErrors.name =
        "Name required";
    }

    if (
      !/\S+@\S+\.\S+/.test(
        form.email
      )
    ) {
      validationErrors.email =
        "Invalid email";
    }

    setErrors(
      validationErrors
    );

    return (
      Object.keys(
        validationErrors
      ).length === 0
    );
  };

  const handleSubmit =
    e => {

      e.preventDefault();

      if (!validate()) return;

      addUser(form);

      setForm({
        name: "",
        email: ""
      });
    };

  return (
    <form
      onSubmit={
        handleSubmit
      }
    >

      <input
        value={form.name}
        placeholder="Name"
        onChange={e =>
          setForm(prev => ({
            ...prev,
            name:
              e.target.value
          }))
        }
      />

      {errors.name &&
        <p>
          {errors.name}
        </p>
      }

      <input
        value={form.email}
        placeholder="Email"
        onChange={e =>
          setForm(prev => ({
            ...prev,
            email:
              e.target.value
          }))
        }
      />

      {errors.email &&
        <p>
          {errors.email}
        </p>
      }

      <button
        type="submit"
      >
        Save User
      </button>

    </form>
  );
}
```

***

# User List

```jsx
import {
  useUsers
} from "./context/UserContext";

function UserList() {

  const { users } =
    useUsers();

  return (
    <>
      <h2>Users</h2>

      {users.map(user => (
        <div
          key={user.id}
        >
          {user.name}
          {" - "}
          {user.email}
        </div>
      ))}
    </>
  );
}
```

***

# 2. Theme Multiple Widgets Together

A common enterprise dashboard pattern.

***

## Theme Provider

```jsx
// context/ThemeContext.jsx

import {
  createContext,
  useContext,
  useState,
  useMemo
} from "react";

const ThemeContext =
  createContext(null);

const themes = {

  light: {
    background:
      "#ffffff",

    text:
      "#000000",

    card:
      "#f4f4f4"
  },

  dark: {
    background:
      "#1a1a1a",

    text:
      "#ffffff",

    card:
      "#333333"
  }
};

export function ThemeProvider({
  children
}) {

  const [mode,
         setMode] =
    useState("light");

  const toggleTheme =
    () => {

      setMode(prev =>
        prev === "light"
          ? "dark"
          : "light"
      );
    };

  const value = useMemo(
    () => ({
      theme:
        themes[mode],
      mode,
      toggleTheme
    }),
    [mode]
  );

  return (
    <ThemeContext.Provider
      value={value}
    >
      {children}
    </ThemeContext.Provider>
  );
}

export const useTheme =
  () =>
    useContext(
      ThemeContext
    );
```

***

## Reusable Widget Component

```jsx
import {
  useTheme
} from "./context/ThemeContext";

function Widget({
  title,
  value
}) {

  const { theme } =
    useTheme();

  return (
    <div
      style={{
        background:
          theme.card,

        color:
          theme.text,

        padding: "20px",

        borderRadius:
          "10px",

        width: "250px"
      }}
    >

      <h3>{title}</h3>

      <h1>{value}</h1>

    </div>
  );
}

export default Widget;
```

***

## Dashboard

```jsx
import Widget from "./Widget";

function Dashboard() {

  return (
    <div
      style={{
        display: "flex",
        gap: "20px"
      }}
    >

      <Widget
        title="Users"
        value="1250"
      />

      <Widget
        title="Revenue"
        value="$80K"
      />

      <Widget
        title="Orders"
        value="564"
      />

    </div>
  );
}
```

***

## Theme Toggle

```jsx
import {
  useTheme
} from "./context/ThemeContext";

function Header() {

  const {
    mode,
    toggleTheme
  } = useTheme();

  return (
    <button
      onClick={
        toggleTheme
      }
    >
      Current Theme:
      {mode}
    </button>
  );
}
```

***

# App Composition

```jsx
import {
  ThemeProvider
} from "./context/ThemeContext";

import {
  UserProvider
} from "./context/UserContext";

function App() {

  return (

    <ThemeProvider>

      <UserProvider>

        <Header />

        <UserForm />

        <UserList />

        <Dashboard />

      </UserProvider>

    </ThemeProvider>

  );
}

export default App;
```

***

# Senior React Interview Explanation

### Why Provider Pattern Here?

```text
ThemeProvider
  ↓
All Widgets Receive Theme

UserProvider
  ↓
Form Updates Global State
  ↓
User List Updates Automatically
```

### Benefits

✅ No prop drilling

✅ Centralised state management

✅ Easy theme switching

✅ Reusable widgets

✅ Scalable dashboard architecture

✅ Clean separation of concerns

### Real Enterprise Use Cases

```text
AuthProvider
ThemeProvider
UserProvider
NotificationProvider
FeatureFlagProvider
SettingsProvider
DashboardProvider
```

This is the exact pattern commonly used in large React applications with dashboards, admin portals, banking systems, and SaaS products.



# 1. Add Form Reset + Success Message

Enhance the previous `UserForm`.

```jsx
import { useState } from "react";
import { useUsers } from "./context/UserContext";

const initialState = {
  name: "",
  email: ""
};

export default function UserForm() {
  const { addUser } = useUsers();

  const [form, setForm] =
    useState(initialState);

  const [errors, setErrors] =
    useState({});

  const [success, setSuccess] =
    useState("");

  const validate = () => {
    const validationErrors = {};

    if (!form.name.trim()) {
      validationErrors.name =
        "Name is required";
    }

    if (
      !/\S+@\S+\.\S+/.test(
        form.email
      )
    ) {
      validationErrors.email =
        "Invalid email";
    }

    setErrors(validationErrors);

    return (
      Object.keys(
        validationErrors
      ).length === 0
    );
  };

  const handleSubmit = e => {
    e.preventDefault();

    if (!validate()) return;

    addUser(form);

    // Success Message
    setSuccess(
      `User ${form.name} created successfully`
    );

    // Reset Form
    setForm(initialState);

    setErrors({});

    setTimeout(() => {
      setSuccess("");
    }, 3000);
  };

  const resetForm = () => {
    setForm(initialState);
    setErrors({});
    setSuccess("");
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        value={form.name}
        placeholder="Name"
        onChange={e =>
          setForm(prev => ({
            ...prev,
            name: e.target.value
          }))
        }
      />

      {errors.name && (
        <p style={{ color: "red" }}>
          {errors.name}
        </p>
      )}

      <input
        value={form.email}
        placeholder="Email"
        onChange={e =>
          setForm(prev => ({
            ...prev,
            email: e.target.value
          }))
        }
      />

      {errors.email && (
        <p style={{ color: "red" }}>
          {errors.email}
        </p>
      )}

      {success && (
        <p
          style={{
            color: "green"
          }}
        >
          {success}
        </p>
      )}

      <button type="submit">
        Save
      </button>

      <button
        type="button"
        onClick={resetForm}
      >
        Reset
      </button>
    </form>
  );
}
```

***

# 2. Sync Theme with System Preference

Modern applications automatically detect:

```text
Dark Mode
Light Mode
```

from:

```javascript
window.matchMedia()
```

***

## ThemeProvider

```jsx
import {
  createContext,
  useState,
  useEffect,
  useContext
} from "react";

const ThemeContext =
  createContext();

export function ThemeProvider({
  children
}) {

  const getSystemTheme = () =>
    window.matchMedia(
      "(prefers-color-scheme: dark)"
    ).matches
      ? "dark"
      : "light";

  const [theme,
         setTheme] =
    useState(
      getSystemTheme()
    );

  useEffect(() => {

    const mediaQuery =
      window.matchMedia(
        "(prefers-color-scheme: dark)"
      );

    const handleChange =
      e => {

        setTheme(
          e.matches
            ? "dark"
            : "light"
        );
      };

    mediaQuery.addEventListener(
      "change",
      handleChange
    );

    return () => {
      mediaQuery.removeEventListener(
        "change",
        handleChange
      );
    };

  }, []);

  const toggleTheme =
    () => {

      setTheme(prev =>
        prev === "light"
          ? "dark"
          : "light"
      );
    };

  return (
    <ThemeContext.Provider
      value={{
        theme,
        toggleTheme
      }}
    >
      {children}
    </ThemeContext.Provider>
  );
}

export const useTheme =
  () =>
    useContext(
      ThemeContext
    );
```

***

## Usage

```jsx
function Header() {

  const {
    theme,
    toggleTheme
  } = useTheme();

  return (
    <button
      onClick={toggleTheme}
    >
      Theme: {theme}
    </button>
  );
}
```

***

# 3. Add New Widget Types

Instead of:

```jsx
<Widget />
```

create a widget registry.

***

## Base Widget

```javascript
const widgetPrototype = {
  width: 300,
  height: 200,
  refreshInterval: 30000,
  showHeader: true
};
```

***

## Revenue Widget

```javascript
const revenueWidget = {
  ...widgetPrototype,

  type: "revenue",

  title: "Revenue",

  value: "$120K"
};
```

***

## User Widget

```javascript
const userWidget = {
  ...widgetPrototype,

  type: "users",

  title: "Users",

  value: "1250"
};
```

***

## Chart Widget

```javascript
const chartWidget = {
  ...widgetPrototype,

  type: "chart",

  title: "Monthly Growth",

  data: [
    20,
    40,
    50,
    75
  ]
};
```

***

# Widget Factory

```jsx
function WidgetRenderer({
  widget
}) {

  switch (
    widget.type
  ) {

    case "revenue":
      return (
        <RevenueWidget
          config={widget}
        />
      );

    case "users":
      return (
        <UsersWidget
          config={widget}
        />
      );

    case "chart":
      return (
        <ChartWidget
          config={widget}
        />
      );

    default:
      return null;
  }
}
```

***

# Revenue Widget

```jsx
function RevenueWidget({
  config
}) {

  return (
    <div>
      <h3>
        {config.title}
      </h3>

      <h1>
        {config.value}
      </h1>
    </div>
  );
}
```

***

# User Widget

```jsx
function UsersWidget({
  config
}) {

  return (
    <div>
      <h3>
        {config.title}
      </h3>

      <h1>
        {config.value}
      </h1>
    </div>
  );
}
```

***

# Chart Widget

```jsx
function ChartWidget({
  config
}) {

  return (
    <div>
      <h3>
        {config.title}
      </h3>

      <pre>
        {JSON.stringify(
          config.data
        )}
      </pre>
    </div>
  );
}
```

***

# Dashboard

```jsx
const widgets = [
  revenueWidget,
  userWidget,
  chartWidget
];

function Dashboard() {

  return (
    <div
      style={{
        display: "flex",
        gap: "20px"
      }}
    >
      {widgets.map(
        (widget, index) => (
          <WidgetRenderer
            key={index}
            widget={widget}
          />
        )
      )}
    </div>
  );
}
```

***

# Enterprise Extension Strategy

When adding a new widget:

```text
WidgetConfig
        ↓
WidgetRenderer
        ↓
New Widget Component
```

Example:

```text
RevenueWidget
UserWidget
ChartWidget
TableWidget
KPIWidget
NotificationWidget
MapWidget
```

No changes needed elsewhere.

***

# Senior React Interview Answer

For enterprise dashboards:

✅ Forms should support validation, reset, and success/error feedback.

✅ Theme providers should synchronise with system preferences using `prefers-color-scheme`.

✅ Widgets should follow the Prototype + Factory pattern, where all widgets inherit common configuration and a central renderer dynamically chooses the appropriate widget implementation. This makes dashboards highly extensible and maintainable for large-scale applications.
