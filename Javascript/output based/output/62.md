# File 62

```js
// This is a JavaScript Quiz from BFE.dev


console.log(1)

const mc = new MessageChannel()

mc.port1.onmessage = () => {
  console.log(2)
}

Promise.resolve().then(() => {
  console.log(3)
})

setTimeout(() => {
  console.log(4)
}, 0)

console.log(5)

mc.port2.postMessage('')

console.log(6)
```

**Output:**

```javascript
1
5
6
3
2
4

```

---

### Step-by-Step Breakdown

**Core Event Loop Lifecycle:**

1. **Synchronous Execution (Call Stack):**

* `console.log(1)` runs $\rightarrow$ logs **`1`**.
* `mc.port1.onmessage` event handler is registered.
* `Promise.resolve().then(...)` schedules its callback in the **Microtask Queue**.
* `setTimeout(..., 0)` registers a timer with the timer subsystem (subject to minimal clamping $\ge 1\text{ms}$).
* `console.log(5)` runs $\rightarrow$ logs **`5`**.
* `mc.port2.postMessage('')` schedules a task immediately in the **Task Queue (Macrotask Queue)** without timer delay.
* `console.log(6)` runs $\rightarrow$ logs **`6`**.

1. **Microtask Queue (Emptied before any new Macrotask):**

* The Promise `.then()` callback executes $\rightarrow$ logs **`3`**.

1. **Task Queue (Macrotasks):**

* **MessageChannel Task:** The `postMessage` task is already queued and ready $\rightarrow$ `mc.port1.onmessage` fires and logs **`2`**.
* **Timer Task:** The `setTimeout(..., 0)` task executes $\rightarrow$ logs **`4`**.
