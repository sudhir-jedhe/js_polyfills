const obj = {}
const fun = () => {}
console.log(obj.toString === Object.toString)
console.log(fun.toString === Function.toString)
console.log(obj.toString === Object.prototype.toString)
console.log(fun.toString === Function.prototype.toString)
console.log(Object.toString === Object.prototype.toString)
console.log(Function.toString === Function.prototype.toString)

**Output:**

```javascript
false
true
true
true
false
true

```

---

**Step-by-Step Breakdown:**

1. **`obj.toString === Object.toString` $\rightarrow$ `false**`

* `obj.toString` resolves to `Object.prototype.toString`.
* `Object` is a constructor function, so `Object.toString` is inherited from `Function.prototype.toString`.
* `Object.prototype.toString !== Function.prototype.toString`.

1. **`fun.toString === Function.toString` $\rightarrow$ `true**`

* Both `fun` (a function instance) and `Function` (a constructor function) inherit `toString` from `Function.prototype`.

1. **`obj.toString === Object.prototype.toString` $\rightarrow$ `true**`

* Plain objects (`{}`) inherit directly from `Object.prototype`.

1. **`fun.toString === Function.prototype.toString` $\rightarrow$ `true**`

* Functions inherit directly from `Function.prototype`.

1. **`Object.toString === Object.prototype.toString` $\rightarrow$ `false**`

* `Object.toString` comes from `Function.prototype.toString` (because `Object` is a function), which is different from `Object.prototype.toString` (the general object-to-string converter).

1. **`Function.toString === Function.prototype.toString` $\rightarrow$ `true**`

* `Function` inherits from `Function.prototype`, so `Function.toString` directly references `Function.prototype.toString`.
