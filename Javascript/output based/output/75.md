# File 75

```js
// This is a JavaScript Quiz from BFE.dev 

const num = +((~~!+[])+(~~!+[])+[]+(~~!+[]))
console.log(num)
```

```python
# We can test the exact JS logic equivalence in Python
plus_empty_array = 0
not_plus_empty_array = not bool(plus_empty_array) # true
bitwise_not_twice = int(not_plus_empty_array) # 1

# (1 + 1 + [] + 1)
# 1 + 1 = 2
# 2 + "" = "2"
# "2" + 1 = "21"
# + "21" = 21
print(int("2" + str(1)))


```

```text
21


```

**Output:**

```javascript
21

```

---

### Step-by-Step Breakdown

1. **Evaluating `(~~!+[])`:**

* `+[]` $\rightarrow$ Unary plus converts `[]` to a number: `0`.
* `!0` $\rightarrow$ Logical NOT on `0` yields `true`.
* `~~true` $\rightarrow$ Double bitwise NOT coerces `true` to a 32-bit integer: `1`.
* Thus, each `(~~!+[])` evaluates to `1`.

1. **Evaluating the Addition Chain from Left to Right:**

* Substitute each part: `(1 + 1 + [] + 1)`
* `1 + 1` $\rightarrow$ `2` (number addition)
* `2 + []` $\rightarrow$ `2 + ""` $\rightarrow$ `"2"` (array coercion to string triggers string concatenation)
* `"2" + 1` $\rightarrow$ `"21"` (string concatenation)

1. **Unary `+` on the Final String:**

* `+("21")` $\rightarrow$ Converts the string `"21"` to the number **`21`**.
