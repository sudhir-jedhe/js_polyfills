# File 12

```js
function log(a,b,c,d) {
  console.log(a,b,c,d)
  arguments[0] = 'bfe'
  arguments[3] = 'dev'
 
  console.log(a,b,c,d)
}

log(1,2,3)
```

**Output:**

```javascript
1 2 3 undefined
bfe 2 3 undefined

```

---

### Step-by-Step Breakdown

1. **First `console.log(a, b, c, d)`:**

* Function is invoked with 3 arguments: `log(1, 2, 3)`.
* Parameters take their mapped values: `a = 1`, `b = 2`, `c = 3`.
* Parameter `d` receives no argument, so it defaults to `undefined`.
* Logs: `1 2 3 undefined`.

1. **Non-strict Mode `arguments` Object Linkage:**

* In non-strict mode, parameters that **received an argument at invocation** (`a`, `b`, `c`) share a live, two-way sync with their respective index in the `arguments` object (`arguments[0]`, `arguments[1]`, `arguments[2]`).
* **`arguments[0] = 'bfe'`**: Modifying `arguments[0]` directly mutates parameter `a`, making `a = 'bfe'`.
* **`arguments[3] = 'dev'`**: Parameter `d` was **not passed** during the call (`arguments.length` is 3). Because no initial argument was bound to index `3`, **no live linkage exists** between `arguments[3]` and variable `d`.
* `arguments[3]` gets assigned `'dev'`, but parameter `d` remains untouched as `undefined`.

1. **Second `console.log(a, b, c, d)`:**

* `a` is now `'bfe'`, `b` is `2`, `c` is `3`, and `d` is still `undefined`.
* Logs: `bfe 2 3 undefined`.

---

### Step-by-Step Event Loop Lifecycle

```
========================================================================================
EVENT LOOP LIFECYCLE BREAKDOWN
========================================================================================

1. CALL STACK (Synchronous Execution):
   - `log(1, 2, 3)` is pushed onto the Call Stack.
   - First `console.log(1, 2, 3, undefined)` executes and pops.
   - Mutates `arguments[0]` (syncing `a`) and `arguments[3]`.
   - Second `console.log('bfe', 2, 3, undefined)` executes and pops.
   - `log()` finishes and is popped from the Call Stack.

2. MICROTASK QUEUE:
   - (Empty) - No promises or microtasks.

3. MACROTASK QUEUE:
   - (Empty) - No timers or asynchronous tasks.

* Execution completes entirely within a single synchronous turn.
========================================================================================

```

```js

```
