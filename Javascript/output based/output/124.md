# File 124

```js
const a = (1,2,3)
console.log(a)
```

**Output:**

```javascript
3

```

---

### Step-by-Step Breakdown

**Core Rule (Comma Operator `,`):**

* The **comma operator (`,`)** evaluates each of its operands from left to right sequentially and returns the value of the **last operand**.
* Parentheses `(1, 2, 3)` group the expressions and ensure the comma operator is evaluated as a single expression before assignment.

---

1. The expression `(1, 2, 3)` is evaluated from left to right:

* Evaluates `1`.
* Evaluates `2`.
* Evaluates `3`.

1. The entire expression resolves to the value of the final operand: **`3`**.
2. `a` is assigned `3`.
3. `console.log(a)` logs **`3`**.
