# File 34

```js
// This is a JavaScript Quiz from BFE.dev

let a = 1
console.log(a +++ a)

let b = 1
console.log(b + + + b)

let c = 1
console.log(c --- c)

let d = 1
console.log(d - - - d)

```

**Output:**

```javascript
3
2
-1
0

```

---

### Step-by-Step Breakdown

**Core Parsing Rule (Maximal Munch):**
JavaScript's lexical tokenizer greedily matches the longest possible valid operator sequence before moving on.

* `+++` is tokenized as `++` (postfix/prefix increment) followed by `+` (addition/unary plus).
* `---` is tokenized as `--` (postfix/prefix decrement) followed by `-` (subtraction/unary minus).

---

1. **`a +++ a` $\rightarrow$ `3**`

* Tokenized as: `(a++) + a`
* Left operand evaluates first: `a++` yields the current value **`1`**, and increments variable `a` to `2`.
* Right operand evaluates next: `a` is now **`2`**.
* Addition: `1 + 2` $\rightarrow$ **`3`**.

1. **`b + + + b` $\rightarrow$ `2**`

* Tokenized with explicit spacing as: `b + (+ (+b))`
* `b` remains `1` throughout (no mutation occurs).
* Unary operations: `+ (+1)` $\rightarrow$ `1`.
* Addition: `1 + 1` $\rightarrow$ **`2`**.

1. **`c --- c` $\rightarrow$ `-1**`

* Tokenized as: `(c--) - c`
* Left operand evaluates first: `c--` yields the current value **`1`**, and decrements variable `c` to `0`.
* Right operand evaluates next: `c` is now **`0`**.
* Subtraction: `1 - 0` $\rightarrow$ **`1`** (Wait, let's verify: `(c--) - c` $\rightarrow$ `1 - 0 = 1`).

Wait, let's check `c --- c`:

* `c --- c` parses as `(c--) - c` or `c - (--c)`?
* Maximal munch on `c --- c`:
* After identifier `c`, the parser consumes the longest token: `--` (postfix decrement).
* Next token is `-` (binary minus).
* Expression is `(c--) - c`.
* `c` starts at `1`.
* `c--` evaluates to `1`, and `c` becomes `0`.
* Then `- c` evaluates to `- 0`.
* `1 - 0 = 1`.

Let's double-check `c --- c`:
Wait! In JS tokenizer, after `c`, does it take `--`? Yes.
So `c -- - c` = `(c--) - c`.
Wait, why did I write `-1` in the output block above? Let's fix that!
Let's calculate:
`let c = 1; c --- c`

1. `c++` or `c--`:
`c = 1`
`c--` returns `1`, side effect: `c = 0`.
Then binary `-`: `1 - (current c)` = `1 - 0 = 1`.

Let's check `d - - - d`:
`d - (- (- d))`
`d` is 1.
`- (- 1)` = `+1`.
`d - 1` = `1 - 1 = 0`.

Let's re-verify `a +++ a`:
`a = 1`
`(a++) + a` $\rightarrow$ `1 + 2 = 3`.

Let's re-verify `b + + + b`:
`b + (+ (+ b))` $\rightarrow$ `1 + 1 = 2`.

Let's re-verify `c --- c`:
`c = 1`
`(c--) - c` $\rightarrow$ `1 - 0 = 1`.

Let's re-verify `d - - - d`:
`d = 1`
`d - (- (- d))` $\rightarrow$ `1 - (- (-1)) = 1 - 1 = 0`.

Let's write out the clean final response.**Output:**

```javascript
3
2
1
0

```

---

### Step-by-Step Breakdown

**Core Parsing Rule (Maximal Munch):**
JavaScript's lexical tokenizer greedily consumes the longest possible valid operator sequence before moving to the next token:

* `+++` is tokenized as `++` (postfix increment) followed by `+` (binary addition).
* `---` is tokenized as `--` (postfix decrement) followed by `-` (binary subtraction).

---

1. **`a +++ a` $\rightarrow$ `3**`

* Tokenized as: `(a++) + a`
* Left operand evaluates first: `a++` yields the original value **`1`**, and increments variable `a` to `2`.
* Right operand evaluates next: `a` is now **`2`**.
* Binary addition: `1 + 2` $\rightarrow$ **`3`**.

1. **`b + + + b` $\rightarrow$ `2**`

* Tokenized as: `b + (+ (+b))`
* Variable `b` is never mutated and remains `1`.
* Unary operations: `+ (+1)` $\rightarrow$ `1`.
* Binary addition: `1 + 1` $\rightarrow$ **`2`**.

1. **`c --- c` $\rightarrow$ `1**`

* Tokenized as: `(c--) - c`
* Left operand evaluates first: `c--` yields the original value **`1`**, and decrements variable `c` to `0`.
* Right operand evaluates next: `c` is now **`0`**.
* Binary subtraction: `1 - 0` $\rightarrow$ **`1`**.

1. **`d - - - d` $\rightarrow$ `0**`

* Tokenized as: `d - (- (-d))`
* Variable `d` is never mutated and remains `1`.
* Unary operations: `- (-1)` $\rightarrow$ `1`.
* Binary subtraction: `1 - 1` $\rightarrow$ **`0`**.
