# File 37

```js
// This is a JavaScript Quiz from BFE.dev

const arr = [1,2]
arr.push(3,4)
arr.unshift(5,6)
console.log(arr)
```

**Output:**

```javascript
[5, 6, 1, 2, 3, 4]

```

---

### Step-by-Step Breakdown

1. **`const arr = [1, 2]`**

* Initializes the array: `[1, 2]`.

1. **`arr.push(3, 4)`**

* Appends `3` and `4` to the end of the array in the order provided.
* `arr` becomes: `[1, 2, 3, 4]`.

1. **`arr.unshift(5, 6)`**

* Inserts `5` and `6` at the beginning of the array as a single batch, preserving their argument order (`5` at index `0`, `6` at index `1`).
* `arr` becomes: `[5, 6, 1, 2, 3, 4]`.

1. **`console.log(arr)`**

* Logs the final array: `[5, 6, 1, 2, 3, 4]`.
