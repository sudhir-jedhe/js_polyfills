Here are the exact inputs, outputs, and explanations for each screenshot:

---

### 1. Function Overwriting vs Variable Assignment (`Screenshot 2026-08-24 at 5.41.51 AM`)

**Code:**

```javascript
var x = 1;
console.log(x);
function x() {
  console.log("2");
}
x();

```

* **Output:**

```text
1
TypeError: x is not a function

```

* **Explanation:**

1. Function declaration `function x() {}` is hoisted first.
2. `var x` declaration is hoisted (no-op because identifier `x` already exists).
3. At runtime, line 1 (`var x = 1`) executes and assigns the number `1` to `x`.
4. `console.log(x)` logs `1`.
5. Line 6 (`x()`) attempts to invoke `1` as a function, throwing a `TypeError: x is not a function`.

---

### 2. Array Element Mutation via Reference (`Screenshot 2026-08-24 at 5.45.39 AM`)

**Code:**

```javascript
var arrA = [0];
var arrB = arrA;
arrB[0] = 42;
console.log(arrA);

```

* **Output:** `[42]`
* **Explanation:**
* `arrB` holds a reference to the same array in memory as `arrA`.
* Modifying index 0 via `arrB[0] = 42` directly mutates the shared underlying array.
* `console.log(arrA)` logs `[42]`.

---

### 3. Event Loop (`setTimeout` vs Synchronous Log) (`Screenshot 2026-08-24 at 5.42.36 AM`)

**Code:**

```javascript
setTimeout(() => {
  console.log("1");
}, 0);

console.log("2");

```

* **Output:**

```text
2
1

```

* **Explanation:**
* `console.log("2")` runs immediately on the synchronous call stack.
* The `setTimeout` callback enters the macrotask queue and executes only after the current synchronous execution frame completes.

---

### 4. Mutating `const` Object Properties (`Screenshot 2026-08-24 at 5.42.41 AM`)

**Code:**

```javascript
const data = {
  id: "1"
};
data.id = "2";

console.log(data.id);

```

* **Output:** `"2"`
* **Explanation:**
* `const` prevents variable reassignment (`data = ...`), but does not freeze the object itself.
* Mutating properties on a `const` object (`data.id = "2"`) is completely valid in JavaScript.

---

### 5. Synchronous Promise Executor (`Screenshot 2026-08-24 at 5.42.20 AM`)

**Code:**

```javascript
new Promise(() => {
  console.log("d");
});
console.log("s");

```

* **Output:**

```text
d
s

```

* **Explanation:**
* The executor function passed to `new Promise(...)` runs **synchronously** when the promise is instantiated.
* `"d"` logs first, followed immediately by `"s"`.

---

### 6. Array Shallow Copy via `slice()` (`Screenshot 2026-08-24 at 5.41.59 AM`)

**Code:**

```javascript
var arrA = [1, 2, 3];
var arrB = arrA.slice();
arrB[0] = 0;
console.log(arrA);

```

* **Output:** `[1, 2, 3]`
* **Explanation:**
* `arrA.slice()` creates a shallow copy of `arrA` at a new memory address.
* Mutating `arrB[0] = 0` only modifies `arrB` and leaves `arrA` untouched.

---

### 7. The `delete` Operator on Variables (`Screenshot 2026-08-24 at 5.44.09 AM`)

**Code:**

```javascript
const name = "saikrishna";
age = 21;
console.log(delete name);
console.log(delete age);

```

* **Output:**

```text
false
true

```

* **Explanation:**
* `delete` is meant to delete configurable object properties, not declared variables.
* `delete name`: Variables declared with `const`, `let`, or `var` cannot be deleted $\to$ returns `false` (or throws in strict mode).
* `delete age`: `age = 21` creates an undeclared property on the global object (`window`/`global`), which is configurable and deletable $\to$ returns `true`.

---

### 8. Duplicate Keys in Object Literal (`Screenshot 2026-08-24 at 5.43.02 AM`)

**Code:**

```javascript
const data = {
  name: "sai",
  name: "krishna"
};

console.log(data.name);

```

* **Output:** `"krishna"`
* **Explanation:**
* When duplicate keys are defined in an object literal, the last key assignment overwrites any previous ones.

---

### 9. Object Method `this` Context (`Screenshot 2026-08-24 at 5.45.11 AM`)

**Code:**

```javascript
var person = {
  name: "Sam",
  hello: function() {
    console.log(this.name);
  }
};

person.hello();

```

* **Output:** `"Sam"`
* **Explanation:**
* `person.hello()` invokes `hello` as a method on `person`.
* `this` binds to the `person` object, accessing `person.name` (`"Sam"`).

---

### 10. `var` vs `let` Block Scoping (`Screenshot 2026-08-24 at 5.43.18 AM`)

**Code:**

```javascript
var x = 1;
let y = 1;
{
  var x = 2;
  let y = 2;
}
console.log(x, y);

```

* **Output:** `2 1`
* **Explanation:**
* `var` is function/globally scoped. `var x = 2` inside `{ ... }` overwrites the outer `x = 1`.
* `let` is block-scoped. `let y = 2` exists only inside `{ ... }` and disappears when leaving the block, leaving outer `let y = 1` intact.

Here are the exact inputs, outputs, and explanations for each screenshot:

---

### 1. String Immutability (`Screenshot 2026-08-24 at 5.42.48 AM`)

**Snippet:**

```javascript
let str = "jscafe";
str[0] = "c";

console.log(str);

```

* **Output:** `"jscafe"`
* **Explanation:**
* Strings in JavaScript are primitive and **immutable**.
* Modifying individual characters via indexing (`str[0] = "c"`) fails silently in non-strict mode (or throws a `TypeError` in strict mode). The string remains unchanged.

---

### 2. Object Property Mutation via Reference (`Screenshot 2026-08-24 at 5.45.31 AM`)

**Snippet:**

```javascript
var objA = { prop: 42 };
var objB = objA;
objB.prop = 90;
console.log(objA);

```

* **Output:** `{ prop: 90 }`
* **Explanation:**
* `objB` and `objA` hold a reference to the same object in memory.
* Mutating `objB.prop = 90` directly updates the underlying object, reflecting in `objA`.

---

### 3. Synchronous `clearTimeout` (`Screenshot 2026-08-24 at 5.43.13 AM`)

**Snippet:**

```javascript
let timer = setTimeout(() => {
  console.log("hello");
}, 0);

clearTimeout(timer);

```

* **Output:** *(Nothing is logged)*
* **Explanation:**
* `clearTimeout(timer)` runs synchronously on the call stack before the macrotask queue can process the `setTimeout` callback, cancelling its execution.

---

### 4. Relational Operator Chaining (`Screenshot 2026-08-24 at 5.44.51 AM`)

**Snippet:**

```javascript
let x = 10 > 9 > 8;
console.log(x === true);

```

* **Output:** `false`
* **Explanation:**
* Relational operators evaluate left-to-right:

1. `10 > 9` evaluates to `true`.
2. `true > 8` coerces `true` to the number `1` $\to 1 > 8$ evaluates to `false`.

* `x` is `false`, so `false === true` evaluates to `false`.

---

### 5. String `.length` Property Immutability (`Screenshot 2026-08-24 at 5.42.27 AM`)

**Snippet:**

```javascript
let str = "sai";
str.length = 0;
console.log(str.length);

```

* **Output:** `3`
* **Explanation:**
* The `length` property of a primitive string is **read-only**.
* Attempting to assign `str.length = 0` is ignored in non-strict mode.

---

### 6. `Object.freeze()` (`Screenshot 2026-08-24 at 5.42.16 AM`)

**Snippet:**

```javascript
const box = {
  x: 5,
  y: 10
};
Object.freeze(box);
box.x = 100;

console.log(box);

```

* **Output:** `{ x: 5, y: 10 }`
* **Explanation:**
* `Object.freeze()` makes the object's properties immutable and prevents adding, modifying, or deleting properties.
* `box.x = 100` fails silently in non-strict mode (or throws in strict mode), leaving `box.x` as `5`.

---

### 7. Deleting an Array Element (`Screenshot 2026-08-24 at 5.42.45 AM`)

**Snippet:**

```javascript
const x = [1, 2, 3];
delete x[1];
console.log(x.length);

```

* **Output:** `3`
* **Explanation:**
* The `delete` operator removes the value at index `1` and leaves an empty slot (`[1, <1 empty item>, 3]`), creating a sparse array.
* It does not re-index the array or decrease `x.length`.

---

### 8. Reference Retained in Array (`Screenshot 2026-08-24 at 5.42.03 AM`)

**Snippet:**

```javascript
let person = { name: 'sai' };
const members = [person];
person = null;

console.log(members);

```

* **Output:** `[ { name: 'sai' } ]`
* **Explanation:**
* `members[0]` holds a direct reference to the object in heap memory.
* Setting `person = null` only clears the `person` variable's pointer; the object itself still exists in memory and remains referenced by `members[0]`.

---

### 9. Making `(x == 1 && x == 2 && x == 3)` True (`Screenshot 2026-08-24 at 5.43.44 AM`)

**Snippet:**

```javascript
let x;

if (x == 1 && x == 2 && x == 3) {
  console.log("Hello");
}

```

* **Solution:**

```javascript
let x = {
  val: 1,
  valueOf() {
    return this.val++;
  }
};

```

* **Explanation:**
* Loose equality (`==`) coerces objects to primitives by invoking their `valueOf()` or `toString()` methods.
* Each comparison calls `valueOf()`, which returns the current `val` and increments it ($1 \to 2 \to 3$).

---

### 10. `var` in a `for` Loop with `setTimeout` (`Screenshot 2026-08-24 at 5.43.49 AM`)

**Snippet:**

```javascript
for (var i = 0; i < 10; i++) {
  setTimeout(function() {
    console.log(i);
  });
}

```

* **Output:** Prints `10` ten times (each on a new line).
* **Explanation:**
* `var i` is function/globally scoped, meaning all 10 timeout callbacks share the same single `i` variable in memory.
* The synchronous loop completes first, leaving `i = 10`.
* When the 10 asynchronous `setTimeout` callbacks execute, they all read the final value of `i`, which is `10`.
Here are the inputs, outputs, and step-by-step explanations for each screenshot:

---

### 1. String `.length` Immutability (`Screenshot 2026-08-24 at 5.45.14 AM`)

**Snippet:**

```javascript
let str = "sai";
str.length = 0;
console.log(str.length);

```

* **Output:** `3`
* **Explanation:**
* The `length` property of a primitive string is read-only.
* Attempting to assign `str.length = 0` fails silently in non-strict mode (or throws a `TypeError` in strict mode), leaving `str.length` as `3`.

---

### 2. Implicit Global Assignment & Addition (`Screenshot 2026-08-24 at 5.44.46 AM`)

**Snippet:**

```javascript
y = 10;
var x = 1 + y;
console.log(x);

```

* **Output:** `11`
* **Explanation:**
* Assigning `y = 10` creates an undeclared property on the global object.
* `1 + 10` evaluates to `11`.
* `console.log(x)` logs `11`.

---

### 3. Array Addition String Coercion (`Screenshot 2026-08-24 at 5.44.58 AM`)

**Snippet:**

```javascript
let a = [1];
let b = [2];
console.log(a + b);

```

* **Output:** `"12"`
* **Explanation:**
* The binary `+` operator coerces both arrays to strings via `.toString()`: `[1].toString()` $\to$ `"1"`, `[2].toString()` $\to$ `"2"`.
* String concatenation `"1" + "2"` evaluates to `"12"`.

---

### 4. Function Default Parameters with `null` (`Screenshot 2026-08-24 at 5.41.55 AM`)

**Snippet:**

```javascript
function sum(a = 5, b = 7) {
  console.log(a + b);
}

sum(null, 20);

```

* **Output:** `20`
* **Explanation:**
* Default parameters only trigger when an argument is `undefined` or omitted.
* Because `null` is explicitly passed for `a`, the default value `5` is not applied.
* In addition, `null` coerces to `0`: `0 + 20 = 20`.

---

### 5. Postfix Increment Addition (`Screenshot 2026-08-24 at 5.43.21 AM`)

**Snippet:**

```javascript
let a = 5;
let b = a++;
console.log(a + b);

```

* **Output:** `11`
* **Explanation:**
* `b = a++` assigns the current value of `a` (`5`) to `b`, then increments `a` from $5 \to 6$.
* `a + b` evaluates to $6 + 5 = 11$.

---

### 6. IIFE with Boolean Type Coercion (`Screenshot 2026-08-24 at 5.42.52 AM`)

**Snippet:**

```javascript
(function(num) {
  console.log(num * num);
})(false);

```

* **Output:** `0`
* **Explanation:**
* Passing `false` to the function sets `num = false`.
* The multiplication operator `*` coerces `false` to the numeric value `0`.
* `0 * 0` evaluates to `0`.

---

### 7. String Primitive Immutability (`Screenshot 2026-08-24 at 5.43.40 AM`)

**Snippet:**

```javascript
let str = "jscafe";
str[0] = "c";

console.log(str);

```

* **Output:** `"jscafe"`
* **Explanation:**
* Primitive strings cannot be mutated via bracket indexing (`str[0] = "c"`).
* The operation is ignored, leaving the original string unchanged.

---

### 8. Array to String Loose Equality (`Screenshot 2026-08-24 at 5.44.32 AM`)

**Snippet:**

```javascript
const arr1 = [1, 2, 3];
const str = "1,2,3";
console.log(arr1 == str);

```

* **Output:** `true`
* **Explanation:**
* Loose equality `==` coerces `arr1` to a primitive via `[1, 2, 3].toString()`, which returns `"1,2,3"`.
* Comparing `"1,2,3" == "1,2,3"` yields `true`.

---

### 9. Variable Scope & Undeclared Assignment (`Screenshot 2026-08-24 at 5.42.34 AM`)

**Snippet:**

```javascript
function data() {
  let a = b = 5;
}
data();

console.log(b);

```

* **Output:** `5` (in non-strict mode) / `ReferenceError: b is not defined` (in strict mode)
* **Explanation:**
* Evaluated as `b = 5; let a = b;`.
* `b` is assigned without a declaration keyword, creating an undeclared global variable accessible in the outer scope.

---

### 10. Hoisting with `var` and `NaN` Increment (`Screenshot 2026-08-24 at 5.42.30 AM`)

**Snippet:**

```javascript
x++;
console.log(x);
var x = 21;

```

* **Output:** `NaN`
* **Explanation:**

1. `var x` declaration is hoisted to the top and initialized to `undefined`.
2. `x++` evaluates `undefined + 1`, which results in `NaN`.
3. `console.log(x)` logs `NaN` before the assignment `x = 21` is reached.

Here are the exact inputs, outputs, and step-by-step explanations for each screenshot:

---

### 1. `NaN` Equality (`Screenshot 2026-08-24 at 5.45.18 AM`)

**Snippet:**

```javascript
const x = NaN;
const y = NaN;
console.log(x === y);

```

* **Output:** `false`
* **Explanation:**
* In JavaScript, `NaN` (Not-a-Number) is defined by IEEE 754 floating-point standards as not equal to any value, including itself.
* Both `NaN === NaN` and `NaN == NaN` always evaluate to `false`. *(To test for `NaN`, use `Number.isNaN()` or `Object.is(x, y)`).*

---

### 2. Postfix Increment Addition (`Screenshot 2026-08-24 at 5.41.47 AM`)

**Snippet:**

```javascript
let a = 5;
let b = a++;
console.log(a + b);

```

* **Output:** `11`
* **Explanation:**
* `b = a++` assigns the current value of `a` (`5`) to `b`, then increments `a` from $5 \to 6$.
* `a + b` evaluates to $6 + 5 = 11$.

---

### 3. Postfix and Prefix Increment (`Screenshot 2026-08-24 at 5.43.35 AM` / `5.45.07 AM`)

**Snippet:**

```javascript
let x = 1;
let y = 2;
console.log(x++ + ++y);

```

* **Output:** `4`
* **Explanation:**
* `x++` (postfix): returns `1` for the addition expression, then increments `x` to `2`.
* `++y` (prefix): increments `y` from $2 \to 3$ immediately and returns `3`.
* The sum is $1 + 3 = 4$.

---

### 4. `var` Hoisting from Dead Code Block (`Screenshot 2026-08-24 at 5.43.09 AM`)

**Snippet:**

```javascript
if (false) {
  var a = "sai";
}

console.log(a);

```

* **Output:** `undefined`
* **Explanation:**
* `var` is function/globally scoped and hoisted to the top of its scope regardless of block conditions, initialized as `undefined`.
* Because `if (false)` never executes, the assignment `a = "sai"` is skipped.
* `console.log(a)` logs `undefined`.

---

### 5. Empty Array vs Boolean Loose Equality (`Screenshot 2026-08-24 at 5.42.59 AM`)

**Snippet:**

```javascript
let a = [];
let b = false;
console.log(a == b);

```

* **Output:** `true`
* **Explanation:**
* Loose equality `==` coerces both sides to numbers:

1. `b` (`false`) coerces to `0`.
2. `a` (`[]`) coerces to primitive string `""` via `[].toString()`.
3. `""` coerces to number `0`.

* Comparing `0 == 0` evaluates to `true`.

---

### 6. String `.replace()` Method (`Screenshot 2026-08-24 at 5.45.54 AM`)

**Snippet:**

```javascript
let str = "Change";
str = str.replace("C", "R");
console.log(str);

```

* **Output:** `"Rhange"`
* **Explanation:**
* `str.replace("C", "R")` creates and returns a new string replacing the first matched `"C"` with `"R"`.
* Assigning it back to `str` logs `"Rhange"`.

---

### 7. `.push()` Return Value (`Screenshot 2026-08-24 at 5.44.12 AM`)

**Snippet:**

```javascript
let newList = [1].push(2);
console.log(newList.push(3));

```

* **Output:** `TypeError: newList.push is not a function`
* **Explanation:**
* `Array.prototype.push()` returns the **length** of the modified array (`2`), not the array itself.
* Therefore, `newList` holds the number primitive `2`.
* Attempting to call `.push()` on a number throws a `TypeError`.

---

### 8. Sparse Array Iteration with `.forEach()` (`Screenshot 2026-08-24 at 5.44.02 AM`)

**Snippet:**

```javascript
const x = [];
x[4] = 1;
x.forEach((i) => {
  console.log("Hi");
});

```

* **Output:** `"Hi"` (logged only once)
* **Explanation:**
* Assigning `x[4] = 1` creates an array of length 5 with empty/unassigned slots at indices `0, 1, 2, 3`.
* `Array.prototype.forEach` skips unassigned/empty slots and only executes its callback for defined elements (index `4`).

---

### 9. Array In-Place Mutation with `.reverse()` (`Screenshot 2026-08-24 at 5.43.31 AM`)

**Snippet:**

```javascript
let arr = [1, 2];
let rev = arr.reverse();
console.log(arr, rev);

```

* **Output:** `[2, 1] [2, 1]`
* **Explanation:**
* `Array.prototype.reverse()` mutates the original array in place **and** returns a reference to the same array.
* Both `arr` and `rev` point to the identical reversed array `[2, 1]`.

Here are the snippets, outputs, and explanations for each image:

---

### 1. `x.substring(5, 1)` (`Screenshot 2026-08-24 at 5.42.56 AM`)

**Snippet:**

```javascript
let x = "Learn";
console.log(x.substring(5, 1));

```

* **Output:** `"earn"`
* **Explanation:**
* `String.prototype.substring(start, end)` automatically swaps arguments if `start > end`.
* `x.substring(5, 1)` becomes `x.substring(1, 5)`.
* Characters from index `1` to `4` (`"e"`, `"a"`, `"r"`, `"n"`) are extracted, returning `"earn"`.

---

### 2. Operator Precedence (`Screenshot 2026-08-24 at 5.46.17 AM` & `5.46.12 AM`)

**Snippet:**

```javascript
let x = 10 + 20 * 3;
console.log(x);

```

* **Output:** `70`
* **Explanation:**
* Multiplication (`*`) has higher precedence than addition (`+`).
* `20 * 3 = 60`, then `10 + 60 = 70`.

---

### 3. Arrow Function and `this` Context (`Screenshot 2026-08-24 at 5.46.03 AM`)

**Snippet:**

```javascript
const arrowFunc = () => {
  console.log(this.count);
};
arrowFunc();

```

* **Output:** `undefined`
* **Explanation:**
* Arrow functions inherit `this` lexically from their enclosing scope (here, the global execution context).
* `count` is not defined on the global object (`window`/`global`), so `this.count` evaluates to `undefined`.

---

### 4. `var` Hoisting with Postfix Increment (`Screenshot 2026-08-24 at 5.44.05 AM`)

**Snippet:**

```javascript
x++;
console.log(x);
var x = 21;

```

* **Output:** `NaN`
* **Explanation:**
* `var x` declaration is hoisted to the top as `undefined`.
* `x++` computes `undefined + 1`, which produces `NaN`.
* `console.log(x)` logs `NaN`.

---

### 5. `NaN` Strict Equality (`Screenshot 2026-08-24 at 5.43.25 AM`)

**Snippet:**

```javascript
const x = NaN;
const y = NaN;
console.log(x === y);

```

* **Output:** `false`
* **Explanation:**
* In JavaScript, `NaN` is not equal to any value, including itself (`NaN === NaN` is `false`).

---

### 6. String Subtraction Coercion (`Screenshot 2026-08-24 at 5.45.50 AM`)

**Snippet:**

```javascript
let val = "5" - 2;
console.log(val);

```

* **Output:** `3`
* **Explanation:**
* The subtraction operator (`-`) only exists for numeric operations and forces string-to-number conversion.
* `"5"` is coerced to the number `5`, so `5 - 2` evaluates to `3`.

---

### 7. Array and Logical NOT Coercion (`Screenshot 2026-08-24 at 5.45.02 AM`)

**Snippet:**

```javascript
console.log([1, 2] + ![]);

```

* **Output:** `"1,2false"`
* **Explanation:**

1. `![]`: An array is truthy, so `![]` evaluates to `false`.
2. `[1, 2] + false`: The `+` operator coerces `[1, 2]` to `"1,2"` via `.toString()`.
3. String concatenation `"1,2" + false` produces `"1,2false"`.

---

### 8. Addition with String Concatenation (`Screenshot 2026-08-24 at 5.44.55 AM`)

**Snippet:**

```javascript
console.log(3 + 4 + "5");

```

* **Output:** `"75"`
* **Explanation:**

1. Evaluated left-to-right: `3 + 4` performs numeric addition $\to 7$.
2. `7 + "5"` performs string concatenation $\to `"75"`.
