*** copy 1.md ***

The **JavaScript `Math` object** is a built-in namespace object that provides mathematical constants and functions.

Unlike other built-in standard objects, `Math` is **not a constructor**. You cannot instantiate it with `new Math()` or call it as a function—all of its properties and methods are static.

---

## 1. Math Properties (Constants)

`Math` provides built-in constants for common mathematical values.

```javascript
// Example 1: Math.PI (Ratio of a circle's circumference to its diameter)
console.log(Math.PI); 
// Output: 3.141592653589793

// Example 2: Math.E (Euler's number) and Math.LN10 (Natural log of 10)
console.log(Math.E);    // Output: 2.718281828459045
console.log(Math.LN10); // Output: 2.302585092994046

// Example 3: Math.SQRT2 (Square root of 2)
console.log(Math.SQRT2); 
// Output: 1.4142135623730951

```

---

## 2. Rounding Methods

JavaScript offers four primary methods to round numbers depending on your needs.

```javascript
// Example 1: Math.round() - Rounds to nearest integer
console.log(Math.round(4.5)); // Output: 5
console.log(Math.round(4.4)); // Output: 4

// Example 2: Math.ceil() (Rounds UP) & Math.floor() (Rounds DOWN)
console.log(Math.ceil(4.1));  // Output: 5
console.log(Math.floor(4.9)); // Output: 4

// Example 3: Math.trunc() - Truncates fractional part (returns integer part)
console.log(Math.trunc(4.9));  // Output: 4
console.log(Math.trunc(-4.9)); // Output: -4

```

---

## 3. Power, Root, and Absolute Methods

```javascript
// Example 1: Math.pow(x, y) - Raises x to power y (equivalent to x ** y)
console.log(Math.pow(2, 3)); 
// Output: 8

// Example 2: Math.sqrt(x) & Math.cbrt(x) - Square and Cube roots
console.log(Math.sqrt(64)); // Output: 8
console.log(Math.cbrt(27)); // Output: 3

// Example 3: Math.abs(x) - Absolute (positive) value
console.log(Math.abs(-12.5)); 
// Output: 12.5

```

---

## 4. Min, Max, and Random Methods

```javascript
// Example 1: Math.min() & Math.max()
console.log(Math.min(10, 20, 5, 30)); // Output: 5
console.log(Math.max(10, 20, 5, 30)); // Output: 30

// Example 2: Using Math.max with Spread Operator on Arrays
const scores = [88, 92, 79, 95];
console.log(Math.max(...scores)); 
// Output: 95

// Example 3: Math.random() - Generates pseudo-random float between 0 (inclusive) and 1 (exclusive)
// Generate random integer between min (1) and max (10) inclusive:
function getRandomInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
console.log(getRandomInt(1, 10)); 
// Output: Random integer between 1 and 10

```

---

## 5. Trigonometric & Sign Methods

```javascript
// Example 1: Math.sin(), Math.cos(), Math.tan() (Arguments in radians)
console.log(Math.sin(Math.PI / 2)); 
// Output: 1

// Example 2: Math.sign() - Returns 1, -1, 0, -0, or NaN indicating sign
console.log(Math.sign(-50)); // Output: -1
console.log(Math.sign(50));  // Output: 1
console.log(Math.sign(0));   // Output: 0

// Example 3: Math.hypot() - Calculates square root of sum of squares (Pythagoras)
console.log(Math.hypot(3, 4)); 
// Output: 5 (sqrt(3² + 4²))

```

In JavaScript, standard numbers are represented as **64-bit floating-point values** (IEEE 754 standard). This causes well-known precision issues with decimal arithmetic (like `0.1 + 0.2 !== 0.3`).

To handle numbers beyond IEEE 754 limits or perform exact integer calculations, ES2020 introduced **`BigInt`**.

Here is a breakdown of how floating-point precision works, how to fix its issues, and how to work with `BigInt`.

---

## 1. Understanding Floating-Point Precision Issues

JavaScript stores numbers in binary floating-point format. Certain base-10 decimals (like `0.1` or `0.2`) cannot be represented exactly in binary fractions and turn into repeating fractions, causing rounding errors.

```javascript
console.log(0.1 + 0.2); 
// Output: 0.30000000000000004

console.log(0.1 + 0.2 === 0.3); 
// Output: false

```

### How to Fix Floating-Point Issues

#### Method A: Convert Decimals to Integers (Cents Pattern)

Multiply by a factor of 10 to turn decimals into integers, perform operations, and divide back. This is the standard pattern for financial calculations.

```javascript
// Add $0.10 and $0.20 accurately by calculating in cents
const cents1 = 0.1 * 100;
const cents2 = 0.2 * 100;
const total = (cents1 + cents2) / 100;

console.log(total); // Output: 0.3

```

#### Method B: `toFixed()` or `toPrecision()`

Use `toFixed(n)` to round output for display purposes (note: `toFixed` returns a string).

```javascript
const sum = 0.1 + 0.2;
const formattedSum = Number(sum.toFixed(2));

console.log(formattedSum); // Output: 0.3

```

#### Method C: Using `Number.EPSILON`

`Number.EPSILON` represents the smallest difference between two representable numbers. Use it to perform safe equality comparisons.

```javascript
function numbersAreEqual(a, b) {
  return Math.abs(a - b) < Number.EPSILON;
}

console.log(numbersAreEqual(0.1 + 0.2, 0.3)); // Output: true

```

---

## 2. Working with `BigInt`

`BigInt` is a primitive data type used to represent **arbitrarily large integers** beyond the safe integer limit of standard numbers (`Number.MAX_SAFE_INTEGER`, which is $2^{53} - 1 = 9,007,199,254,740,991$).

### Creating a `BigInt`

Append `n` to an integer literal or call the `BigInt()` constructor function.

```javascript
// Example 1: BigInt literal
const big1 = 9007199254740995n;

// Example 2: Constructor from string
const big2 = BigInt("9007199254740995");

// Example 3: Exceeding Number limit accurately
const safeNum = Number.MAX_SAFE_INTEGER + 2; // Inaccurate floating point
const safeBig = BigInt(Number.MAX_SAFE_INTEGER) + 2n; // Accurate BigInt

console.log(safeNum); // Output: 9007199254740992 (Inaccurate)
console.log(safeBig); // Output: 9007199254740993n (Exact)

```

---

## 3. Mathematical Operations with `BigInt`

`BigInt` supports standard arithmetic operators (`+`, `-`, `*`, `/`, `%`, `**`), but **truncates fractional values** since it only represents integers.

```javascript
// Example 1: Basic arithmetic
const a = 100n;
const b = 3n;

console.log(a + b); // Output: 103n
console.log(a * b); // Output: 300n
console.log(a ** b); // Output: 1000000n

// Example 2: Integer division truncates decimals towards zero
console.log(5n / 2n); // Output: 2n (NOT 2.5n)

// Example 3: Modulo operator
console.log(5n % 2n); // Output: 1n

```

---

## 4. Key Rules and Pitfalls of `BigInt`

### Rule A: Cannot Mix `BigInt` and Standard `Number`

You cannot perform mathematical operations between `BigInt` and `Number` without explicit coercion. Doing so throws a `TypeError`.

```javascript
try {
  const result = 10n + 5; // Throws TypeError
} catch (err) {
  console.log(err.message); 
  // Output: "Cannot mix BigInt and other types, use explicit conversions"
}

// Correct approach: Cast explicitly
const validResult = 10n + BigInt(5);
console.log(validResult); // Output: 15n

```

### Rule B: Loose Comparison vs. Strict Comparison

`BigInt` and `Number` can be loosely equal (`==`), but not strictly equal (`===`).

```javascript
console.log(10n == 10);  // Output: true
console.log(10n === 10); // Output: false

```

### Rule C: JSON Serialization Error

`JSON.stringify()` cannot serialize `BigInt` out of the box and throws a `TypeError`. You must customize `.toJSON()` or convert it to a string first.

```javascript
const data = { id: 100n };

try {
  JSON.stringify(data);
} catch (err) {
  console.log(err.message); 
  // Output: "Do not know how to serialize a BigInt"
}

// Fix: Override prototype to stringify BigInt values
BigInt.prototype.toJSON = function () {
  return this.toString();
};

console.log(JSON.stringify(data)); 
// Output: '{"id":"100"}'

```

Here is a complete guide to all properties and methods of the JavaScript **`Math`** object, with 3 concrete code examples for each.

---

## Part 1: Math Constants (Properties)

### 1. `Math.E`

*Euler's constant, approximately `2.718`.*

```javascript
// Example 1: Accessing the constant directly
console.log(Math.E); 
// Output: 2.718281828459045

// Example 2: Calculating exponential growth e^2
console.log(Math.E ** 2); 
// Output: 7.38905609893065

// Example 3: Comparing with Math.exp(1)
console.log(Math.E === Math.exp(1)); 
// Output: true

```

---

### 2. `Math.LN2`

*Natural logarithm of 2, approximately `0.693`.*

```javascript
// Example 1: Accessing LN2 constant
console.log(Math.LN2); 
// Output: 0.6931471805599453

// Example 2: Calculating half-life decay constant
const halfLife = 10; // 10 years
const decayRate = Math.LN2 / halfLife;
console.log(decayRate); 
// Output: 0.06931471805599453

// Example 3: Comparing with Math.log(2)
console.log(Math.LN2 === Math.log(2)); 
// Output: true

```

---

### 3. `Math.LN10`

*Natural logarithm of 10, approximately `2.302`.*

```javascript
// Example 1: Accessing LN10 constant
console.log(Math.LN10); 
// Output: 2.302585092994046

// Example 2: Converting base-10 log to natural log
const log10Value = 2;
console.log(log10Value * Math.LN10); 
// Output: 4.605170185988092

// Example 3: Comparing with Math.log(10)
console.log(Math.LN10 === Math.log(10)); 
// Output: true

```

---

### 4. `Math.LOG2E`

*Base-2 logarithm of $E$, approximately `1.442`.*

```javascript
// Example 1: Accessing LOG2E constant
console.log(Math.LOG2E); 
// Output: 1.4426950408889634

// Example 2: Converting natural log to base-2 log
console.log(Math.log(8) * Math.LOG2E); 
// Output: 3

// Example 3: Comparing with Math.log2(Math.E)
console.log(Math.LOG2E === Math.log2(Math.E)); 
// Output: true

```

---

### 5. `Math.LOG10E`

*Base-10 logarithm of $E$, approximately `0.434`.*

```javascript
// Example 1: Accessing LOG10E constant
console.log(Math.LOG10E); 
// Output: 0.4342944819032518

// Example 2: Converting natural log to base-10 log
console.log(Math.log(100) * Math.LOG10E); 
// Output: 2

// Example 3: Comparing with Math.log10(Math.E)
console.log(Math.LOG10E === Math.log10(Math.E)); 
// Output: true

```

---

### 6. `Math.PI`

*Ratio of a circle's circumference to its diameter ($\pi$), approximately `3.14159`.*

```javascript
// Example 1: Circle area calculation (pi * r^2)
const radius = 5;
console.log(Math.PI * radius ** 2); 
// Output: 78.53981633974483

// Example 2: Convert degrees to radians
const degrees = 180;
const radians = degrees * (Math.PI / 180);
console.log(radians); 
// Output: 3.141592653589793

// Example 3: Circumference of circle (2 * pi * r)
console.log(2 * Math.PI * radius); 
// Output: 31.41592653589793

```

---

### 7. `Math.SQRT1_2`

*Square root of $\frac{1}{2}$ ($\approx 0.707$).*

```javascript
// Example 1: Accessing SQRT1_2 constant
console.log(Math.SQRT1_2); 
// Output: 0.7071067811865476

// Example 2: Sine and Cosine of 45 degrees (PI/4) equal SQRT1_2
console.log(Math.sin(Math.PI / 4)); 
// Output: 0.7071067811865476

// Example 3: Comparing with 1 / Math.SQRT2
console.log(Math.SQRT1_2 === 1 / Math.SQRT2); 
// Output: true

```

---

### 8. `Math.SQRT2`

*Square root of 2, approximately `1.414`.*

```javascript
// Example 1: Accessing SQRT2 constant
console.log(Math.SQRT2); 
// Output: 1.4142135623730951

// Example 2: Calculate hypotenuse of isosceles right triangle with side length 1
console.log(1 * Math.SQRT2); 
// Output: 1.4142135623730951

// Example 3: Comparing with Math.sqrt(2)
console.log(Math.SQRT2 === Math.sqrt(2)); 
// Output: true

```

---

## Part 2: Rounding & Truncation Methods

### 9. `Math.abs(x)`

*Returns the absolute (positive) value of `x`.*

```javascript
// Example 1: Absolute value of negative number
console.log(Math.abs(-42)); // Output: 42

// Example 2: Distance between two values
const distance = Math.abs(10 - 25);
console.log(distance); // Output: 15

// Example 3: Converts non-numeric string representations
console.log(Math.abs("-10.5")); // Output: 10.5

```

---

### 10. `Math.ceil(x)`

*Rounds `x` upwards to the nearest integer.*

```javascript
// Example 1: Rounding positive decimals up
console.log(Math.ceil(4.1)); // Output: 5

// Example 2: Rounding negative decimals up (closer to 0)
console.log(Math.ceil(-4.9)); // Output: -4

// Example 3: Page count calculation for pagination
const totalItems = 23, itemsPerPage = 10;
console.log(Math.ceil(totalItems / itemsPerPage)); // Output: 3

```

---

### 11. `Math.f16round(x)`

*Rounds `x` to the nearest 16-bit half-precision float value.*

```javascript
// Example 1: Rounding pi to 16-bit float
console.log(Math.f16round(1.337)); 
// Output: 1.3369140625

// Example 2: High precision numbers reduced to 16-bit float
console.log(Math.f16round(Math.PI)); 
// Output: 3.140625

// Example 3: Edge overflow values become Infinity
console.log(Math.f16round(65536)); 
// Output: Infinity

```

---

### 12. `Math.floor(x)`

*Rounds `x` downwards to the nearest integer.*

```javascript
// Example 1: Rounding positive float down
console.log(Math.floor(4.9)); // Output: 4

// Example 2: Rounding negative float down (away from 0)
console.log(Math.floor(-4.1)); // Output: -5

// Example 3: Integer index calculation
console.log(Math.floor(7 / 2)); // Output: 3

```

---

### 13. `Math.fround(x)`

*Returns the nearest 32-bit single-precision float representation of a number.*

```javascript
// Example 1: Converting 64-bit double to 32-bit float
console.log(Math.fround(1.5)); // Output: 1.5

// Example 2: Precision loss on non-exact binary fraction
console.log(Math.fround(1.337)); // Output: 1.3370000123977661

// Example 3: fround of Math.PI
console.log(Math.fround(Math.PI)); // Output: 3.1415927410125732

```

---

### 14. `Math.round(x)`

*Rounds `x` to the nearest integer.*

```javascript
// Example 1: Standard rounding behavior
console.log(Math.round(3.4)); // Output: 3
console.log(Math.round(3.5)); // Output: 4

// Example 2: Rounding negative half-values (rounds towards +Infinity)
console.log(Math.round(-3.5)); // Output: -3

// Example 3: Rounding to specific decimal places (e.g., 2 places)
const price = 12.3456;
console.log(Math.round(price * 100) / 100); // Output: 12.35

```

---

### 15. `Math.trunc(x)`

*Returns the integer part of `x` by stripping all fractional digits.*

```javascript
// Example 1: Truncating positive floats
console.log(Math.trunc(4.9)); // Output: 4

// Example 2: Truncating negative floats
console.log(Math.trunc(-4.9)); // Output: -4

// Example 3: Truncating string input
console.log(Math.trunc("123.456")); // Output: 123

```

---

## Part 3: Trigonometric Methods

### 16. `Math.acos(x)`

*Returns the arccosine (in radians) of `x` (for $x \in [-1, 1]$).*

```javascript
// Example 1: Arccosine of 1 is 0 radians
console.log(Math.acos(1)); // Output: 0

// Example 2: Arccosine of -1 is PI radians
console.log(Math.acos(-1)); // Output: 3.141592653589793

// Example 3: Values outside [-1, 1] yield NaN
console.log(Math.acos(2)); // Output: NaN

```

---

### 17. `Math.acosh(x)`

*Returns the hyperbolic arccosine of `x` (for $x \ge 1$).*

```javascript
// Example 1: Hyperbolic arccosine of 1
console.log(Math.acosh(1)); // Output: 0

// Example 2: Hyperbolic arccosine of E
console.log(Math.acosh(Math.E)); // Output: 1.6574544541530771

// Example 3: Values less than 1 yield NaN
console.log(Math.acosh(0.5)); // Output: NaN

```

---

### 18. `Math.asin(x)`

*Returns the arcsine (in radians) of `x` (for $x \in [-1, 1]$).*

```javascript
// Example 1: Arcsine of 0 is 0
console.log(Math.asin(0)); // Output: 0

// Example 2: Arcsine of 1 is PI/2
console.log(Math.asin(1)); // Output: 1.5707963267948966

// Example 3: Convert output to degrees
console.log(Math.asin(0.5) * (180 / Math.PI)); // Output: 30

```

---

### 19. `Math.asinh(x)`

*Returns the hyperbolic arcsine of `x`.*

```javascript
// Example 1: Hyperbolic arcsine of 0
console.log(Math.asinh(0)); // Output: 0

// Example 2: Hyperbolic arcsine of 1
console.log(Math.asinh(1)); // Output: 0.881373587019543

// Example 3: Handles negative numbers
console.log(Math.asinh(-1)); // Output: -0.881373587019543

```

---

### 20. `Math.atan(x)`

*Returns the arctangent of `x` as a numeric value between $-\frac{\pi}{2}$ and $\frac{\pi}{2}$ radians.*

```javascript
// Example 1: Arctangent of 0
console.log(Math.atan(0)); // Output: 0

// Example 2: Arctangent of 1 is PI/4
console.log(Math.atan(1)); // Output: 0.7853981633974483

// Example 3: Convert arctangent output to degrees
console.log(Math.atan(1) * (180 / Math.PI)); // Output: 45

```

---

### 21. `Math.atan2(y, x)`

*Returns the arctangent of the quotient of its arguments $\frac{y}{x}$, handling quadrant signs correctly.*

```javascript
// Example 1: Calculate angle to point (1, 1) in radians
console.log(Math.atan2(1, 1)); // Output: 0.7853981633974483 (45 deg)

// Example 2: Calculate angle to point in 3rd quadrant (-1, -1)
console.log(Math.atan2(-1, -1) * (180 / Math.PI)); // Output: -135

// Example 3: Game angle tracking (angle from player to mouse)
const dx = 10, dy = 10;
const angleInRad = Math.atan2(dy, dx);
console.log(angleInRad); // Output: 0.7853981633974483

```

---

### 22. `Math.atanh(x)`

*Returns the hyperbolic arctangent of `x` (for $x \in (-1, 1)$).*

```javascript
// Example 1: Hyperbolic arctangent of 0
console.log(Math.atanh(0)); // Output: 0

// Example 2: Hyperbolic arctangent of 0.5
console.log(Math.atanh(0.5)); // Output: 0.5493061443340548

// Example 3: Values outside (-1, 1) yield NaN or Infinity
console.log(Math.atanh(1)); // Output: Infinity

```

---

### 23. `Math.cos(x)`

*Returns the cosine of `x` (where `x` is in radians).*

```javascript
// Example 1: Cosine of 0 is 1
console.log(Math.cos(0)); // Output: 1

// Example 2: Cosine of PI radians (180 degrees)
console.log(Math.cos(Math.PI)); // Output: -1

// Example 3: Computing x-coordinate on a circle
const radius = 100, angle = Math.PI / 3; // 60 degrees
console.log(radius * Math.cos(angle)); // Output: 50

```

---

### 24. `Math.cosh(x)`

*Returns the hyperbolic cosine of `x`.*

```javascript
// Example 1: Hyperbolic cosine of 0
console.log(Math.cosh(0)); // Output: 1

// Example 2: Hyperbolic cosine of 1
console.log(Math.cosh(1)); // Output: 1.5430806348152437

// Example 3: Catenary curve shape calculation
const x = 2;
console.log((Math.exp(x) + Math.exp(-x)) / 2 === Math.cosh(x)); // Output: true

```

---

### 25. `Math.sin(x)`

*Returns the sine of `x` (where `x` is in radians).*

```javascript
// Example 1: Sine of 0 is 0
console.log(Math.sin(0)); // Output: 0

// Example 2: Sine of PI/2 radians (90 degrees)
console.log(Math.sin(Math.PI / 2)); // Output: 1

// Example 3: Computing y-coordinate on a circle
const radius = 100, angle = Math.PI / 6; // 30 degrees
console.log(radius * Math.sin(angle)); // Output: 49.99999999999999

```

---

### 26. `Math.sinh(x)`

*Returns the hyperbolic sine of `x`.*

```javascript
// Example 1: Hyperbolic sine of 0
console.log(Math.sinh(0)); // Output: 0

// Example 2: Hyperbolic sine of 1
console.log(Math.sinh(1)); // Output: 1.1752011936438014

// Example 3: Odd function property sinh(-x) === -sinh(x)
console.log(Math.sinh(-2) === -Math.sinh(2)); // Output: true

```

---

### 27. `Math.tan(x)`

*Returns the tangent of an angle `x` (in radians).*

```javascript
// Example 1: Tangent of 0 is 0
console.log(Math.tan(0)); // Output: 0

// Example 2: Tangent of PI/4 (45 degrees) is 1
console.log(Math.tan(Math.PI / 4)); // Output: 0.9999999999999999

// Example 3: Calculating triangle opposite side height
const distance = 50, angleRad = Math.PI / 4;
console.log(distance * Math.tan(angleRad)); // Output: 49.99999999999999

```

---

### 28. `Math.tanh(x)`

*Returns the hyperbolic tangent of `x`.*

```javascript
// Example 1: Hyperbolic tangent of 0
console.log(Math.tanh(0)); // Output: 0

// Example 2: Hyperbolic tangent of Infinity is 1
console.log(Math.tanh(Infinity)); // Output: 1

// Example 3: Neural network activation function (bipolar sigmoid)
console.log(Math.tanh(2.5)); // Output: 0.9866142981514303

```

---

## Part 4: Exponential, Logarithmic & Power Methods

### 29. `Math.cbrt(x)`

*Returns the cube root of `x`.*

```javascript
// Example 1: Cube root of 27
console.log(Math.cbrt(27)); // Output: 3

// Example 2: Cube root of negative numbers
console.log(Math.cbrt(-64)); // Output: -4

// Example 3: Cube root of 1
console.log(Math.cbrt(1)); // Output: 1

```

---

### 30. `Math.exp(x)`

*Returns $E^x$, where $E$ is Euler's number.*

```javascript
// Example 1: Math.exp(1) equals E
console.log(Math.exp(1)); // Output: 2.718281828459045

// Example 2: Math.exp(0) equals 1
console.log(Math.exp(0)); // Output: 1

// Example 3: Exponential growth calculation
console.log(Math.exp(2)); // Output: 7.38905609893065

```

---

### 31. `Math.expm1(x)`

*Returns $E^x - 1$ (accurate for very small values of `x`).*

```javascript
// Example 1: expm1 of 0
console.log(Math.expm1(0)); // Output: 0

// Example 2: expm1 of 1
console.log(Math.expm1(1)); // Output: 1.718281828459045

// Example 3: Precision comparison with small values vs exp(x) - 1
const small = 1e-10;
console.log(Math.expm1(small)); // Output: 1.00000000005e-10

```

---

### 32. `Math.log(x)`

*Returns the natural logarithm (base $E$) of `x`.*

```javascript
// Example 1: Natural log of E
console.log(Math.log(Math.E)); // Output: 1

// Example 2: Natural log of 1 is 0
console.log(Math.log(1)); // Output: 0

// Example 3: Log of 0 returns -Infinity
console.log(Math.log(0)); // Output: -Infinity

```

---

### 33. `Math.log10(x)`

*Returns the base-10 logarithm of `x`.*

```javascript
// Example 1: Base-10 log of 100
console.log(Math.log10(100)); // Output: 2

// Example 2: Base-10 log of 1000
console.log(Math.log10(1000)); // Output: 3

// Example 3: Counting digits of an integer
const num = 50000;
console.log(Math.floor(Math.log10(num)) + 1); // Output: 5

```

---

### 34. `Math.log1p(x)`

*Returns the natural logarithm of $1 + x$ (accurate for small values near 0).*

```javascript
// Example 1: log1p of 0
console.log(Math.log1p(0)); // Output: 0

// Example 2: log1p of E - 1
console.log(Math.log1p(Math.E - 1)); // Output: 1

// Example 3: Small decimal precision preservation
console.log(Math.log1p(1e-15)); // Output: 9.999999999999995e-16

```

---

### 35. `Math.log2(x)`

*Returns the base-2 logarithm of `x`.*

```javascript
// Example 1: Base-2 log of 8
console.log(Math.log2(8)); // Output: 3

// Example 2: Base-2 log of 1024
console.log(Math.log2(1024)); // Output: 10

// Example 3: Calculating binary tree height for N nodes
const nodes = 16;
console.log(Math.log2(nodes)); // Output: 4

```

---

### 36. `Math.pow(x, y)`

*Returns `x` raised to the power of `y` ($x^y$).*

```javascript
// Example 1: 2 to the power of 3
console.log(Math.pow(2, 3)); // Output: 8

// Example 2: Fractional powers (Square root equivalent)
console.log(Math.pow(16, 0.5)); // Output: 4

// Example 3: Negative exponent
console.log(Math.pow(2, -2)); // Output: 0.25

```

---

### 37. `Math.sqrt(x)`

*Returns the square root of `x`.*

```javascript
// Example 1: Square root of 25
console.log(Math.sqrt(25)); // Output: 5

// Example 2: Square root of 2
console.log(Math.sqrt(2)); // Output: 1.4142135623730951

// Example 3: Square root of negative number yields NaN
console.log(Math.sqrt(-9)); // Output: NaN

```

---

## Part 5: Utility & Comparison Methods

### 38. `Math.clz32(x)`

*Returns the number of leading zero bits in the 32-bit binary representation of `x`.*

```javascript
// Example 1: clz32 of 1 (binary: 00000000000000000000000000000001)
console.log(Math.clz32(1)); // Output: 31

// Example 2: clz32 of 1000
console.log(Math.clz32(1000)); // Output: 22

// Example 3: clz32 of 0 (all 32 bits are zero)
console.log(Math.clz32(0)); // Output: 32

```

---

### 39. `Math.max(x1, x2, ...)`

*Returns the highest value from the passed arguments.*

```javascript
// Example 1: Max from comma-separated numbers
console.log(Math.max(10, 20, 5, 30)); // Output: 30

// Example 2: Max value in an array using spread operator
const numbers = [42, 100, 15, 8];
console.log(Math.max(...numbers)); // Output: 100

// Example 3: Math.max with negative values
console.log(Math.max(-10, -5, -20)); // Output: -5

```

---

### 40. `Math.min(x1, x2, ...)`

*Returns the lowest value from the passed arguments.*

```javascript
// Example 1: Min from comma-separated numbers
console.log(Math.min(10, 20, 5, 30)); // Output: 5

// Example 2: Min value in an array using spread operator
const scores = [88, 92, 45, 67];
console.log(Math.min(...scores)); // Output: 45

// Example 3: Math.min with negative values
console.log(Math.min(-10, -5, -20)); // Output: -20

```

---

### 41. `Math.random()`

*Returns a pseudo-random floating-point number between `0` (inclusive) and `1` (exclusive).*

```javascript
// Example 1: Generate random float between 0 and 1
console.log(Math.random()); // e.g., 0.48291048201

// Example 2: Generate random integer between 1 and 100
function getRandomInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
console.log(getRandomInt(1, 100)); // e.g., 73

// Example 3: Select random element from an array
const items = ["apple", "banana", "cherry"];
const randomItem = items[Math.floor(Math.random() * items.length)];
console.log(randomItem); // e.g., "banana"

```

---

### 42. `Math.sign(x)`

*Returns `1`, `-1`, `0`, `-0`, or `NaN` indicating the sign of `x`.*

```javascript
// Example 1: Positive and negative inputs
console.log(Math.sign(42));  // Output: 1
console.log(Math.sign(-42)); // Output: -1

// Example 2: Zero inputs
console.log(Math.sign(0));  // Output: 0
console.log(Math.sign(-0)); // Output: -0

// Example 3: Invalid number input
console.log(Math.sign("abc")); // Output: NaN

```
