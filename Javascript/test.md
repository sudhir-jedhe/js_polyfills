
Learn how to extract reusable components with Tailwind

Configure custom fonts, theme colors, and design tokens

Set up dark mode support

Configure dynamic runtime themes and dark mode in v4

Create custom utility classes using @utility directive

Configure monorepos and package workspaces in v4

Explore best practices for @apply vs CSS variables

Build dynamic runtime theme switcher with CSS variables

View complete list of v4 design token variable namespaces

Configure fluid typography and clamp() scales in @theme

Configure container queries with @container in v4
